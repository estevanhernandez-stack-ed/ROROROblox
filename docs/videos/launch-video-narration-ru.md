# Launch video narration — ru (Русский)

Translated from `launch-video-narration-en.md`. Keys and structure unchanged;
every VO and OVERLAY value localized. These are speakable lines for the
multilingual TTS, not listing copy.

**Kept English on purpose:** RoRoRo, Squad Launch, 626 Labs, Microsoft Store,
Roblox, RAM, Windows, Mac — and `Imagine something else.`, the brand tagline.
The kit said to reuse an existing per-language rendering of the tagline; there
is none (it appears in no `listing-copy-*.md` and no `Strings.*.resx`, only as
the English card footer), so rather than invent one it stays English and matches
the footer that ships on the cards.

**Spoken digits:** `626` is voiced as `шесть-два-шесть Labs` in the cta line, and
stays as digits in any OVERLAY.

## Lines

### hook (slide 1)
- OVERLAY: `8 клиентов. 1 ПК.`
- VO: `Это ВОСЕМЬ клиентов Roblox одновременно, на одном ПК. И приложение бесплатное.`

### accounts (slide 2)
- OVERLAY: `сохрани раз. запускай.`
- VO: `Это RoRoRo! Сохрани каждый альт один раз — дальше один клик на каждый. Статус в реальном времени, игра, где каждый сейчас, даже RAM, который ест каждый клиент.`

### squad (slide 3)
- OVERLAY: `тот же сервер. все сразу.`
- VO: `Squad Launch — лучшее здесь. Одна ссылка, и каждый аккаунт попадает на ТОТ ЖЕ приватный сервер, вместе.`

### history (slide 4)
- OVERLAY: `теперь ведёт счёт`
- VO: `И он ведёт счёт! Пик альтов, общее время, таблица лидеров с сериями входов. Всё считается на твоей машине, ничего не уходит.`

### themes (slide 5)
- OVERLAY: `ИИ создаёт тему`
- VO: `Четыре встроенные темы, плюс конструктор тем, где ИИ создаёт тебе свою по описанию настроения.`

### trust (slide 6)
- OVERLAY: `пароль его не касается`
- VO: `Пароль никогда не попадает в приложение, хранилище зашифровано, а контроль памяти перезапускает клиент до утечки. Есть и горячие клавиши.`

### cta (slide 7)
- OVERLAY: `сканируй, открой на ПК`
- VO: `Бесплатно в Microsoft Store, Windows И Mac. Отсканируй код, сохрани страницу, забери на компьютер. RoRoRo, от шесть-два-шесть Labs. Imagine something else.`

## Post caption (for localized social posts)

`Восемь клиентов Roblox на одном ПК — бесплатно. RoRoRo сохраняет твои альты, запускает каждый одним кликом, собирает их на одном приватном сервере и теперь ведёт статистику. Windows + Mac. Ищи RORORO. Не связано с Roblox.`

The not-affiliated sentence is mandatory and is present above.
