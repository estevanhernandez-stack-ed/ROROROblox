# Findings register — Settings + navigation audit

> Stage 1 of a scoped vibe-glow campaign. 95 raw findings from five Opus lenses;
> every one faced a fresh Opus skeptic prompted to refute it. 75 survived, 12 were
> refuted, 8 were absorbed into another finding's fix.
>
> Measuring stick: docs/superpowers/specs/2026-08-04-rororo-settings-navigation-conventions.md
> Ranked by goal-service, then severity x visibility.

## Counts

| lens | raw | survived | downgraded | absorbed | refuted |
|---|---|---|---|---|---|
| visual | 20 | 6 | 7 | 5 | 2 |
| consistency | 16 | 10 | 4 | 2 | 0 |
| qol | 21 | 7 | 12 | 0 | 2 |
| copy | 22 | 15 | 5 | 0 | 2 |
| a11y | 16 | 5 | 4 | 1 | 6 |
| **total** | **95** | **43** | **32** | **8** | **12** |

`survived` + `downgraded` = 75 rows from the audit. Later waves may add rows they
discover; the table above counts the audit only. Register total is now **94**
(F-076, F-077–F-083, F-084, F-085–F-092, F-093–F-094 added after the audit — see
the wave-additions note below).

**Wave additions.** A wave ships only its own batch; anything it discovers becomes
a row here, not a drive-by fix.

- **F-076** — added by wave 2, whose own capture tooling put a live webhook
  credential in a PNG.
- **F-077–F-082** — added by waves 4–6 and the memory investigations.
- **F-083** — added 2026-08-09 as the follow-up to PR #96: `ExpectedClientMb` is a
  constant measured on one machine.
- **F-084** — added 2026-08-09 by F-001's final whole-branch review: eight
  owner-resolution sites gate on `IsLoaded` without `IsVisible`.
- **F-085–F-088** — added 2026-08-10 by the v1.17 flatline cycle's reconciliation
  item. Three are colour literals the theme cannot reach — the Bloxstrap banner,
  the consent sheet's `TryFindResource` fallbacks, the status bar's live-process
  dot. The fourth is a blind spot in the contrast gate itself. Three of the four
  were left unfixed by that item and are rows precisely so they stop living in a
  spec; **F-088 was then fixed and closed by v1.17 item 3a**, added at `/build`
  because the builder chose to fix it in-cycle rather than defer it.
- **F-089** — added 2026-08-10 by item 3a's own fence, on its first run. Four
  un-themed hexes in `App.xaml`'s `SelectionDotStyle` that no open row owned and
  spec §7 does not name. It exists because that fence permits a literal only where
  an open finding already owns one — so the choice was a row or a silent
  permission, and a silent permission is the thing the fence was built to stop.
  **Fixed and closed by v1.17 item 3b**, the same session that opened it: it was
  the last always-visible brand hue on the main window, so leaving it open would
  have shipped a cycle whose headline claim its own captures contradicted.
- **F-090** — added 2026-08-10 by the Phase 2 rendered gate, on its first full
  matrix run. The derived `InteractiveEdgeBrush` clears 1.4.11 against `Navy` and
  lands under it on `RowBg`, where 8 of 14 input call sites actually sit. Notable
  for how it survived: it is not un-themed, not un-tested and not un-reviewed —
  three separate checks each had a principled reason not to look at it, and F-031
  records a ratio that is correct about `Navy` and was read as general. **The first
  row in this register opened by a pixel rather than by arithmetic or by eye.**
- **F-093–F-094** — added 2026-08-10 by the v1.18 Settings cycle's reconciliation
  item. **F-093** is `DefaultPlaceUrl`, documented as editable and referenced by
  the App zero times, surfaced by that cycle's own new reachability fence on its
  first run and exempted there rather than papered over. **F-094** is
  `AlertsStatusLine` painting webhook-deleted failures in the accent; `spec.md > §10`
  set the condition — *"worth a row if it turns out to report failures too"* — and
  reading `AlertStatusLine.Compose` settled it, so the condition is met and the row
  is open. Three further v1.18 findings were considered and **declined** rows with
  reasons on the record; see **Notes carried forward**.

## Status, derived from the rows above on 2026-08-21 (wave 16)

**95 clean · 20 open · 4 closed-as-ruled · 119 total.** Parsed from the last cell of every row.
Two rows went open → clean (F-118, F-119); none added. 93 + 2 = 95, 22 − 2 = 20, total unchanged.

**Both rows were wrong about their own size, in opposite directions, and measuring settled each in
minutes.** F-118 was priced as per-site judgement across 163 sites; classifying them by the ladder's
own definition of "meta" returned 2 mono and 0 uppercase, so the tier was 162 prose and 1 label and
there was almost no judgement to make. F-119 was recorded as a truncated name; it was **hiding three
entire accounts** from a seven-account follow strip, which made following them impossible from the
main window. A row that reads cosmetic can be functional and a row that reads expensive can be
cheap — and the difference cost one script each time.

**The app now has 16 raw font-size declarations in 3 sizes, from 326 in 11.**

The line below, derived earlier on 2026-08-21, read:

**93 clean · 22 open · 4 closed-as-ruled · 119 total.** Parsed from the last cell of every row.
Reconciles against the wave-14 line below: three went open → clean (F-044, F-057, F-115) and two
were added open (F-118, F-119). 90 + 3 = 93, 23 − 3 + 2 = 22, closed unchanged at 4, 117 + 2 = 119.

**Two of the three closed only half of what their row said, and the halves are now separate rows.**
That is deliberate and it is the F-054 → F-112 precedent: a row whose measurable claim shipped gets
closed, and the part that needs per-site judgement gets its own id, because a row that reads
half-true in both directions is the thing this register keeps getting mis-scoped by.

**F-119 is recorded as CAUSED, not discovered.** Raising the 9px follow chips is the F-044 fix and
it is right; it also pushed a strip that was already truncating account names into truncating one
more. The honest version of "we fixed the type" includes what the type cost.

**The cause of F-057 was structural and no row had named it:** the app had 326 font-size
declarations and **no size resource of any kind**. Eleven sizes is not carelessness, it is what
happens when there is nothing to inherit from.

The line below, derived earlier on 2026-08-21, read:

**90 clean · 23 open · 4 closed-as-ruled · 117 total.** Parsed from the last cell of every row that
starts with `| F-`. Reconciles against the wave-13 line below: three rows went open → clean (F-050,
F-090, F-114) and two were added open (F-116, F-117). 87 + 3 = 90, 24 − 3 + 2 = 23, closed unchanged
at 4, 115 + 2 = 117.

**`ContrastPairGateTests` carries no exemptions for the first time since it was built.** Its single
entry was F-050's, and the pair it forgave no longer exists in the app. Recorded here because a gate
with an empty debt list is the only state that cannot quietly rot: every future entry has to argue
for itself against a clean slate rather than join a crowd.

**Two rows closed by re-measuring their own surface first, and one of them found the finding inside
the test suite.** F-050 named "every primary CTA" and the CTA style has been Cyan-on-Navy for some
time — the magenta pair survives on five pills. F-090's fix exposed a fixture constant named
`GoodDivider` that failed the very floor the fixture existed to represent. The register's rule that a
row is a reading rather than a fact now has a companion: **so is a test's fixture.**

The line below, derived 2026-08-20 and superseded, read:

**87 clean · 24 open · 4 closed-as-ruled · 115 total.** Parsed from the last cell of every row that
starts with `| F-`, not adjusted from the line below it. Reconciles against the 2026-08-20 line that
wave 12 left (**81 clean · 27 open · 4 closed · 112 total**): five rows went open → clean (F-018,
F-034, F-038, F-039, F-061), F-113 was added already clean, F-114 and F-115 were added open.
81 + 5 + 1 = 87, 27 − 5 + 2 = 24, closed unchanged at 4, 112 + 3 = 115.

**Two of the five closures re-measured their own row upward before closing it, and both times the
row was low.** F-034 said three brand-leak sites remained; there were four, and the missed one is a
toast title. F-061 said three casing sites; there are five, and the two it missed are instructions
that name the button by its old label. Nothing about either row was wrong when written — both were
reconciled on 2026-08-09 and the app moved underneath them. It is the same lesson F-032 and F-068
have each taught this register: **a count in a row is a reading, not a fact, and it decays.**

The line below, derived 2026-08-12 and superseded, read:

**66 clean · 35 open · 4 closed-as-ruled · 105 total.** (Re-derived 2026-08-12 after the window-render spike added F-100 and process inspection added F-101.) The v1.21 close-out line, still correct as of item 12, read:

**64 clean · 31 open · 4 closed-as-ruled · 99 total.** Derived from the rows on 2026-08-12 by v1.21 item 12, not adjusted from the previous line. The arithmetic against that line reconciles exactly: nine rows went open → clean (F-021, F-022, F-063, F-065, F-066, F-074, F-085, F-086, F-087), F-070 went open → closed-as-ruled, F-095 went open → clean at item 11, and F-099 was added open. 55 + 9 = 64, 41 − 9 − 1 = 31, 3 + 1 = 4, 98 + 1 = 99.

**A ROW WAS INVISIBLE TO THE DERIVATION AGAIN, AND IT WAS NOT A MISSING COLUMN THIS TIME.** The previous line below records F-095 and F-096 hiding behind a dropped *why* column. Deriving this line found **F-091 carrying a literal U+000C FORM FEED** inside `themes\flatline.json`, where the `\f` of the path had been interpreted as a control character. Markdown renders it as whitespace and every editor shows the row intact, so it looks correct in the file and in review — but `splitlines()` treats U+000C as a line break, so any line-based count sees a truncated row with no status cell and skips it silently. Repaired. **The lesson is narrower than "check the columns": a register is a data file, and the check has to be a parse, not a read.** Two consecutive cycles have now derived a scoreboard that was wrong because a row did not survive being split on what looked like a line.

The previous line, superseded, read: **54 clean · 41 open · 3 closed-as-ruled · 98 total**, derived 2026-08-11, not adjusted from the line before it. It read **51 clean · 41 open · 2 closed-as-ruled · 94 total** and was wrong in three directions at once: F-095 and F-096 were written at v1.19 with the *why* column missing, which pushed their status cells out of the last position and made them invisible to a derivation that reads the last cell, and the closed count was one short of the three rows that carry it (F-040, F-069, F-091); when it fell behind is not recoverable from the file and is not guessed at here. The columns are repaired above; this is what the rows actually say.

> ### v1.18 Settings remediation, 2026-08-10 — twelve rows closed, one refused
>
> Counted from the status cells with the same parse `ContrastPairGateTests.NoExemptionOutlivesItsFinding`
> uses, not carried forward from the line above it. The cycle scoped **thirteen** rows to close;
> **twelve** did, each verified against the tree rather than against a commit message.
>
> **Closed:** F-019, F-020, F-023, F-024, F-026, F-033, F-037, F-043, F-051, F-053, F-062, F-078.
>
> **Refused: F-046 stays `open`.** Its destructive rank shipped and three enumerated sites carry it,
> but the row's own headline comparison still renders — `Plugins/PluginsWindow.xaml:258-268` Remove is
> a hand-rolled magenta fill and still the loudest control on its screen, and the 2026-08-10
> re-verification defined this row's "assigned by consequence" clause by exactly that site. The cycle
> held its line at three sites deliberately rather than dragging F-068's forward, which was correct;
> it is still not a closed row. Same handling F-001 got when wave 3 left three items outstanding.
>
> **Counts that moved, recorded with direction per this file's own rule.** F-068 **61 to 55** sites,
> **24 to 22** files — the first movement in five days, and five of the six came from a sweep that
> adopted an existing rank rather than hand-copying attributes, which would have moved it the other
> way. F-079 `Background="#"` literals **12 to 11**, `Modals/` total hex **54 to 52**.
> `AllowedXamlLiteralCeiling` **97 to 95**. F-086's gate coverage **44 elements across 18 files to 39
> across 16**, pairs unchanged at 8 — a good fix shrank a gate's field of view by 5 elements and
> nothing announced it, which is F-086's own thesis arriving from the opposite direction.
>
> **F-050 was not touched and its row is byte-identical.** Verified by diff, not by eye.
> **F-052's 137-declaration backlog is whole** — the cycle named only the controls it added, stated
> as a rule in `spec.md > §3.4` so the number stays honest. F-068 and F-091 likewise un-nibbled.

The pass the v1.18 cycle was scoped off, hours before it started, is preserved below it.

> ### Re-verified 2026-08-10 — all 51+6 open rows checked against the tree
>
> A full re-verification pass ran over every open row before scoping the next remediation cycle.
> Per-row detail is preserved in
> [`2026-08-10-register-reverification/`](2026-08-10-register-reverification/). Verdicts across 57
> rows: **24 accurate · 22 drifted · 5 partly shipped · 5 already fixed · 1 superseded.**
>
> **Six rows should not have been open.** F-027, F-042, F-055, F-056 and F-058 were fixed by other
> waves' artifacts and nobody flipped them; F-069 was closed as a class by the 2026-08-04 ruling and
> is F-040's twin, the precedent `CLAUDE.md` already cites for *rulings override rows*. Five more are
> partly shipped. **Eleven of 57 rows — nearly a fifth — would have mis-scoped a cycle read off this
> table.** That is the F-001 failure at scale, and it is why this pass exists.
>
> **The drift is faster than the re-reads.** F-018's citation was reconciled on 2026-08-09 and was
> already stale by 2026-08-10. F-085 was written by v1.17 item 6 and falsified by item 3a of the same
> cycle, the same day. A row is not stale after a month; it is stale after an afternoon.
>
> **Rows whose numbers moved are annotated in place with the new figure and the direction**, per this
> file's own rule. The 22 drifted rows' line citations were verified and are recorded in the batch
> reports; they are not all transcribed back into the table, because line numbers drift again on the
> next commit and the batch reports name the content to search for instead.
 Counted from the status
cells, not carried forward from a previous summary. The v1.17 flatline cycle's
reconciliation item changed no row's status and added four; item 3a then closed
one of those four (F-088) and added F-089; item 3b closed F-089. The rendered gate
built after that cycle added F-090, and the C2 walkthrough added F-091 and F-092. Net: total +7,
`clean` +2, `open` +5.
**F-050 stays `open` on purpose** —
that cycle shipped a theme that does not need the exemption, which is not the same
thing as implementing F-050's fix direction. See its row.

### Reconciliation pass, 2026-08-09

The register had drifted from the tree, and it drifted in a way worth naming:
**glow waves maintain their own rows; work that lands outside a wave does not.**
Five rows were describing problems that no longer existed, every one fixed by
work outside a wave's own batch — the memory wave (PR #96), the Settings→Games
rename, and wave 3 closing F-036 sideways. A sixth was sitting open against a
ruling.

This is not bookkeeping. It cost a build cycle: F-001 was picked as the top open
finding at 5/5, and roughly 60% of it had already shipped in wave 3. The scope was
three menu items, not a container. Anyone picking from a stale register pays that
again.

**Flipped to clean** (each verified against the tree, not a changelog):

- **F-028** — the Settings→Games rename settled button/title/class/folder, and the
  ~310-char subtitle is gone. Fixed outside any wave.
- **F-080** — `LiveProcessSummary` is bound in `MainWindow.xaml`. Shipped in PR #96.
- **F-081** — the exit log now carries exit code and uptime. Shipped in PR #96.
- **F-082** — `BelowReserve` exists on the snapshot and the watchdog logs
  `memory headroom crossed`. Shipped in PR #96, and watched firing on a real
  oversubscribed machine on 2026-08-09 (545 MB free against a 2615 MB reserve).
- **F-036** — both the header pitch and the status-bar tagline are gone;
  `MainWindow.xaml:988-1000` is now a comment explaining the removal. Closed
  sideways by wave 3.
- **F-032** — the label token moved to `WhiteBrush`, quiet secondary now carried by
  weight plus the F-031 edge, fenced by `MutedTextFenceTests`.

**Credit and a caveat on method.** F-036 was not found by this pass. It was found
by **PR #93**, an earlier reconciliation opened 2026-08-05 that never merged and
went stale while the register drifted further. This pass filtered on three things
— rows citing files that no longer exist, rows whose claims are mechanically
countable, and the top rows by severity. F-036 fell outside all three. A fourth
filter caught the rest: **finding IDs cited in source comments**, which is a strong
signal that code addressed a row. Every open row cited in code was checked; only
F-026 came back genuinely open, and its comment says so explicitly.

**So this pass is not proof the other 52 are real.** It is proof that six were not.
The honest position is that the register is now materially more accurate than it
was, not that it is verified end to end.

**Closed as ruled:**

- **F-040** — its fix direction is “Delete both codepoints, no replacement.” The
  rulings section below closed that entire class on 2026-08-04 and states it
  overrides the register. The row should never have stayed open; it was sitting
  against a decision already made.

**Still open, evidence corrected** (see the reconciliation notes appended to each
row): F-018 and F-039 are each half-shipped by F-001; F-021's citation moved with
the Games rename; F-042 has drifted **worse** since the audit.

**F-032 deserved separate mention while it was open.** It measured 11 controls binding
`MutedTextBrush` as `Foreground`; by the reconciliation pass `MainWindow.xaml` alone had 15.
Waves 5 and 6 added contrast machinery around it without stopping the leak spreading. It has
since been fixed and fenced — but an open row is not a static thing, and nothing was watching
that one grow.

**Added:** F-084, the hidden-owner defect class, surfaced by F-001's final review.

**The rule this suggests:** any PR that closes a register row flips the row in the
same PR, wave or not. Otherwise the register measures wave activity rather than the
app.

Closed by wave 1, verified 2026-08-04: F-006, F-008.
Closed by wave 2 (merged `159fe4b`): F-002, F-003.
Closed by wave 3 (merged `3fc838b`): F-009, F-010, F-011, F-012.
Closed standalone (merged `9587ee6b`): F-076.
Closed by wave 4: F-004, F-005, F-007, F-014, F-015, F-016, F-017 — all four campaign goals now have shipped work.

> This line said **6 clean · 70 open** for about ten minutes. It was accurate when
> written and wrong by the time it merged, because F-076 landed in a second PR
> right behind this one. Re-derived from the rows rather than patched by one —
> which is the whole reason the rule says derive, never carry forward. Leaving the
> note because a scoreboard that silently self-corrects teaches nobody anything.

**Wave 1's rows (F-006, F-008) are now verified and closed** — against the tree,
not the changelog:

- **F-006** — `GamesWindow.xaml:6` `Title="RoRoRo -- Games"`, `:344` header
  `Text="Games"`, and the exact prescribed tooltip ("Saved games and private
  servers for the launch picker.") on the toolbar's Games button at
  `MainWindow.xaml:1123`. The only surviving "Library" in the app is inside a
  code comment. *(Evidence pointer re-verified 2026-08-11: the tooltip string is
  unchanged and F-006 stays closed, but it rode the control. It was on a Tools
  `MenuItem` when this row was written, and v1.20 moved Games back out to a peer
  toolbar button. The cited line said `:1102` and had been wrong for two cycles.)*
- **F-008** — the streamer toggle is at `PreferencesWindow.xaml:142` and *Reroll
  all identities* at `:154`, both in Settings. The tray checkbox survives
  (`TrayService.cs`, `_streamerModeItem`). The main-window band and its duplicated
  helper line are gone; the only streamer string left on MainWindow is the
  per-row *Reroll identity* context item, which is a per-account action, not the
  band the finding was about.

**F-001 did not close with wave 3.** Its fix direction lists Stop all, Open log
folder and the Welcome tour as Tools items alongside History/Diagnostics/Plugins/
Library. The first three have no view-model command — the tray raises events
handled in `App.xaml.cs` — so they need new plumbing rather than a menu entry.
F-001 stays open with exactly those three outstanding.

**Residue carried out of wave 2, still open:** C5 is unmet *inside* a Settings
page. The nine section cards remain fill-only and collapse under flatline;
splitting them across five pages did not make cards load-bearing.

**Residue carried out of wave 3, still open:** item hover and pressed fills
inside the Tools popup come from WPF-UI's own dictionary, which `ThemeService`
never touches — so they ignore the user's theme. That is true of every
`ContextMenu` in the app, not just this one, which is why wave 3 themed only what
it introduced (the popup border and the separator) and left the rest.

## The register

| id | lens id | surface | lens | sev | vis | evidence | verdict | fix direction | status |
|---|---|---|---|---|---|---|---|---|---|
| F-001 | QF-19 | History/Diagnostics/Settings IA | qol | 5 | 5 | `AppSettings.cs:418-433` SettingsBlob has no history/diagnostics field; History writes only favorites+Clear; Diagnostics writes only a user-chosen zip | Tools, not preferences — verb-shaped, episodic, zero overlap with settings.json; tray precedent (leg 4) actually argues against a Settings/Tools split, not for it | Build Tools container on main window (pairs with QF-21): History, Diagnostics, Plugins, Library, Stop all, Open log folder, Welcome tour; Settings keeps only what writes settings.json/discord.dat [new-mechanism] | clean |
| F-002 | QF-20 | Preferences | qol | 5 | 5 | `PreferencesWindow.xaml:6-7,37-330` fixed 500x600 NoResize, 8 cards/~30 controls in one ScrollViewer; RowBgBrush vs BgBrush = 1.09:1 brand / 1.00:1 flatline **Reconciled 2026-08-10:** the 1.00:1 is `flatline-lab`'s, the adversarial fixture at `src/ROROROblox.Tests/FlatlineLabGateTests.cs` — not the `flatline` theme v1.17 ships, which measures **1.33:1**, better than brand's 1.09:1. Both numbers are real and they belong to different themes. | Split into a 5-page left nav rail — but "survives flatline" is a property of the selection indicator you build, not of rails per se; 5 pages over ~30 controls trades scroll for near-empty rooms | SettingsShell: ListBox rail + ContentControl, one UserControl/page, MinWidth ~760, resizable; shape-based selection indicator (3px bar + weight), not a fill; DiscordConfig single-owner refactor lands with/before it [new-mechanism] | clean |
| F-003 | QF-2 | Preferences | qol | 5 | 5 | `PreferencesWindow.xaml:6-7` Height=600 Width=500 NoResize; flatline scrollbar thumb covers ~1/5 of track | The tallest, densest surface in the app is the one window the user cannot resize | Split into named pages behind a nav rail and drop ResizeMode=NoResize; MinHeight/MinWidth as an interim one-line fix [new-mechanism] | clean |
| F-004 | CV-1 | cross-surface, all 25 windows | copy | 5 | 5 | 25 `Title=` + `FriendFollowWindow.xaml.cs:133` runtime title = 7 competing conventions; 18/25 already fit a destination/interruption split, 7 break | No stated title rule exists — most common invention (prefixing product name) is the "looks like an ad" defect, literally | One 3-line rule: title bar names destination only; heading matches title bar; destinations take a noun, interruptions state the problem. Apply to the 7 breaks; reserve two-tone wordmark for MainWindow + About [new-mechanism] | clean |
| F-005 | CV-8 | main toolbar / Preferences / tray | copy | 5 | 5 | `MainWindow.xaml:1020` "Settings" → `PreferencesWindow.xaml` Title="RoRoRo -- Preferences", header prints "Preferences / Settings" (:21,27); tray :227 "Preferences..." | Same destination called Settings, Preferences, both-at-once, and Preferences again — nobody chose | "Settings" everywhere; retire "Preferences" as a user-facing word; replace stale tooltip (:1028, omits Discord/alerts/idle/transport) | clean |
| F-006 | CV-7 | main toolbar → Games/Library | copy | 5 | 4 | `MainWindow.xaml:1056` Content="Games" → `SettingsWindow.xaml` Title="RoRoRo -- Library", header "Library" (:343); tooltip covers only half the window | One destination, three names; tooltip describes only games, not private servers — goal-3 verbatim | Pick "Games" everywhere visible (title, header, tooltip: "Saved games and private servers for the launch picker.") | clean |
| F-007 | CO-3 | 12 destination windows + 7 modals | consistency | 4 | 5 | Two-tone header copied 12x at 4 sizes/4 grammars — 22px "Page/descriptor" (6 of 12), 24px About, reversed no-separator Welcome, 20px/18px singles | Twelve near-copies with no agreement on size, grammar, or whether the product name belongs in the title — the mechanism behind goal 4 | One PageHeader control taking Page + Descriptor, separator/size/casing fixed inside; title bar carries product name once, not twice [new-mechanism] | clean |
| F-008 | QF-5 | main window, Row 3 band | qol | 4 | 5 | `MainWindow.xaml:1365` tooltip and `:1379` body text identical, word for word; toggle already has a tray home (`TrayService.cs:211`) | A setting already reachable from the tray occupies a full band of the main window and explains itself twice | Move toggle + Reroll all identities into the Settings shell; keep the tray checkbox; delete the duplicated helper line | clean |
| F-009 | QF-21 | main window, Row 2 toolbar | qol | 4 | 5 | `MainWindow.xaml:1019-1073` six identical buttons at MinWidth=860, band already edge-to-edge; Games opens a library at Settings' weight (C4 drift) | Collapse six buttons to Settings + Tools▾ — capacity/heterogeneity hold; "tray already proves this" does not, since the tray groups Settings inside Tools, not apart | Two buttons where six were; Tools▾ mirrors the tray's item list; rename Games→Library in the menu; keep Launch multiple/Squad Launch in place [new-mechanism] | clean |
| F-010 | CV-2 | main window | copy | 4 | 5 | `MainWindow.xaml:992` "Run multiple Roblox clients side by side. Add a saved account, click Launch As to open it." | Store-listing copy pasted into the app, read on every launch by a user with 8 accounts already on screen | Delete the line — WelcomeWindow's first-run guide and the empty-state text (:1597-1601) already carry it to the user who needs it | clean |
| F-011 | CV-3 | main window footer status bar | copy | 4 | 5 | `MainWindow.xaml:1809` "Multi-launcher for Windows. A 626 Labs product." — the other three status-bar cells report live state | Positioning copy in a status bar — the literal instance of goal 4's "ad" reading | Delete it; the line already lives in About (:73,77) where it's the actual subject | clean |
| F-012 | CV-11 | main window toolbar | copy | 3 | 5 | `MainWindow.xaml:1020` gear glyph (U+2699, text-presentation, survives invariant 5 on the letter) against 5 bare-text peer nav buttons | Not an emoji violation — a consistency violation; one of six identically-weighted buttons wears an icon for no distinction the other five lack | Content="Settings"; all six carry icons or none do — "none" is cheaper and more theme-durable | clean |
| F-013 | QF-1 | every secondary window | qol | 4 | 3 | `MainViewModel.cs:3188-3547` every opener is `window.ShowDialog()`; `PreferencesWindow.xaml.cs:43-55` documents DiscordConfig is safe only because the dialog is modal | Six modal islands off one toolbar — every cross-surface task costs a close-and-reopen round trip; going modeless has a hard prerequisite the finding correctly names | Fold six into one non-modal shell with a persistent left nav; DiscordConfig gets a single owner first [new-mechanism] | open |
| F-014 | CV-5 | Games/Library | copy | 4 | 3 | `SettingsWindow.xaml:347` — 55-word, four-sentence subtitle including a rhetorical question, above the content it describes | A help article wearing a subtitle's clothes — C7's subordinate-by-position rule violated | Delete the subtitle; move each sentence to the control it explains (:427, :443); drop the rename note (already the tooltip at :182) | clean |
| F-015 | CO-16 | Settings/Library, About, Preferences subtitles | consistency | 3 | 4 | Preferences/Diagnostics/History hold the one-line grey contract; `SettingsWindow.xaml:347` is a 340-char paragraph in the same slot; About stacks two subtitle roles; Welcome uses 12px/0,6,0,0 vs everyone's 11px/0,4,0,0 | Subtitle slot has no length/count contract — absorbed a help paragraph on one page, a tagline stack on another; the concrete shape of goal 4's "ad" reading | Give the shared PageHeader a single optional one-line descriptor with a hard character budget; relocate the Library paragraph into page body [new-mechanism] | clean |
| F-016 | CV-4 | Preferences | copy | 3 | 4 | `PreferencesWindow.xaml:31` "How RoRoRo behaves — startup, idle, Discord, alerts, and theme." — omits Recycle and Accounts transport (2 of 8 cards) | A table of contents for a page six inches below it, and one that rots every release | Delete the line; with CV-1 applied the header reads "Settings" and the first card starts | clean |
| F-017 | CV-6 | cross-surface, 11 page subtitles | copy | 3 | 4 | Diagnostics (:35), History (:34), Plugins (:77) plus Squad Launch/Join by link/Friends/Theme builder/Export/Import | Every window subtitles itself, so the device carries no information — states the test and applies it: delete 4, shorten 2, keep 6 | DELETE Accounts/Settings/Library/History subtitles; SHORTEN Diagnostics and Plugins to their behavior-changing clause only; KEEP the other 6 as-is | clean |
| F-018 | QF-6 | main window / tray | qol | 3 | 3 | `TrayService.cs:203,215` Multi-Instance state + Stop all are tray-only; grep of MainWindow.xaml/MainViewModel.cs returns only banner-copy references **Reconciled 2026-08-09:** the Stop-all half shipped with F-001 (`MainWindow.xaml` Tools group, bound to `StopAllCommand`). Multi-Instance state is still tray-only — the one `multi-instance` hit in `MainWindow.xaml:1471` is a comment on the contested-lock banner, i.e. still banner copy. | The product's core switch and most destructive action exist only in a right-click tray menu Windows hides by default | **CLOSED 2026-08-20.** The footer's empty star column — left that way by F-011, which removed the marketing line that used to sit in it — now carries `Multi-Instance on/off/error`, bound to the view model and following `ITrayService.StatusChanged`. THE SUBSCRIPTION IS THE FIX: six places call `UpdateStatus`, so notifying the window from each of them would be six chances to miss one, and the arm that would go stale is ERROR — the transition that arrives with nothing on screen having changed. The tray keeps the switch; the readout's tooltip names the tray in every arm, including ON, because a user who has never right-clicked the icon cannot discover the menu exists. Three states, two existing brushes, one weight: no new token for a user theme to supply, and no fifth status-colour site for `ThemedStatusColourTests`. Verified live — UIA reports `Multi-Instance on` with its help text, and the capture shows it in the footer. | clean |
| F-019 | AX-12 | Preferences (Settings) | a11y | 3 | 3 | `PreferencesWindow.xaml:37-330` one ScrollViewer, fixed NoResize; 19 sequential focus stops, no headings/groups; 3 section labels are plain Text; RowBgBrush vs BgBrush = 1.09:1 brand **Closed 2026-08-10 by v1.18 item 1**, verified against the tree. Every leg of this row is now answered by a different artifact and no single one of them did it alone. `NoResize` went with the nav rail (F-002/F-003): `PreferencesWindow.xaml:7-8` sets `Height 640 Width 860 MinHeight 480 MinWidth 760` and the file carries no `ResizeMode` attribute. The section labels are the part item 1 owns — **all 8 headings now carry `SectionHeadingStyle` plus `AutomationProperties.Name` and `AutomationProperties.HeadingLevel="Level2"`** at `:117, :197, :256, :289, :333, :515, :661, :695`, standing OUTSIDE the cards rather than hand-rolled inside them, and `HeadingLevel` is the property that actually lets a screen reader move group-to-group. Plain-Text section labels: **3 to 0.** The linear run is re-measured rather than assumed: 23 declared focusable controls before this cycle, more now, but collapsed pages are out of the tab order so the run a user faces is per-page — worst case is Alerts and memory at **14**, or 15 while the muted-accounts block is visible, down from the audited 19. Item 3's own commit records that growth past item 1's stated 12 rather than contorting around it. | Settings being "too tall" is a 19-stop linear tab run with no headings and no bypass — confirms "a group that is only a fill is not a group" | Whatever the visual answer, each group needs a real container with a real heading so keyboard/screen-reader users move group-to-group [new-mechanism, shared with QF-20] | clean |
| F-020 | QF-4 | Squad Launch modal / Preferences | qol | 2 | 2 | `SquadLaunchWindow.xaml.cs:59,73,79` read/write a global persisted preference reachable only from inside the modal it modifies **Closed 2026-08-10 by v1.18 item 4**, verified against the tree. `CarefulSquadLaunchToggle` sits at `PreferencesWindow.xaml:174-181` as the third ROW inside Startup's card, binding the same persisted `CarefulSquadLaunch` through the same `IAppSettings` accessors; the in-modal toggle stays, both surfaces re-read on open and neither caches. It is a row and not a card because item 1 had just collapsed this page's single-control cards, and a third card would have restored the shape that collapse removed. The rail has no launch page and none was added — Startup already holds a launch setting, so the fix landed on an existing page rather than a sixth one. **What this row proves about the cycle's own new fence, recorded because it is uncomfortable:** `SettingsReachabilityTests` could never have caught F-020. `CarefulSquadLaunch` was already fence-reachable from the modal, so the fence was green on the exact defect this row describes. See the note carried forward below. | The only way to change how squad launches behave is to begin one | Mirror the in-modal toggle into the Settings shell's launch page, binding the same IAppSettings value | clean |
| F-021 | CV-20 | Games/Library empty state | copy | 2 | 2 | `SettingsWindow.xaml:452` "Use the Squad Launch toolbar button to add one." — sends user to a closed window; the server actually saves itself automatically (`SquadLaunchWindow.xaml.cs:367`) **Reconciled 2026-08-09:** citation stale, defect intact — the copy now lives at `Games/GamesWindow.xaml:404` after the Settings→Games rename. **CLEAN v1.21.0.0, 2026-08-12 (item 10).** Verified against the tree before rewriting: there is no "add a private server" action anywhere in the app, so the old copy pointed at a window that is not open for a thing that window does not do. The ONLY site that persists one is `SquadLaunchWindow.xaml.cs:371`, which calls `IPrivateServerStore.AddAsync` as a side effect of launching a pasted link. The empty state now names that mechanism: *"Paste a private server link into Squad Launch and launch it. It saves here automatically."* | An empty state issuing an errand for a mechanism that's actually automatic — a goal-3 defect | "Private servers save themselves the first time you launch one from Squad Launch." | clean |
| F-022 | CV-21 | main window FPS-mismatch banner | copy | 2 | 2 | `MultiInstanceCopy.cs:23-26` — 45 words leading with diagnosis, actionable clause buried last; banner is conditional and dismissible **CLEAN v1.21.0.0, 2026-08-12 (item 10).** Re-read at HEAD first, as this cycle required, and the register's quote was indeed stale — the string had been rewritten and the figure moved from 15 to 20 seconds. The DEFECT survived the rewrite: the action was still the last of three sentences, behind two of mechanism, so the only sentence a reader can act on was the one they reached last. Action now leads, 47 words to 44, same facts and the same 20-second figure. | Correct, load-bearing content, but unpacks before the verdict | Invert — lead with "Set every account to the same FPS cap to launch at full speed," mechanism follows, same length | clean |
| F-023 | QF-3 | Preferences (absent), memory watchdog | qol | 3 | 1 | `AppSettings.cs:277-347` persists 4 watchdog settings; `IAppSettings.cs:104` "no dialog wiring exists yet"; zero .xaml hits **Closed 2026-08-10 by v1.18 item 3**, verified against the tree. All four are editable on the page already named for them — a Memory section at `PreferencesWindow.xaml:514-654` on "Alerts and memory", holding `MemoryWatchdogEnabledToggle :521`, `MemoryReserveMbInput :568`, `MemoryCapMbInput :591` and `ProjectionWarnMinutesInput :614`, each round-tripping through the existing `IAppSettings` accessors with no second source of truth. Free text rather than a preset picker, forced by the acceptance criterion: a picker cannot produce an out-of-range value, so "refused visibly" would be unfalsifiable by construction. Refusal is its own line, `MemorySettingsWarning :645`, in `RowExpiredAccentBrush` with the triangle and `LiveSetting Assertive` — a setter that clamps quietly is indistinguishable from one that worked. **Item 4a extended it at C1:** `AutomaticMemoryLine :554` says what a blank box resolves to on this machine, derived through the same `MemoryDefaults` calls `App.WireMemoryWatchdogAsync` runs, so the number on screen cannot disagree with the number the watchdog uses. **The durable artifact is not the controls.** `SettingsReachabilityTests` was written first and watched go red naming exactly these four and nothing else, then green — the fence is what stops this class regrowing, and it is why this row survived months. | RoRoRo routes the memory alert but gives no control over when it fires or what's too much — editor is a text editor | Give the memory watchdog a section in the new Settings shell next to the alert routing that already exists | clean |
| F-024 | QF-18 | Preferences → Alerts / account rows | qol | 2 | 1 | `MainWindow.xaml:282-291` mute persists per-account (`MainViewModel.cs:2883`); Alerts card (`:168-251`) never mentions muted accounts **Closed 2026-08-10 by v1.18 item 5**, verified against the tree. `MutedAccountsRow` at `PreferencesWindow.xaml:394-410` carries the count line and `UnmuteAllButton`, sitting directly under the two routing rows because it is the exception to them. **The count reads the ROWS, not the persisted id list**, and the asymmetry is deliberate and pinned by `MutedAccountsSummaryTests`: an id in `DiscordConfig.MutedAccountIds` outlives the removal of its account and nothing prunes it, so a file-derived count would report accounts the user does not have and cannot find. Unmute all still empties the whole list, orphans included, because "all" means all, and it writes through `SaveDiscordConfigAsync` rather than the per-row writer whose own doc names it as the other writer of this record. Zero muted collapses the whole block rather than rendering a stray `0`. Unmute all takes the ordinary secondary treatment on purpose — it is destructive-adjacent and not on item 10's enumerated list of three, and a fourth site was the signal to drop that work rather than widen it. **Citation fix carried in the same item:** items 3 and 4 had attributed careful mode to F-024 in two source comments. Careful mode is F-020. A wrong comment would have handed this row the wrong closure evidence today. | Per-account muting is right-click-only, and the Alerts page owning every other alert decision doesn't mention it | Add a muted-account count + unmute-all affordance to the Alerts section — data already in the VM | clean |
| F-025 | VC-1 | main-window | visual | 5 | 5 | `App.xaml:27,36` BgBrush==NavyBrush; 8 secondary controls rely on a DividerBrush border alone; flatline shows all 8 collapse to text while Launch multiple's cyan border survives | C8's secondary recipe is one-legged — fill contributes zero contrast even in the shipped theme; a single collapsible brush takes out 8 controls on the most-seen screen | Affordance from a property the theme can't flatten — accent-set outline or geometry; needs an app-wide control-style dictionary [new-mechanism] | clean |
| F-026 | VC-4 | preferences | visual | 4 | 5 | `PreferencesWindow.xaml` 8 sibling cards — 5 hold one setting, 3 (incl. Alerts, 13 controls) sit at identical weight **Reconciled 2026-08-09:** still open and deliberately so — `Controls/ControlStyles.xaml:119-120` states it was not smuggled into wave 6. Effort has dropped though: that wave made this **a one-place edit instead of a nine-place one**. Preferences currently holds 9 card borders under a 5-page nav rail, so the nav rail (F-002/F-003) supplied the level above the card; what remains is the card-vs-section weight distinction itself. **Closed 2026-08-10 by v1.18 item 1**, verified against the tree, and the answer was already in the dictionary. `SectionHeadingStyle` ships 13px SemiBold White at `Margin="0,18,0,6"` — which is exactly what this page had been hand-writing at three sites — so the 18-above/6-below rhythm IS the missing level, placed BETWEEN cards rather than inside one. No second container primitive was added; `SubCardBorderStyle`, `GroupBox` and `Expander` appear in `PreferencesWindow.xaml:98-100` only in the comment forbidding them, because a second container rank restates the two-meanings defect this row is about. **Counts re-measured at HEAD, per this file's own rule.** Item 1 took cards **9 to 7** and single-control cards **3 to 0**, control count conserved at 22; item 3 then added the Memory section, so the tree today holds **8 cards** at `:120, :200, :259, :292, :336, :518, :664, :700` and **8 headings**, with **zero** single-control cards. Net direction: cards shrank 9 to 8, single-control cards shrank 3 to 0. The weight collision the row is actually about is gone — a one-checkbox card and the nine-control Alerts card no longer sit as siblings at identical weight, because the level above them is now a heading and not another border. **And the framing this item was handed was wrong, corrected here:** the checklist said the hierarchy must survive colour removal, "check flatline, not just brand", implying flatline is the weak case. Measured, RowBg on Bg is 1.09 brand, 1.08 midnight, 1.08 magenta-heat, **1.33 flatline** — flatline has the STRONGEST card fill and all four are under 3:1. Grouping-by-fill was thin everywhere, not thin in one theme. Second time in two cycles that `flatline` was treated as adversarial when the adversarial numbers belong to `flatline-lab`. | One grouping primitive carries two incompatible meanings at identical weight — why goal 2's "split it up" can't be answered with more cards | Separate the two jobs structurally — a section needs a level above the card, a setting stays a row inside it [new-mechanism] | clean |
| F-027 | VC-11 | main-window | visual | 4 | 5 | Cyan/slash/white lockup reused 9x with 6 different second-token meanings — product/page, product/version, page/instruction, etc. **Closed 2026-08-10 by re-verification.** The lockup went 9 sites / 6 second-token meanings to **2 / 2**, shrank. `Controls/PageHeader.xaml` fixed size and grammar across 11 windows with the wordmark off by default (`:30` sets `Visibility=Collapsed`, revealed only by a `ShowWordmark=True` trigger). The two survivors, `MainWindow.xaml:1078` and `AboutWindow.xaml:60-71`, are exactly the two **F-004** (`clean`) reserves by name, and `PageHeader.xaml:17-20` states that rule in a comment citing F-004. Fixed by F-007's artifact; nobody flipped the row. Second instance of the F-001 pattern. | The lockup is the brand wordmark; reusing it as page chrome forces a second token with no consistent job | Retire the lockup from page chrome (About, splash, tray only); page chrome names the destination once, at one size [new-mechanism] | clean |
| F-028 | VC-13 | library | visual | 4 | 5 | `SettingsWindow.xaml:347` ~310-char/4-sentence subtitle vs 62 chars in Preferences; Title="RoRoRo -- Library" on class SettingsWindow in folder Settings/, opened via button "Games" | The one-line subtitle convention becomes a manual on the one window whose name/class/folder/button already disagree four ways | Move the rules to the controls they govern; settle the name (button/title/class/folder) in the same pass | clean |
| F-029 | CO-4 | Plugins/Preferences/Library/JoinByLink/SquadLaunch/ThemeBuilder/Import/Rename/CaptionColorPicker vs MainWindow | consistency | 4 | 5 | 2 ui:TextBox (MainWindow only) vs 11 plain TextBox across 10 windows, disagreeing on fill; flatline shows the install-URL field vanish while the main-window filter survives | Two input systems; the one used on 11 of 12 surfaces disappears under a theme the app ships support for | One AppTextBox style — standardize on ui:TextBox (invariant 4 already assigns chrome to WPF-UI) or give the shared style a shape cue independent of a collapsing fill/border | clean |
| F-030 | CO-6 | cross-surface, every secondary button | consistency | 4 | 5 | C8's secondary recipe is Navy==Bg in all three built-in themes; nav band, Remove, Reroll all, and 8+ other buttons rely on it; Launch multiple's cyan border is the sole survivor | Four secondary-button recipes exist; the most-used one binds its only shape cue to the divider brush | One SecondaryButton style whose outline reads at any brush setting; new token optional-with-fallback per invariant 6 [new-mechanism] | clean |
| F-031 | AX-5 | nav band + per-row Friends/Remove/Reroll all/Open themes folder/follow chips | a11y | 4 | 5 | DividerBrush vs NavyBrush = 1.26:1 brand / 1.00:1 flatline (fails WCAG 1.4.11's 3:1 in both); CyanBrush vs NavyBrush (Launch multiple) = 9.39:1/4.34:1, passes both **Reconciled 2026-08-10:** both flatline columns belong to `flatline-lab`, the adversarial fixture at `src/ROROROblox.Tests/FlatlineLabGateTests.cs`, which reproduces the 1.00:1 and the 4.34:1 on every test run. The `flatline` theme v1.17 ships is a different theme and measures **1.51:1** and **12.84:1** on the same two pairs — the divider still under 3:1 by design, so `EdgeRemediation.Decide` derives `InteractiveEdgeBrush` at 3.03:1 exactly as it does for brand. Both stated; conflating them is the defect being fixed. | The border was never a legible boundary even in the shipped brand theme — fix is binding the boundary to a contrast-guaranteed token, not "add a border" | Interactive boundaries bind a token guaranteed ≥3:1; add a load-time validation step that warns on a theme falling under 3:1 [new-mechanism] | clean |
| F-032 | AX-7 | nav band + Friends/Remove/Reroll all/star/Open themes folder | a11y | 4 | 5 | 11 controls bind MutedTextBrush as Foreground (incl. Remove at `:915`) — same token as helper prose; MutedTextBrush vs WhiteBrush = 2.42x brand but 1.00:1 flatline **Reconciled 2026-08-09: worse, not better.** `MainWindow.xaml` alone now binds `MutedTextBrush` as `Foreground` on **15** controls, up from the 11 measured at audit. Waves 5-6 added contrast machinery (`ContrastGuard`, `InteractiveEdgeBrush`) without stopping this leak spreading. **Reconciled 2026-08-10:** the 1.00:1 is `flatline-lab`'s, reproduced by `src/ROROROblox.Tests/FlatlineLabGateTests.cs`. Shipped `flatline` measures **2.65:1** muted-vs-white, better separation than brand's 2.42:1, so the token carries "secondary" on ratio there without a weight change. Note what neither number is watched by: PR #100's own fix removed the last declared pair whose foreground was the prose token, so the contrast gate cannot see `MutedTextBrush` at all any more — F-086. | The app expresses "control" and "explanatory prose" with the same color token — C7's collapse and C6's affordance loss share one root cause | Bind control labels to the primary text token; carry "secondary" in weight + the AX-5 boundary; extend Preferences' indent device to the main window | clean |
| F-033 | QF-7 | main window, Compact mode | qol | 4 | 4 | `MainViewModel.cs:663` plain SetField property; nothing writes it to disk; SettingsBlob has no compact field **Closed 2026-08-10 by v1.18 item 6**, verified against the tree. `CompactMode` is on the persisted record at `Core/AppSettings.cs:508` with a `false` default, accessors at `:394` and `:401`, interface at `Core/IAppSettings.cs:151-152`, and `MainWindow.xaml.cs:105` calls `RestoreCompactModeAsync` on load. No migration, asserted rather than assumed — an existing `settings.json` with no such key loads and defaults off. **Restore deliberately does NOT route through the public setter**, which is why `ApplyCompactMode` is split out at `MainViewModel.cs:725`: a restore through the setter would write back the value it just read on every launch, and on a read-only `settings.json` would log a failure the user never caused. A test asserts zero writes on restore. Persist is the fire-and-forget-into-try/catch shape the neighbouring `PersistIsSelectedAsync` and `PersistTagsAsync` already use, so nothing awaits the disk in front of a click; a failed persist keeps the toggle for the session and loses only the memory across a restart, which is the failure the disk actually had. | Compact mode — pitched by the Welcome tour as a second-monitor workflow — is forgotten on every restart | Add CompactMode bool to SettingsBlob (no migration needed) and restore it in MainWindow.OnLoaded | clean |
| F-034 | CV-9 | tray menu | copy | 4 | 4 | `TrayService.cs:221` "Open ROROROblox"; also tooltip :98-100 "ROROROblox — Multi-Instance ON/OFF/ERROR"; `DiagnosticsWindow.xaml.cs:220` "ROROROblox support snapshot" **Reconciled 2026-08-09:** 1 of the 4 leak sites is fixed — `FriendFollowWindow.xaml.cs` no longer builds a repo-name title. Three remain: `TrayService.cs:98-101` (tooltip, all three states), `:221` (“Open ROROROblox”), `DiagnosticsWindow.xaml.cs:222`. | The repo name shipped into the menu and tooltip a tray-resident app shows most often, violating the RoRoRo-brand memory rule | **CLOSED 2026-08-20. RE-MEASURED: the row said three sites remained; there were four.** The fourth is `IdleAlertPresenter.cs:17`, `ShowToast("ROROROblox", …)` — three files from a memory balloon already titled "RoRoRo — memory warning", so the app announced itself under two names in the same notification tray. All four now read from `Branding.ProductName`, and the tray's tooltip and menu header route through `Core/MultiInstanceStatusLine` so they cannot drift apart again (they had: the tooltip carried the repo name in all three states while the menu two pixels away did not). FENCED by `BrandNameFenceTests` in two passes, because one is not enough: a SHAPE rule rejects repo-name literals that read as prose, and a SINK rule rejects any literal reaching `ShowToast`/`Header`/`Content`/`Text`/`Title` — needed because `ShowToast("ROROROblox", …)` is character-identical to the eleven legitimate uses of the same word as a folder name and only the destination separates them. Both halves proved by putting each defect back and watching the fence name it. | clean |
| F-035 | CO-1 | cross-surface (root cause) | consistency | 5 | 3 | `App.xaml` has 10 brushes, 9 converters, 1 keyed control style total; 6 nav buttons and 8 Preferences cards repeat the same attribute sets verbatim | C8 describes a component vocabulary that exists only as habit — every window is free to reinvent it, and most have | Promote the vocabulary into a real merged ResourceDictionary — CardBorder, PageHeader, SectionHeading, PrimaryButton, SecondaryButton, DialogFooter, AppTextBox [new-mechanism] | clean |
| F-036 | VC-12 | main-window | visual | 3 | 5 | `MainWindow.xaml:992-996` header subtitle pitches the product, `:1808-1814` permanent status-bar tagline — both visible simultaneously **Reconciled 2026-08-09:** clean. Both lines are gone — `MainWindow.xaml:988-1000` is now a comment documenting the removal and why the empty state does not repeat the pitch. Closed sideways by wave 3 (F-010, F-011); caught by the unmerged PR #93, not by this pass's own filters. | The main window explains what the product is twice, in permanent chrome, to a user already looking at their own account list | Pitch belongs on About and the Store listing; recover the header row and footer slot for content that changes | clean |
| F-037 | VC-15 | preferences | visual | 3 | 5 | Accent fill on Send test, "+ Build a theme...", and Close (loudest control on the page) in Preferences; same cyan Close is loudest on About/History/Plugins; Diagnostics reverses it **Closed 2026-08-10 by v1.18 item 9**, verified against the tree: **all nine Close buttons now carry `SecondaryStrongButtonStyle` and zero accent-filled Close buttons remain** — `AboutWindow.xaml:138`, `DiagnosticsWindow.xaml:60`, `FriendFollowWindow.xaml:72`, `GamesWindow.xaml:413`, `SessionHistoryWindow.xaml:62`, `PluginsWindow.xaml:376`, `PreferencesWindow.xaml:806`, `SquadLaunchWindow.xaml:96`, `CaptionColorPickerWindow.xaml:84`. Diagnostics was the precedent and did not change. `IsDefault` and `IsCancel` kept as authored on all nine: which button Enter fires is a keyboard affordance, not a rank. Verified WPF-UI's own Button template has no default-button accent trigger to fight the sweep — zero occurrences of `IsDefaulted` in `Wpf.Ui.dll`. **Re-measured before the sweep and the audit figure had moved:** 5 accent-filled / 4 secondary, not 4/1 — `CaptionColorPickerWindow` had joined the loud side while FriendFollow, Games and SquadLaunch joined Diagnostics on the quiet one. So this was a decide-and-sweep across an almost even split, not a fix-one-window. **Residue named rather than swept:** `SendTestButton :472` and `BuildThemeButton :722` keep their magenta fill. Both are actions their page exists to perform, which is what the fix direction reserves fill for, but both are hand-copied inline recipes rather than a rank — that half belongs to F-068. | Accent fill marks four different things — on 4 of 5 windows the loudest control on screen does nothing | Reserve filled treatment for the action the window exists to perform; dismissal takes the secondary treatment everywhere, as Diagnostics already does | clean |
| F-038 | QF-11 | History | qol | 4 | 3 | `SessionHistoryWindow.xaml.cs:65-72` swallows read/Clear failures to the same empty-state copy | A history file that can't be read presents as "you have never launched anything"; a failed Clear looks identical to a successful one | **CLOSED 2026-08-20.** `Core/SessionHistoryStatus` models the three outcomes the window collapsed into one: Loaded, Empty, Unreadable. The read failure now keeps the exception message — "in use by another process" is the difference between a shrug and a fix — and the placeholder says the file could not be read instead of telling a user with a full history to go and make some. Clear grew the same treatment: a failure says so and says nothing was deleted, and the success line is suppressed when the reload behind it failed, so the window never gives two answers to one question. A status line under the heading follows the Diagnostics model this row cited, with `LiveSetting="Polite"` so the failure is announced rather than silently swapped in. Verified live: the capture reads `100 launches recorded.` | clean |
| F-039 | QF-13 | Welcome tour / About | qol | 4 | 3 | `WelcomeWindow.xaml.cs:26-38` one-shot sentinel; only documentation of 6 unlabelled row affordances; sentinel marked before the account-count check, so upgrading users burn it silently **Reconciled 2026-08-09:** half shipped with F-001 — the Tools-group entry exists and the sentinel bug named in this row is fixed (`MarkShown()` now sits inside the account-count branch). The **About** entry this row also asks for is still outstanding, which is the only reason it stays open. | The only explanation of RoRoRo's least self-evident controls is shown once, before the user has accounts, then unreachable forever | **CLOSED 2026-08-20.** About's Links list gains "Show the welcome tour" — the half wave 3 left, since the Tools entry and the sentinel bug both shipped with F-001. Owned by About rather than by the main window: About is itself modal, so a tour parented past it would open behind the window that asked for it, which meant `ShowTour` taking an optional owner. Verified live by UIA (the Hyperlink resolves by accessible name) and by capture. **THE CAPTURE ALSO CAUGHT WHAT THE CODE DID NOT**: About is `ResizeMode="NoResize"` at a fixed height, and one more line pushed the DPAPI paragraph off the bottom — and revealed the trademark disclaimer had ALREADY been clipped mid-sentence before this wave touched it, on a Store-listed product. Height 460 → 540; both paragraphs now render whole. Recorded as F-113 rather than left inside this row. | clean |
| F-040 | CV-10 | row context menu / Theme builder | copy | 4 | 3 | `MainWindow.xaml:302` "🎲 Reroll identity"; `ThemeBuilderWindow.xaml:52` "&#x1F4CB; Copy AI prompt" — the only two emoji-presentation codepoints in the app | Two decorative full-color emoji in monochrome chrome — a live invariant-5 breach the brief wrongly assumed was already enforced | Delete both codepoints, no replacement | closed |
| F-041 | CO-5 | all nine dialogs with a Close button | consistency | 3 | 4 | Nine Close buttons across five paddings — accent-filled/IsDefault (22,8/20,8/14,6) vs bordered secondary/no-default (16,8/14,8) | The most-repeated element in the app has no agreed weight, size, or default-button behavior | One DialogFooter composite owning Close's rank, padding, IsDefault/IsCancel, right-alignment | **CLOSED 2026-08-20.** RE-MEASURED: the rank was already settled — wave 5 made all nine `SecondaryStrongButtonStyle`, so "no agreed weight" is stale. What remained was padding in FIVE values (14,6 / 14,8 / 16,8 / 20,8 / 22,8) and an `IsDefault` split of four-to-four that **looked principled and was not**: the four whose Close is not the default had no default button at all, so Enter did nothing in Diagnostics, Friends, Games and Squad Launch. One `DialogCloseButtonStyle` now owns padding, alignment and both Enter and Esc — safe everywhere because dismissing is the one action that destroys nothing. Fenced by `DialogCloseButtonFenceTests`, which refuses a Close that skips the style OR restates what it owns, since a local override is how five paddings happened. | clean |
| F-042 | CO-8 | MainWindow / Preferences | consistency | 3 | 4 | `MainWindow.xaml:1360` Streamer-mode is the only ui:ToggleSwitch; all 6 Preferences booleans are CheckBox **Reconciled 2026-08-09:** count drifted — Preferences now holds **7** `CheckBox` controls, up from the 6 measured at audit. **Closed 2026-08-10 by re-verification.** Zero `ToggleSwitch` elements exist app-wide. The streamer toggle was converted to a `CheckBox` by **F-008**, and `PreferencesWindow.xaml:125-130` says so in a comment. The 2026-08-09 reconciliation counted the seven CheckBoxes and missed that the seventh **is** this defect, resolved. | Two mutually exclusive boolean representations; the setting goal 1 wants relocated is the sole user of the minority one | Pick one boolean control before the move — convert Preferences to ToggleSwitch, or Streamer mode to CheckBox on the way in | clean |
| F-043 | CV-13 | Preferences | copy | 3 | 4 | Six checkbox labels across three grammatical persons — second-person imperative (:46,83,102), first person "my"/"I'm" (:63,139,155) **Closed 2026-08-10 by v1.18 item 8**, verified against the tree. **Both of this row's numbers had moved and both are corrected here.** "Six settings" was right at audit and is **nine** today — F-008's streamer relocation plus items 3 and 4 added three — and the first-person count was **three**, not one, which is what this row's own citations and its fix direction always said. All three are fixed, in the exact words the fix direction wrote by example: `PreferencesWindow.xaml:147` "Launch your main account when RoRoRo starts.", `:669` "Show what you're playing on Discord.", `:685` "Let friends join your server from Discord." — and that last one also gained the terminal period it was missing, which this row never counted because the label was not among its six. The nine labels today are at `:128, :147, :180, :214, :240, :299, :527, :669, :685`; zero first-person pronouns, all terminal periods. `PreferencesCopyTests` pins four rules over labels and hints with a partition assertion so nothing falls through a gap. **Its scope boundary is deliberate and load-bearing:** button content is NOT covered, because a button can legitimately speak in the user's own first person — pressing it IS the user saying the thing, so "I don't have a Discord server" `:475` is correct copy and a blanket ban would need an exemption for the one string that is right. Which leaves "My channel" as the one first-person string still on the page, deliberately unreached: it is a routing destination name that persists as a `Tag` and that `AlertStatusLine` composes around, so renaming it is a vocabulary decision rather than a copy pass. | Six settings, three voices, one page — first person is a different speaker from the rest of the app's "you" | All second person, all terminal periods — "Launch your main account...", "Show what you're playing...", "Let friends join your server..." | clean |
| F-044 | AX-8 | cross-surface | a11y | 3 | 4 | 11px is the default body size app-wide; drops to 10px (helper/chip text), 9px (MAIN pill), 8px (popup badge) | Body content sits under the 12px floor and drops to 8-10px in six places, stacking with the contrast failures | **CLOSED 2026-08-21 on the half it can measure; the other half is F-118.** The row says body text "drops to 8-10px in **six places**" — **RE-MEASURED: 38 declarations across 12 files**, including two 8px pills. All 38 now bind `MetaFontSize` (11px), and `TypeLadderFenceTests.NothingIsSmallerThanTheMetaStep` means there is nothing below it, ever. Verified by capture: the MAIN pill is legible at a glance where at 8px it was a smudge. **The second claim — "body content sits under the 12px floor" — is NOT fixed and does not close.** 163 declarations sit at 11px, which is 50% of the app's text, and most of them are prose that belongs at Body. Sorting prose from meta is per-site judgement across 22 files, so it is F-118 rather than a number this row can move. Split on the F-054 → F-112 precedent: closing a row whose measurable half shipped, and giving the unmeasured half its own id, beats a row that reads half-true in both directions. **A cost worth recording:** raising the 9px follow chips pushed the main window's chip strip from just-fitting to clipping — see F-119. | clean |
| F-045 | AX-11 | main window + row context menu | a11y | 3 | 4 | `MainWindow.xaml:1020` "⚙ Settings" announces as "gear Settings"; star-only button has no name beyond the glyph; `:302` literal die emoji in menu copy | Five glyphs carry meaning only sighted users receive; the star toggle has no accessible name beyond a Unicode shape | Mark glyphs as decoration, carry meaning in a text name; promote the existing tooltip text (:687-691) to the star's accessible name | **CLOSED 2026-08-20 — mostly already gone, and nobody had flipped it.** The headline case, `MainWindow.xaml:1020` "gear Settings", **does not exist**: waves 1-3 replaced those nav buttons. The star toggle it names is not in our XAML at all — it comes from WPF-UI's icon font, so it is not ours to name here, recorded rather than silently dropped. What remained was the die, and the fix respects the campaign's standing ruling that the die and clipboard are thematic rather than decoration: the glyph STAYS and the menu item carries `AutomationProperties.Name="Reroll identity"`, so it is seen and not spoken. Rulings override rows. | clean |
| F-046 | CO-7 | MainWindow/Games/Plugins/Preferences | consistency | 3 | 3 | "Remove" is quiet bordered secondary on MainWindow/Settings but filled-accent SemiBold on Plugins; accent fill also encodes test/launch/creation elsewhere **PARTLY SHIPPED by v1.18 item 10, and this row does NOT close.** The v1.18 cycle scoped it to close and the tree does not support that, so it stays open with the remainder named — same handling F-001 got when wave 3 left three items outstanding. **Shipped:** `DestructiveButtonStyle` now exists in `Controls/ControlStyles.xaml:106-112`, the rank this row said was missing, and it is assigned by enumeration to exactly three sites — Remove on the account row `MainWindow.xaml:1016`, Clear in History `SessionHistoryWindow.xaml:60`, Stop all `StopAllConfirmWindow.xaml:61`. **The rank carries no hue, and the reasoning is measured rather than aesthetic:** `MagentaBrush` fails in two of four shipped themes (magenta-heat sets Cyan and Magenta to the same value, so a magenta destructive button and a cyan primary render identically; under flatline Magenta is the dark achromatic accent at 3.73:1 against a derived edge already at 3.03:1, and a dim grey outline reads as disabled rather than dangerous), and `RowExpiredAccentBrush` fails the mirror image. So the rank is two non-hue relationships — a doubled edge on `InteractiveEdgeBrush`, the one boundary token `ContrastGuard` derives, measured 3.0699 / 3.0194 / 3.0364 / 3.0258 across the four built-ins; and Bold, one weight step above SecondaryStrong's SemiBold, itself one above secondary's Normal. **Outstanding, measured against the tree 2026-08-10 — this row's own headline comparison still renders.** `Plugins/PluginsWindow.xaml:258-268` Remove is still a hand-rolled `MagentaBrush` fill with `WhiteBrush` and SemiBold, still the loudest control on its screen; the 2026-08-10 re-verification defined the "assigned by consequence" clause by exactly that site. `Games/GamesWindow.xaml:175` and `:287` Remove are quiet `SecondaryButtonStyle`, and `SquadLaunch/SquadLaunchWindow.xaml.cs:224-235` builds a fifth Remove in C# where a style change cannot reach it. **Direction: "Remove" treatments across four surfaces grew 2 to 3** — Destructive on MainWindow, Secondary on Games, hand-rolled accent on Plugins. The cycle held its line on purpose rather than dragging F-068's sites forward, which was the right call; it just is not a closed row. What is left is the assignment pass across the other three surfaces. **CLOSED v1.20.0.0, 2026-08-11.** `PluginsWindow`'s Remove — the hand-rolled magenta fill that was this row's headline evidence — takes `DestructiveButtonStyle`. The by-name destructive list is unchanged at three (Remove on the account row, Clear history, Stop all confirm); no fourth site was added by judgement, which `spec.md > §4` made the stop condition. Fill weight now tracks the rank, and the rank is assigned by consequence rather than by window. | Fill weight tracks neither consequence nor frequency — no "filled means important" lesson carries across screens | Define two ranks + one destructive variant in shared button styles; assign by consequence, not by window | clean |
| F-047 | CO-9 | Preferences/History/Diagnostics/Plugins | consistency | 3 | 3 | Three grouping systems — filled card, outlined unfilled container (History), no primitive at all (Diagnostics code-behind, 12px CyanBrush vs C8's 13px SemiBold White) | C5's "filled card is the only grouping primitive" isn't true in code — one variant hides in C# with its own spec | One Card style + one SectionHeading style in the shared dictionary; migrate Diagnostics code-behind to consume them | clean |
| F-048 | CO-10 | all 25 windows | consistency | 3 | 3 | MainWindow is the only ui:FluentWindow; ResizeMode spread is arbitrary — 7 windows no-ResizeMode, 12 NoResize (incl. all 8 modals), 2 CanResize; Preferences fixed while sibling Library resizes | Whether a window can resize isn't derived from window kind, and Preferences — goal 2's subject — is the one locked | Two chrome classes: destinations (resizable, MinHeight/MinWidth, maximize) and interruptions (fixed, no maximize); assign every window to one | **CLOSED 2026-08-20 — and this row was wrong about its own headline example.** It says Preferences is "the one locked"; Preferences is NOT locked, wave 2 made it resizable and nobody flipped the row. Re-measured, the gap was THREE windows of twenty-six, not twenty-five: `WelcomeWindow` and `CookieCaptureWindow` resizable with no minimums (draggable to a sliver), and `ImportAccountsWindow` declaring MinWidth/MinHeight under `ResizeMode="NoResize"` — minimums it could never reach — while its `ExportAccountsWindow` sibling resized. The two classes the row asked for are now a fence, not a paragraph: `WindowChromeFenceTests` classifies every window destination or interruption and enforces the chrome each implies, so window twenty-seven has to choose. | clean |
| F-049 | CO-11 | Preferences | consistency | 3 | 3 | Two ComboBoxes explicitly themed (:114-123,263-270); two newer alert-routing dropdowns (:183-189,199-205) fall through to WPF-UI defaults, diverging under the default theme | The alerts block was added without picking up the theming recipe used twenty lines above it | Fold into the shared dictionary as an implicit ComboBox style so new controls inherit it by default | clean |
| F-050 | AX-6 | every primary CTA | a11y | 3 | 3 | Squad Launch/Send test/MAIN pill (White on Magenta) = 3.79:1 brand/2.99:1 flatline; best theme-derived foreground (navy-on-magenta) only reaches 4.40:1 brand, still under 4.5:1 **Gated 2026-08-09:** `ContrastPairGateTests` now measures this pair under every built-in theme on every test run — brand, midnight, magenta-heat at the time of writing, and flatline since v1.17, which enrols itself because that test iterates whatever `ThemeStore` returns with `IsBuiltIn`. It is the one exemption in that gate, citing this row; `NoExemptionOutlivesItsFinding` deletes the exemption automatically when this row stops being `open`. **Reconciled 2026-08-10:** the 2.99:1 is `flatline-lab`'s, reproduced by `src/ROROROblox.Tests/FlatlineLabGateTests.cs` and asserted there below the exemption's own 3.20 floor. Shipped `flatline` measures **4.68:1** on white-on-magenta, above AA outright, so the exemption is not load-bearing for that theme. **This row does not close.** v1.17 shipped a theme that does not need the exemption; it did not implement the fix direction below, which is the thing that would close it. The two are different claims and only the second one closes a row. Flipping this to anything but `open` deletes the exemption on the next test run and turns brand (3.79:1), midnight (4.16:1) and magenta-heat (3.29:1) red — **all three** pre-flatline built-ins. **Corrected 2026-08-10 by re-verification:** this row, `spec.md` §10.1 and `checklist.md` all said two and omitted midnight. `AaThreshold` is 4.5 (`ContrastPairGateTests.cs:62`) and `spec.md` §4.3 records midnight at 4.16:1 on this exact pair. Only flatline (4.68:1) survives without the exemption. The guard was understating itself by a third, which is the direction that matters most for whoever later decides this row looks cheap to close. **RE-MEASURED 2026-08-11, and it does NOT close — the premise changed, not the defect.** v1.20 moved every magenta-filled BUTTON onto `AccentActionButtonStyle`, which puts magenta on the edge and the label on Navy, so the `(MagentaBrush, WhiteBrush)` pair `ContrastPairGateTests` exempts now matches nothing and the exemption reads as resolved. It is not. White-on-magenta is still shipped on **five badges** — `MainWindow.xaml:57` and `:416` MAIN, `:1403` DEFAULT, `GamesWindow.xaml:238` PRIVATE, `PluginsWindow.xaml:183` update — each a magenta `Border` wrapping a `WhiteBrush` `TextBlock`. The gate measures only elements declaring BOTH halves on one tag, so a parent/child pair is structurally outside its scope. Ratios re-measured this date: **3.79 brand, 4.16 midnight, 3.29 magenta-heat, 4.68 flatline** (direction: unchanged from 2026-08-09; count of gate-visible sites 8 → 0, count of shipped sites 8 → 5). Closing this row would auto-delete the exemption via `NoExemptionOutlivesItsFinding` while the defect it names is on screen in four windows. **Stays open.** The row's fix direction is untouched and still right: resolve the foreground at brush-application time, plus a validation step, because the best theme-derived alternative (navy) only reaches 4.40 brand / 3.79 midnight / 4.90 magenta-heat / 3.73 flatline and clears AA in one theme of four. | Every filled CTA fails 4.5:1 in at least one shipped theme because foreground is chosen at author time, fill at theme time | **CLOSED 2026-08-20. RE-MEASURED, and the row's own surface was stale.** It says "every primary CTA" and cites Squad Launch and Send test — but `CtaButtonStyle` is Cyan-on-Navy and has been for some time. The magenta+white pair survives on FIVE PILLS: MAIN at 8px and 9px, PRIVATE, DEFAULT, and the plugin update badge. Smaller stakes than the row implies, and still 8-10px bold text, so AA's 4.5:1 applies with no large-text allowance. **THE STATED FIX DIRECTION DOES NOT CLOSE THIS ROW, by the row's own numbers.** "Pick the higher-ratio of the two required slots" leaves brand at 4.40:1 and midnight at 4.16:1 — two themes of four still short, which the row records and then proposes anyway. Picking is most of the fix; the rest is a nudge. `ContrastGuard.BestForeground` picks per theme, then walks the WINNER further in its own direction — brand navy gets darker, midnight white gets brighter — landing at 4.53 / 4.52 / 4.90 / 4.68. Two themes are untouched because they were already compliant. Derived into `OnMagentaBrush`, not authored, so no user theme gains a field (invariant 6). **The exemption array in `ContrastPairGateTests` is now EMPTY for the first time since it was built.** Gap stated, not solved: the plugin contract's `ThemePalette` carries eleven fields and this is a twelfth resolved brush — additive and backwards-compatible, but a versioned contract change is its own decision. | clean |
| F-051 | QF-9 | Preferences → Theme | qol | 4 | 2 | `ThemeService.cs:82-91` swallows a persist failure to a log warning, applies live anyway; `PreferencesWindow.xaml.cs:198-206` bare catch{} **Closed 2026-08-10 by v1.18 item 7**, verified against the tree. `ThemeService.SetActiveAsync` now returns a `ThemeChange` (`ThemeService.cs:99`, record at `:366-375`) telling the caller which of three things happened, and the Theme section carries `ThemeStatusLine` at `PreferencesWindow.xaml:786`, composed by `ThemeStatusSummary.ForThemeChange` at `PreferencesWindow.xaml.cs:323`. **Three states, not two.** The third — the theme is gone from the folder entirely — never claims it is on, which a two-state version would have; and a built-in id collision gets its OWN sentence rather than being reported as unreadable, because telling an author their file has a syntax error when the real problem is its name sends them hunting something that is not there. **One deliberate divergence from the pattern this row points at:** the line takes `RowExpiredAccentBrush` and the triangle, not `CyanBrush`. Cyan is the accent, the same treatment a success gets, so a failure painted cyan would look like a save; `RowExpiredAccentBrush` plus the triangle is the warning vocabulary v1.17 established across expired rows, idle chips, memory chips and the compat banner. Success stays silent — but falls back to the folder report rather than blanking, because a bad file is still a bad file after a save that worked. `AlertsStatusLine` itself is untouched and is now its own row, F-094. | If the settings write fails, the user watches the theme apply, sees no message, finds the old theme back after restart | On persist failure, apply live and say so in the Theme section's status line, mirroring the AlertsStatusLine pattern already on the page | clean |
| F-052 | AX-1 | all 26 XAML files | a11y | 4 | 2 | grep for AutomationProperties\|FocusVisualStyle\|TabIndex\|KeyboardNavigation\|IsTabStop across src/ returns zero; 70 unnamed Buttons, 16 unnamed ComboBoxes in the main-window tree | No accessible-naming layer exists in the app's own composition (focus visuals come from WPF-UI per invariant 4, so that limb doesn't independently stand) | A naming rule — no Button/ToggleButton/ComboBox/TextBox ships without AutomationProperties.Name when Content isn't literal text [new-mechanism] | **RE-MEASURED 2026-08-20 and FENCED; remediation still open.** The row is stale in both directions: it says the grep "returns zero" and counts 86 unnamed. Today **111** controls carry a name or literal text and **49** do not — earlier waves named a great many and recorded none of it. `AccessibleNamingFenceTests` now holds the 49 as a CEILING that may fall and may never rise, with a second clause refusing slack so naming work lowers the constant in the same commit. A ceiling rather than a gate because a rule with 49 violations is either a 49-control rewrite or an exemption list nobody reads. The `[new-mechanism]` remediation itself is unstarted. | open |
| F-053 | QF-10 | Preferences → Theme → "Open themes folder" | qol | 3 | 2 | `PreferencesWindow.xaml:293` tooltip claims a dropped theme needs a restart; `ThemeStore.cs:42-72` re-enumerates on every ListAsync(), no restart needed; malformed themes drop silently **Closed 2026-08-10 by v1.18 item 7**, verified against the tree — and **this row's own fix direction was false and is corrected here.** The tooltip at `PreferencesWindow.xaml:740` now reads "Drop a *.json file in here, then close and reopen Settings to see it in the picker." Not "reopen this page", which the fix direction and this cycle's PRD and checklist all prescribed and which is equally wrong: the rail's five pages are one window with toggled `Visibility`, so switching pages re-lists nothing. `ThemeStore.ListAsync` re-enumerates per call and `BuildPreferencesWindow` constructs a fresh window per open, pinned by a test that drops a file between two calls. The window is the refresh unit; the page is not. Unreadable files are reported inline through `ThemeStatusSummary.ForFolder` (`PreferencesWindow.xaml.cs:350`) into the same `ThemeStatusLine` F-051 built, and a folder with one bad file among good ones still loads the good ones. **A second instance of this row's own defect, found and fixed the same session and worth recording here because documentation caused it:** `RawTheme`'s doc comment said the on-disk format was camelCase when `JsonSerializerOptions` sets `SnakeCaseLower`. A camelCase key deserializes to null, `ToTheme` throws on the null check, `TryLoadFileAsync` catches and drops the file — so a theme written to that comment's own instructions vanished with no error, which is verbatim this row's complaint. Caught at the C2 walk when a hand-written test theme was rejected; the rejection was correct and item 7's new status line named the file and said it could not be read, which is exactly the behaviour that item shipped. The feature worked, the fixture was wrong, and the comment that made it wrong is now right. No separate row: it is one comment, already fixed, and it is this row's evidence rather than a new finding. | Copy sends users through a restart the code doesn't require; a bad theme file vanishes with no signal | Correct the tooltip to "reopen this page"; report unreadable files inline | clean |
| F-054 | QF-12 | every window | qol | 3 | 2 | grep for KeyBinding\|InputBindings\|Gesture across all XAML/CS returns zero; `CookieCaptureWindow.xaml` has no Button element at all, so Esc doesn't dismiss it | RoRoRo has no keyboard shortcuts, and the modal a first-run user is guaranteed to hit can't be dismissed with Esc | **CONCRETE HALF CLOSED 2026-08-20; the shortcut system is split out as F-112.** RE-MEASURED, and the row overstated itself: it says the gesture "returns zero across all XAML/CS", but twenty-two windows carry `IsCancel="True"` and dismiss on Esc correctly today. The real gap was FOUR windows — `CookieCaptureWindow` (no buttons at all; it is a browser frame), `DpapiCorruptWindow`, `RobloxNotInstalledWindow`, `WebView2NotInstalledWindow`. Now 0. The capture window is the one that mattered and its own close handler was already written for the gesture: *"Closed before completion (X / ESC). A clean close is a deliberate Cancelled."* Dpapi got `IsCancel` on **Quit**, not a handler — Quit already does what IsCancel means, and the other button renames `accounts.dat`, which Esc must never reach. | (fix direction as above) | clean |
| F-055 | VC-3 | main-window | visual | 2 | 3 | All six nav destinations use MutedTextBrush at 11px, same role as helper prose — but the FPS-banner body is also MutedTextBrush/11px, so nav is at parity, not quieter **Closed 2026-08-10 by re-verification.** The nav band is two controls, `MainWindow.xaml:1144` and `:1161`, both on `SecondaryButtonStyle` resolving to `WhiteBrush`. `MutedTextBrush` is off the nav band entirely and `MutedTextFenceTests` now fences that token to prose. | Navigation shares its text role with de-emphasized prose (the "quieter than" comparative doesn't hold against the brand capture) | Give destinations a content-label foreground role; let grouping (container/rule/rail) carry the "chrome not content" distinction | clean |
| F-056 | VC-16 | main-window | visual | 2 | 3 | Gear glyph on Settings (:1020) is the only glyph among six otherwise-identical peer nav buttons; 8 ad-hoc glyphs vs one real WPF-UI SymbolIcon **Closed 2026-08-10 by re-verification.** The gear is gone. `MainWindow.xaml:1144` reads `Content="Settings"`. This row's fix direction was scoped to the nav-band gear only, and that surface no longer exists. Note the row's citation pointed at `:1020`, which is stale twice over. | One decorated button in a row of six reads as an accident, not a hierarchy signal | Standardize on WPF-UI's symbol set; decide icons per group, not per button (in-scope: nav-band gear only) | clean |
| F-057 | VC-17 | main-window | visual | 2 | 3 | MainWindow alone uses 11px x33, 12px x11, 10px x10, 9px x4, 8px x2 — nine sizes with no ladder | Nine type sizes, no ladder — but the dominant 11px body size (148 uses app-wide) is the app's own established default, so a hard "12px floor" overreaches | **CLOSED 2026-08-21 on its measurable claim, with the remainder split out rather than absorbed.** The row's evidence is MainWindow at nine sizes with no ladder; MainWindow is now on **four** (11, 12, 13, 14) and a ladder exists. App-wide the count went **11 distinct sizes to 8**. **THE CAUSE WAS STRUCTURAL AND THE ROW DID NOT NAME IT: there was no size resource of any kind.** All 326 declarations were raw numbers typed into markup, so nothing existed to inherit from and each control picked what looked right beside its neighbour — eleven is simply where that lands. `App.xaml` now declares Display 22 / Heading 14 / Body 12 / Meta 11 with ROLES, not just numbers, and `TypeLadderFenceTests` caps the count of raw sizes so it can only fall. **What is NOT done and is not claimed: the app is not on four steps.** 13, 18, 20 and 24 remain off-ladder and 163 declarations sit at 11px. That is F-118, and the ceiling ratchets down as it is worked. | clean |
| F-058 | VC-18 | main-window | visual | 2 | 3 | `MainWindow.xaml:1353-1385` Streamer-mode toggle in column 0, explanation in column 2 right-aligned, ~250px gap; duplicates the toggle's own ToolTip **Closed 2026-08-10 by re-verification.** The streamer band is gone from `MainWindow.xaml` and the relocated control at `PreferencesWindow.xaml:131-155` already ships this row's exact fix, helper prose beneath the control at `Margin="22,4,0,8"` with the tooltip duplication removed. Fixed by **F-008**. | Explanation isn't spatially bound to its control (though it is already subordinate by size, 10px) | Put helper prose directly beneath its control, indented, as Preferences already does (Margin=22,4,0,0) | clean |
| F-059 | CO-15 | cross-surface, iconography | consistency | 2 | 3 | ui:ImageIcon appears once (title bar); everything else is a literal text-run glyph; of six nav buttons only Settings carries one | A single decorated button in a row of six reads as an accident rather than a hierarchy signal | All six nav entries take an icon from one source or none do; decide as part of any Tools-container restructure | open |
| F-060 | QF-8 | main window, size and placement | qol | 2 | 3 | `MainWindow.xaml:9-11` hardcodes Height=600 Width=900, CenterScreen; nothing reads/writes placement; compact-restore fallbacks are unreachable in normal operation | A user who resizes the window gets it back at 900x600 centered every launch | Persist width/height/left/top + maximized flag alongside the QF-7 compact flag, with an on-screen-bounds check on restore | **CLOSED 2026-08-20, verified live:** default `(3950,240) 900x600` on a second monitor, resized to `(340,160) 1180x780`, relaunched to `(340,160) 1180x780`. Saved on `OnClosing` before the hide-to-tray branch, because closing the X hides rather than quits and saving only on shutdown would miss nearly every real use. `RestoreBounds` when maximized. The guard — `WindowPlacement.IsRestorableOnto` — tests overlap with the VIRTUAL screen rather than containment, so a window parked half off an edge survives while one saved on an unplugged monitor does not; restoring that would be worse than the forgetting. 12 tests. `SettingsReachabilityTests` caught the five new fields immediately and they are exempted with a stated shared reason. | clean |
| F-061 | CV-16 | Add Roblox account (cookie capture) | copy | 2 | 3 | `CookieCaptureWindow.xaml:19` "Add Roblox Account" (Title Case) vs `:6` "Add Roblox account — log in" (sentence case) vs `MainWindow.xaml:1000` "+ Add Account" — three casings on screen at once | One action, three casings, all visible during the same task | **CLOSED 2026-08-20. RE-MEASURED: the row named three sites; there are five.** The two it missed are instructions — `WelcomeWindow.xaml:61` "Click + Add Account." and `MainWindow.xaml:1704` "Click + Add Account to log in to your first Roblox account." — both of which would have told a user to click a button by a name that no longer existed. Now: button `+ Add account`, title bar `Add account`, header `Add a Roblox account`, and both instructions follow. The "— log in" suffix is gone. `docs/ui-capture-checklist.md`'s "Imperative" title convention is struck rather than edited, the way its retired siblings are: that list is the goal-4 evidence, and evidence quietly updated stops being evidence. | clean |
| F-062 | CV-19 | Preferences — run-on-login card | copy | 2 | 3 | `PreferencesWindow.xaml:52` "Adds a value under HKCU Run. Removes it when unchecked." — first helper line of the page, only string assuming Windows-internals literacy **Closed 2026-08-10 by v1.18 item 8**, verified against the tree. The hint is now at `PreferencesWindow.xaml:134` and reads "This applies to your Windows account only, and it does not start Roblox: that is the setting below. Unchecking this undoes it completely." No registry path; the label at `:128` already states the effect, so the hint carries only what the label cannot — scope, boundary, reversibility. Both claims verified in source rather than assumed: `StartupRegistration` writes to `Registry.CurrentUser`, and Disable deletes the value outright. **One half of this row's verdict does not hold and was nearly repeated as fact:** "first clause duplicates the checkbox label above it" is false textually — "Adds a value under HKCU Run" and "Start RoRoRo when Windows starts." share no wording. The duplication was semantic. The load-bearing half, a registry path being the wrong first read for a clan audience, is what got fixed. | The registry path is implementation, not effect — wrong information for a non-technical clan audience's first read; first clause duplicates the checkbox label above it | "Unchecking this undoes it completely." (trimmed to the reassurance the checkbox label doesn't already give) | clean |
| F-063 | VC-6 | about | visual | 2 | 2 | `AboutWindow.xaml:13-20` 8 literal SolidColorBrush resources; `:34` unbound canvas; `:96` `#15263A` = RowBgBrush's literal value, not bound; flatline shows a hard dark rectangle behind the icon **CLEAN v1.21.0.0, 2026-08-12 (item 4).** The two grounds are bound: `:34` and `:96` both take `RowBgBrush`. **This row's fix direction was overridden on one point and it matters** — it says *"drop the canvas background so the hero sits on the page field"*, and the canvas is BOUND instead. The mark's faces are fixed hexes tuned for a dark field; delete the plate and a light user theme leaves them floating with no ground, which none of the four dark built-ins would ever reveal. **And this row was wrong in the other direction too:** the eight brushes are the mark, not chrome, so removing them as the row's literal reading suggests would recolour the 626 Labs logo. `AboutArtworkTests` now holds both lines — no face may take a theme slot, the plate must — and found a live defect doing it: `:44`'s top face was bound to the `MagentaBrush` THEME SLOT, so the magenta block's lit face was a theme token per theme while its two side faces stayed fixed — shipped, and named by neither this row nor the cycle's spec. **SYMPTOM CORRECTED 2026-08-12 by the window-render spike, which measured it instead of reading it.** The "grey top on a magenta body" above **does not exist**: the middle block's top face is fully occluded by the cyan block stacked on it, because in an isometric voxel stack only the topmost block's top face is ever visible. Proven by painting that face pure `#00FF00` and rendering the real window — zero green pixels. **The markup fix stands on principle** (a theme slot inside brand artwork is wrong whether or not it currently shows, and becomes visible the moment the stack is reordered), but the visual consequence was asserted from reading markup and never rendered. That is the exact error this cycle was convened to stop, committed by the cycle itself, and `AboutMarkRenderTests` is what caught it. | About opts out of the theme system — but the logo's own brand hex must stay fixed per invariant 2, so only the canvas background and card binding are fixable | Bind the card to RowBgBrush (already that value), drop the canvas background so the hero sits on the page field; logo tint steps stay fixed hex | clean |
| F-064 | VC-8 | diagnostics | visual | 2 | 2 | `DiagnosticsWindow.xaml.cs:90-95` section headers at 12px SemiBold CyanBrush vs C8's 13px SemiBold WhiteBrush — real code-behind deviation, though flatline still shows the 4 headings clearly distinct in accent | Diagnostics defines section heading a third way, in C# where no markup review would catch it | Move the heading to the established 13px SemiBold body-foreground treatment; add non-color group separation | clean |
| F-065 | VC-10 | history | visual | 2 | 2 | `SessionHistoryWindow.xaml.cs:150-155` rows built on RowBgBrush alone, vanish under flatline — though the date-group heading (10px SemiBold Cyan) does survive, contra the original "no bucketing" claim **CLEAN v1.21.0.0, 2026-08-12 (item 3), taking THIS ROW'S SECOND OPTION.** The fix direction offers *"a baseline rule OR fixed leading rhythm"*; the cycle's spec picked the rule and the site ruled it out. `ThemeSlots.InteractiveEdge` documents that 1.4.11 governs component boundaries and NOT separators, and that binding the derived edge to a row rule *"would repaint every user's theme from a hairline to mid grey to fix a problem those surfaces do not have"* — enforced by `InteractiveEdgeBindingTests`. Deriving one here measures `#1F3149 -> #647181` in brand, which is that repaint; the plain `DividerBrush` bind measures 1.05-1.16 and does not read. A resting border would also break the app's own row vocabulary, where an edge means STATE (expired 1px, focused 2px). So the rhythm carries it: gutter 6 -> 12 against inset 10 -> 8, and the invariant `RowGutter > RowVerticalInset` was INVERTED before, arguing that a row's first line belonged to the row above. Gated by `HistoryRowRhythmTests`, which reads the constants rather than the spelling, and reads `.cs` because F-098 says a markup gate is evidence about markup only. **Owed: the pixel.** Three sessions under flatline is a human's call at the capture walk. Not on this cycle's flip list at plan time — item 3 was its fix and nothing was scheduled to close it. | Row-card boundaries carry meaning and vanish under flatline together; the bucketing half of the structure does survive via accent color | Give the row a non-fill boundary (baseline rule or fixed leading rhythm) so "which session is which" holds without fill | clean |
| F-066 | CO-2 | Modals/CookieCapture vs rest (in-scope residue) | consistency | 2 | 2 | 62 hardcoded hex duplicate an existing brush, mostly in out-of-scope modals; in-scope residue is `MainWindow.xaml:1474,1477` (mutex-recovery banner) and `AboutWindow.xaml:96` (#15263A = RowBgBrush) **Reconciled 2026-08-10: citation stale, count low, defect intact.** The mutex-recovery literals sit at `MainWindow.xaml:1572,1575` today, and they had already drifted to `:1479,1482` on `main` before the v1.17 branch existed, so this is pre-existing drift rather than branch churn. The same banner carries a **third** literal this row never counted — `Foreground="#F1B232"` at `:1568`. `AboutWindow.xaml:96` still holds exactly as written. Two further un-themed literal sites in the same file were given their own rows rather than folded in here, because their fixes belong elsewhere: F-085 and F-088. **CLEAN v1.21.0.0, 2026-08-12 (item 12), and FOUR OF THE FIVE HAD ALREADY GONE.** This row's in-scope residue was five hex occurrences on three lines: `#F1B232` on the contested-lock banner, then `#17D4FA` on `#0F1F31` and `#FFFFFF` on `#22314A` across the two recovery buttons. The two buttons took `CtaButtonStyle` and `SecondaryButtonStyle` in **v1.20**, taking four literals with them — a button sweep quietly closing most of a colour row, which nothing announced and nothing re-counted, so this row read as five-outstanding for a cycle while it was one. Item 12 bound the fifth to `RowExpiredAccentBrush` per item 1's warning-vocabulary ruling, measured on the PAGE field rather than a banner fill: **8.85 brand / 7.23 midnight / 9.85 magenta-heat / 12.84 flatline**, byte-identical in brand and magenta-heat. **`MainWindow.xaml` now holds ZERO colour literals** — the one file `ThemedStatusColourTests` has never been allowed to blanket-exempt. The out-of-scope modals and `CookieCaptureWindow` are untouched and belong to F-079. | Most of the 62-count sits out of scope; residue is three literals that should bind existing brush keys | Replace the three in-scope literals with their existing brush keys | clean |
| F-067 | CO-14 | cross-surface, type | consistency | 2 | 2 | Mono role declared two ways — fallback stack (16 sites) vs bare "Consolas" (7 sites, 3 in-scope); `RenameWindow.xaml:27` comment confirms the stack is the intended contract; zero display-family declarations app-wide | Seven meta-label sites skip JetBrains Mono where installed; the display role (Space Grotesk) has no expression anywhere and must not be abandoned per invariant 3 | One MonoFontFamily resource referenced everywhere; ship and reference a display-family resource (the "declare it intentionally system-font" branch is refused) | open |
| F-068 | QF-15 | every button in the app | qol | 2 | 2 | 115 Button declarations, ~73 set Background inline; only keyed button style app-wide is CyanCtaButton; App.xaml's only control style is a ToggleButton style **Re-counted 2026-08-10, and the number is FLAT.** 61 un-migrated button call sites across 24 files. Measured with one scanner at three commits: pre-wave-5 (`bf1fdef^`) 101 buttons / 0 styled / **96** hand-copied; at `bf1fdef` (2026-08-05, the commit that wrote this file's own "63 across 15 files" comment) 103 / 34 / **61**; at HEAD 105 / 36 / **61**. Wave 5 migrated 35 in a single pass and **nothing has moved in the five days since**. The "63 across 15 files" figure reproduces at neither snapshot and should be treated as unsourced. **PARTLY SHIPPED, and the two halves are very uneven.** `Controls/ControlStyles.xaml` ships `PrimaryButtonStyle`, `SecondaryButtonStyle` and `SecondaryStrongButtonStyle` (not `Tertiary`), merged at `App.xaml:15`. But the **template-trigger half is 0% shipped** — zero `ControlTemplate` and zero `Style.Triggers` in that file, so hover and pressed still come from WPF-UI's dictionary, which `ThemeService` never touches. The row's literal clause *"App.xaml's only control style is a ToggleButton style"* is **still true of App.xaml itself** (`SelectionDotStyle:61` is its only `Style`), so a reader who does not open `Controls/` concludes nothing shipped. The 61 break down as **22 cyan CTAs with no style proposed for them at all**, 8 magenta CTAs, 13 raw-hex recipes, 18 other; `MainWindow.xaml` alone holds 19. Across buttons plus inputs plus borders it is 121 sites in 26 files — inputs are 11 of 11 done, borders 60 of 76 still hand-themed. `CyanCtaButton` also survives at `PluginsWindow.xaml:23` and 3 more sites. **Re-counted 2026-08-10 after v1.18, and the number finally MOVED: 61 to 55 un-migrated button call sites, 24 to 22 files.** Six, and the accounting matters more than the six. **Five came from item 9's Close sweep, and the direction is the point** — those nine Close buttons adopted `SecondaryStrongButtonStyle`, an existing rank, rather than hand-copying attributes inline to reach the same look. Hand-copying would have ADDED four new recipes, which is this row's actual defect, so a sweep that moved the counter down instead of up is evidence the rank vocabulary is doing its job. The sixth is Stop all giving up its raw `#F22F89` fill and `#FFFFFF` foreground when it took `DestructiveButtonStyle`. **The delta was reproduced independently at the branch point and at HEAD with one scanner: minus 6 sites, 24 files to 22, `AboutWindow.xaml` and `SessionHistoryWindow.xaml` leaving the offender list entirely.** **This row does NOT close.** 55 sites is still 55, the template-trigger half is still 0% shipped — zero `ControlTemplate` and zero `Style.Triggers` in `Controls/ControlStyles.xaml`, so hover and pressed still come from WPF-UI's dictionary, which `ThemeService` never touches — and `App.xaml`'s only `Style` is still `SelectionDotStyle`, so a reader who does not open `Controls/` still concludes nothing shipped. Five days of flat, then six in one cycle, is a rate, not a fix. **CLOSED v1.20.0.0, 2026-08-11.** Scanner definition (`spec.md > §6`, committed as `scripts/count-button-sites.ps1`): an occurrence of `<Button` or `<ui:Button` in a `.xaml` under `src/ROROROblox.App/` whose opening tag carries no a `Style` attribute referencing a `…ButtonStyle` resource (Static or Dynamic). **Branch point 63 un-migrated across 22 files; close 0 across 0, of 108 total.** Comparable to that branch-point figure and to nothing before it — the earlier 55, 72 and 115 were measured under definitions nobody wrote down, and the definition itself was corrected five times mid-cycle (phantom `<Button.Style>` tags, `BasedOn` property elements, local styles inheriting ranks, too-narrow lookahead, and a whole control type outside the definition). Seven Button ranks plus one ToggleButton rank now carry 108 declarations. `ButtonRankFenceTests` is the successor that stops site 109 being written the old way: 112 declarations, 1 named exemption. **SCOPE CORRECTION, same day.** The 63 → 0 above is a count of XAML *declarations*, which is what `spec.md > §6` defines and what the committed script measures. It is not a count of buttons. Five were constructed in C# with `FindResource`, outside every instrument this cycle built — including `ButtonRankFenceTests`, whose own commit claimed 99.1% coverage. Three were byte-identical to a rank that already existed (`FriendFollowWindow` Follow and `SquadLaunchWindow` Launch all → `CtaButtonStyle`; SquadLaunch Remove → `SecondaryButtonStyle`) and are migrated. The row stays `clean` because its definition was written down and honoured; the correction is that the definition was narrower than the sentence "every button in the app" at the top of this row. See F-098.  | The primary/secondary distinction the brief describes is dozens of hand-copied attribute sets with no shared style behind them — same root cause as C6's flatline collapse | Define PrimaryButtonStyle/SecondaryButtonStyle/TertiaryButtonStyle in App.xaml with template-level triggers bound to DynamicResource brushes [new-mechanism] | clean |
| F-069 | QF-17 | account row context menu | qol | 2 | 2 | `MainWindow.xaml:302` "🎲 Reroll identity" — the sole emoji hit in a targeted app-wide grep; sibling menu items carry no glyph **Closed-as-ruled 2026-08-10 by re-verification.** This row's fix direction is to drop the glyph. The **Rulings by the user** section closed that entire class on 2026-08-04, and `CLAUDE.md` cites the same ruling's other victim, F-040, as its worked example of *rulings override rows*. F-040 sat open five days against a decision already made; this one sat open six. Second instance of the identical failure. | Exactly one emoji survives in shipped copy, against a rule the brief assumed was already enforced | Header="Reroll identity" — drop the glyph, matching glyphless siblings | closed |
| F-070 | CV-14 | Join by link | copy | 2 | 2 | `JoinByLinkWindow.xaml:27-33` cyan+white, no magenta, vs 12 siblings pairing Cyan/Magenta/White; `WelcomeWindow.xaml:38-43` also ships half the duo — a second instance the original finding missed **CLOSED-AS-RULED v1.21.0.0, 2026-08-12 (item 10). THE ROW DOES NOT REPRODUCE.** Measured at HEAD before acting. **`WelcomeWindow.xaml` already ships magenta** — line 130, a SemiBold `MagentaBrush` U+2605 run — so one of the two cited files is not an instance of the defect at all. **And the split runs the other way:** counting windows and excluding `App.xaml` and `ControlStyles.xaml`, which are dictionaries, **8 ship both hues and 10 ship cyan only**. Cyan-only is the MAJORITY pattern, not a two-file deviation from twelve. Acting on the fix direction would have moved two files toward a convention the app does not hold, on a count wrong in both directions. If the duo should be a real rule it needs a gate applied to all ten, which is its own row, not four lines of markup inside a copy item. | Two windows ship the header device with half the 626 Labs duo — invariant 2, broader than originally counted | Resolved for free by CV-1's chrome rule; if deferred, restore the magenta " / " run in both windows | closed |
| F-071 | CV-17 | tray menu / Diagnostics | copy | 1 | 3 | "Multi-Instance" surfaces in 8 places (tray tooltip x3, tray menu x3, `:203`, Diagnostics row + bundle line), not the 2 originally claimed | A clan member can't map "Multi-Instance: OFF" to anything they did — but renaming across 8 surfaces is a product decision the finding underprices | If undertaken, rename consistently across all 8 sites to "Side-by-side clients" — not a single-string fix **RE-MEASURED 2026-08-20 by wave 13, and the price moved.** The eight sites are now four: `Core/MultiInstanceStatusLine` owns the tray tooltip (3 arms), the tray menu header (3 arms) and the new main-window footer (3 arms + 3 tooltips) — one file, one edit — leaving `DiagnosticsWindow` (the details row and the support-bundle line, both fed by `DiagnosticsSnapshot.MultiInstanceState`) and `MutexHostStateAdapter`'s `"On"`/`"Off"`, which crosses the plugin contract and is a compatibility question, not a copy one. The row stays OPEN and unchanged in kind: this was never underpriced because of the edit count, it is underpriced because renaming the product's core switch for a non-technical audience is a decision the user takes, not a sweep anybody performs. It is now a cheap decision instead of an expensive one. | open |
| F-072 | AX-13 | History | a11y | 2 | 1 | 503 unpaired Text nodes, 100 sessions, zero DataItem/List containers; only two real focus stops (Clear history, Close), both terminal | History reads to assistive tech as ~500 loose strings with no session boundaries; its total lack of interactive surface is placement evidence for QF-19's "tool not preference" conclusion | A repeated five-field record is a table — give each session a row container whose name composes the fields (placement evidence only, not a rebuild this wave) | open |
| F-073 | AX-15 | main window, FPS-cap banner | a11y | 2 | 1 | UIA tree shows Button 'Dismiss' declared before the banner's own text; visual right-alignment via DockPanel.Dock means a sighted user sees nothing wrong | A screen-reader user is offered "Dismiss" before being told what there is to dismiss | Declare banner content before the dismiss action in markup; name what each Dismiss button dismisses | **HALF DONE 2026-08-20; stays open for the other half.** Both Dismiss buttons now name what they dismiss — "Dismiss the frame-rate cap warning", "Dismiss the Bloxstrap warning" — so a reader reaching the action before the message still hears what it is for. **The declaration order is deliberately NOT swapped.** `DockPanel.LastChildFill` is what lets the message wrap into the remaining width; the Button is declared first precisely so it can dock right and let the text fill. Reordering needs the layout re-proved on a render, not reasoned about, and this wave did not do that. The reason is written beside the markup so the next attempt starts from it. | open |
| F-074 | CV-18 | Stop all confirmation | copy | 1 | 2 | `StopAllConfirmWindow.xaml:36` "UNSAVED GAME STATE WILL BE LOST" in the JetBrains Mono 10px uppercase label style at #5A6982, the dimmest text on the surface **CLEAN v1.21.0.0, 2026-08-12 (item 10).** The warning is body prose now: 12px, the body's own size, SemiBold at full opacity against the body's 0.85, so the consequence is the most legible thing in the dialog instead of the least. Uppercase mono at 10px reads as a machine label, which is the row's point — the treatment said *footnote* while the words said *irreversible*. Copy is *"Unsaved progress in your running games will be lost."* **The colour stays a literal on purpose:** every foreground in that modal is one, F-079 owns migrating the file, and half-theming a single line would leave a themed warning on an un-themed surface. | Shape says "label," words say "you will lose work" — the only warning in a destructive confirmation dressed as a section header | Move into the body as ordinary 12px prose: "Anything not saved in those games is lost." Reserve mono-uppercase for genuine labels | clean |
| F-076 | (wave 2) | Settings -- Alerts & memory | qol | 3 | 3 | Wave-2 flatline capture of the Alerts page rendered both webhook URLs in full plaintext, including the token segment, in a screenshot taken by a scripted capture with no consent step. Capture deleted; `docs/ui-evidence/` is gitignored, so nothing was committed | A Discord webhook URL is a bearer credential — the token is the whole auth. This app ships a streamer mode whose entire job is to keep identity off a live stream, and Settings > Alerts puts a working credential on screen at full contrast. A stream, a screen-share, or a support screenshot leaks post-access to the user's clan channel. Found by this wave's own tooling, which is the proof that it is easy to hit | Mask the saved value by default (show scheme + host + last 4, e.g. `https://discord.com/api/webhooks/...BeqXP`) with an explicit reveal; keep the field editable by paste-over. Pairs with the streamer-mode work already in the campaign | clean |
| F-077 | (wave 5 gate) | main-window per-account row | a11y | 3 | 3 | `MainWindow.xaml:657` — the caption-colour swatch is a `Border` with `Cursor="Hand"`, a `MouseLeftButtonUp` handler and a ToolTip, still bound to `DividerBrush`: 1.26:1 in brand | A UI component under WCAG 1.4.11 that looks like decoration in the markup. Wave 5's fence originally hard-listed `Border` as decorative and would have failed the build on this exact fix; the fence is now role-based, so the fix is unblocked but was not taken | Bind `InteractiveEdgeBrush`. One attribute; the fence already permits it | clean |
| F-078 | (wave 5 gate) | Settings -- Appearance | qol | 2 | 2 | Once the edge question is answered for a theme, `AppSettings.EdgeRemediationAnswers` holds it forever; nothing in the UI re-opens it. `EdgeRemediationWiringTests` notes the store supports re-answering, but no control calls it **Closed 2026-08-10 by v1.18 item 5**, verified against the tree. `ReviewEdgeButton` at `PreferencesWindow.xaml:755-763` on the Appearance page, handled at `PreferencesWindow.xaml.cs:1084`, which calls `ThemeService.ReopenEdgeQuestion()` and then puts the same question through the same `EdgeRemediationWindow.AskIfPendingAsync` and records the answer through the same existing setter. It reads "Review button outlines" rather than "ask me again" on purpose: Story 1.4 requires it to be discoverable WITHOUT knowing the prompt ever happened, and "again" is a word only somebody who saw the first one can parse. **A limit on this fix, recorded rather than glossed:** `EdgeRemediation.Decide` returns `DeriveSilently` for every `IsBuiltIn` theme by design, so brand, midnight, magenta-heat and flatline all correctly report "nothing to choose" through the explanatory dialog at `:1088-1096`. Exercising the real path needs a hand-authored theme whose divider measures under 3:1 against its navy, so the affordance is unverifiable on a default install. Not a defect; a testing note for whoever walks it next. | A consent prompt with no way back is a one-shot decision made in the first ten seconds of a launch. Someone who declined to get on with playing has no route to change their mind short of editing settings.json | A "re-ask about my theme" affordance on the Appearance page, calling the existing setter | clean |
| F-079 | (wave 5 gate) | Modals/ (6 windows) | consistency | 2 | 3 | Twelve `Background="#..."` literals across `StopAllConfirmWindow`, `LeftoverProcessesWindow`, `RobloxAlreadyRunningWindow`, `RobloxNotInstalledWindow`, `WebView2NotInstalledWindow`, `RenameWindow`; none uses the three styles wave 5 created | These modals opt out of theming on their buttons, so under a user theme they render brand colours on a themed field. `EdgeRemediationWindow` is the only window in that folder speaking the new vocabulary **Re-counted 2026-08-10 after v1.18 item 10: `Background="#..."` literals 12 to 11, and `Modals/`'s total hex occurrences 54 to 52.** `StopAllConfirmWindow`'s Stop all took `DestructiveButtonStyle` and gave up both its raw `Background="#F22F89"` and its `Foreground="#FFFFFF"`, so that file goes 2 to 1 on the `Background` count and 7 to 5 on total hexes. `ThemedStatusColourTests`' `AllowedXamlLiteralCeiling` moved 97 to 95 with them, on the standing rule that slack left inside an allow-listed region is room for a new literal to arrive wearing an old row's badge. **This row does NOT close** — its other six modals are untouched. Current per-file `Background="#"` split: `RobloxAlreadyRunningWindow` 4, `LeftoverProcessesWindow` 3, `StopAllConfirmWindow` 1, `DpapiCorruptWindow` 1, `RobloxNotInstalledWindow` 1, `WebView2NotInstalledWindow` 1. **Two file-set errors from the 2026-08-10 re-verification, written back here because nobody did:** `RenameWindow.xaml` is named by this row and holds ZERO `Background="#"` (its only hex is a `BorderBrush` at `:67`), and `DpapiCorruptWindow.xaml` holds one this row never names — `ThemedStatusColourTests.cs:313-318` already knew that half and it was never carried across. Style-adopting windows in `Modals/` also grew 1 to 2: `LaunchHeadroomWindow.xaml:56,61` arrived with PR #96 after this row was written. | Convert to `PrimaryButtonStyle` / `SecondaryStrongButtonStyle`, keeping the deliberate magenta on destructive confirms | **CLOSED 2026-08-20 — its stated claim was ALREADY resolved and nobody flipped the row.** It names eleven `Background="#"` literals on modal buttons; there are ZERO, and all four styles those buttons use are pure `DynamicResource`. Total hexes in `Modals/` had gone 52 to 24 unrecorded. What remained was the same thesis on a different element, and is now also done: 22 `Foreground` literals became `CyanBrush`/`MagentaBrush`/`WhiteBrush`/`MutedTextBrush`. **One stays** — `#F1B232` on `RobloxAlreadyRunningWindow`'s still-locked tick, because the theme has no amber and an eleventh slot would break every user theme on disk, the same reasoning `ContrastGuard` records. `ThemedStatusColourTests`' ceiling moved 57 to 35 with them, on its own rule that slack is room for a new literal to wear an old row's badge. | clean |
| F-075 | CV-12 | cross-surface, row controls, empty states, Welcome | copy | 0 | 4 | `MainWindow.xaml:131,334,700,704,578,1141` — all Emoji_Presentation=No text glyphs; `WelcomeWindow.xaml:123,130` teaches ☰/★ by showing the same glyphs | These are functional glyphs, not decoration — sole content of controls too small for a label, or state indicators the Welcome tour teaches by name | No change — recorded as a deliberate keep so a later pass doesn't strip them; ▶ Start (:1682) flagged as the one marginal case | open |
| F-080 | (field request 2026-08-07) | main-window status bar | qol | 3 | 4 | Status bar reads "8 Roblox clients running" and says nothing about what they cost. During the 733 investigation the answer — 23.0 GB across 8 clients — was only obtainable by running a PowerShell probe against the process list | The number a multi-client user actually needs to decide whether to launch another alt is total RAM in use, and today it lives nowhere in the app. The memory watchdog already samples exactly this every 15s; it is surfaced only as a warning banner after the fact, never as a running figure | Add total private bytes across tracked clients to the status bar, between the client count and Compact. Reuse `MemoryWatchdog`'s existing samples — no new probe. Este's request, from live use | clean |
| F-081 | (investigation 2026-08-07) | `RobloxProcessTracker.cs:385` | qol | 4 | 2 | The exit log line carries pid + accountId and nothing else: no exit code, no last-known private bytes, no session duration. **CORRECTED 2026-08-07:** the original wording of this row said we "record NOTHING when a client ends," which was false — the exit IS logged, and that log line is what proved the 733 updater-kill. What is missing is the WHY, not the event | Distinguishing a user close from a crash from an external termination is impossible from our one line. On 2026-08-07 this produced a WRONG conclusion, not just a slow one: three exits were read as Roblox's updater killing clients when the user had closed them himself, and nothing in our logs could tell the two apart. A reason on the exit line would have made that error impossible | Enrich the existing line — exit code, last private-bytes reading, session duration. One log statement, not a new teardown path. Add a distinct line when several clients exit inside a short window, which is the updater-kill signature | clean |
| F-082 | (investigation 2026-08-08) | `MemoryDefaults.cs:27` + `MemoryWatchdog.cs:212` | qol | 5 | 3 | The watchdog has exactly two triggers and neither can fire for an oversubscribed multi-client user. (1) Per-client cap = 35% of installed RAM, floored at 4 GB — on 16 GB that is 5734 MB, and a Roblox client peaks around 3300 MB, so it never crosses at any tier at or above 16 GB. (2) Projection needs `aggregateGrowth > 0`; once clients plateau, `hasProjection` is false and it cannot fire either. There is NO absolute low-headroom check. Measured 2026-08-07: per-client median 2650 MB, peak 3280 MB. Ten accounts need ~26.5 GB; a 16 GB machine has ~12.9 GB usable — oversubscribed by 13.6 GB, with RoRoRo silent throughout | `852bc66` (v1.12.0.0) replaced a FIRING cap with one that cannot fire. The v1.11.1 logs from 2026-08-01 show the old fixed cap crossing repeatedly at 2640 MB; after the change it is 16886 MB on the same machine. Scaling a PER-CLIENT cap with installed RAM is the wrong axis for a multi-client app — the risk is aggregate, not per-client, and the more RAM you have the higher the cap gets. This does not cause drop-outs; it is why nobody is warned before one. Users on less RAM hit the wall first and get the least notice | Add an absolute headroom trigger — warn when available memory drops below the reserve, independent of growth. Make aggregate footprint the primary signal. Pairs with F-080, which puts the running total on screen so the user can see it coming | clean |
| F-083 | (follow-up 2026-08-09) | `MemoryDefaults.ExpectedClientMb` | qol | 4 | 2 | The pre-launch advisor and the fits-how-many arithmetic both consult a constant — 2650 MB — measured on ONE machine (47 GB, Pet Sim 99, Roblox 733). Hardware, game and client build are all variables. Meanwhile `MemoryWatchdog` samples every tracked client's private bytes every 30 seconds and has this machine's real answer in hand | A threshold tuned to the wrong number either cries wolf or never fires, and both end with the warning being ignored. Este's framing: it should learn. THE TRAP that makes this non-trivial — under memory pressure Windows trims working sets, so `PrivateMemorySize64` reads LOWER exactly when the user is in trouble; learning from those samples shrinks the footprint, quiets the warning, and does so BECAUSE things got worse. A feedback loop that disables the alarm in proportion to the fire | Learn p75 of SETTLED clients (reuse the existing `MinimumObservation` growth gate), discard every sample taken while `BelowReserve`, clamp to [1200, 5000] MB, seed 2650 until 20 samples, reset on a client-version change. Feeds the advisor only — NOT the anomaly cap (a heavy machine would teach itself that heavy is normal, reintroducing F-082 by another route) and not the headroom trigger (that is a measurement, not an estimate). Scoped: docs/superpowers/specs/2026-08-09-rororo-adaptive-footprint-scope.md | open |
| F-084 | (F-001 review, 2026-08-09) | cross-surface — every owned dialog | qol | 3 | 4 | Eight owner-resolution sites gate on `owner.IsLoaded` alone before assigning `dialog.Owner`: `LaunchHeadroomWindow.xaml.cs:74`, `EdgeRemediationWindow.xaml.cs:94`, `ConsentSheet.xaml.cs:46`, `App.xaml.cs:984`, `:1008`, `:1594`, `:1622`. `MainWindow.xaml.cs` cancels close and calls `Hide()` (the normal minimize-to-tray), and **`Hide()` does not unload a window, so `IsLoaded` stays `true` while `IsVisible` goes false**. F-001 fixed exactly this in `StopAllConfirmWindow.Confirm` and `WelcomeWindow.ShowTour` by adding `&& owner.IsVisible`; the other eight were out of that finding's scope. **Re-counted 2026-08-10, and the number was wrong in both directions.** The headline says eight sites; the row's own enumeration lists seven; the tree has seven, all citations resolving verbatim. Worse for scoping: **four of the seven call `SurfaceMainWindow(owner)` on the following line**, which `Show()`s and un-minimises the owner before `ShowDialog()` (`App.xaml.cs:2060-2071`, in tree since May 2026). **Real exposure is three, not eight** — scoping eight one-line fixes buys four no-ops. Both errors predate this row. And it misses four genuinely unguarded `Owner` assignments at `MainViewModel.cs:3597, :3650, :3657, :3664` that no row owns. | Any tray-invoked dialog raised while RoRoRo is minimized becomes an owned child of a window the user cannot see, so dismissal returns activation to an invisible owner instead of falling through. `StopAllConfirmWindow.xaml:13` documents the intended behaviour — “CenterScreen (no owner) — the tray can fire this with the main window hidden” — which the `IsLoaded`-only guard silently defeats. | Add `&& owner.IsVisible` at each site. Better: extract the nine-line resolve-and-parent block (now duplicated across all of them) into one helper so the rule lives once — the spec for F-001 originally called for passing the owner in as a parameter, which is the shape that makes this collapsible. Also drop the now-stale “Owner resolution matches `LaunchHeadroomWindow.ShouldProceed`” claim at `StopAllConfirmWindow.xaml.cs:33`, true before F-001 and false after. | **CLOSED 2026-08-20.** All eleven owner sites now route through one `Parented` helper over a pure `DialogOwnershipDecision`, beside `BlockedStartupDecision` and `LeftoverStartupDecision`. That is the "better" fix this row itself asked for, and it delivers part of F-106 — twelve copies became one seam. **The placement half was not in the row and is why this is more than a one-word guard:** twenty-one windows declare `WindowStartupLocation="CenterOwner"`, and with no owner WPF degrades that to `Manual`, the top-left corner, not the screen centre. Adding `&& owner.IsVisible` alone would have traded an invisible parent for a dialog in the corner. | clean |
| F-085 | (v1.17 flatline, spec §7) | main window, Bloxstrap warning banner | visual | 2 | 2 | `MainWindow.xaml:1630-1644` holds **three** un-themed literals, not the two spec §7 names: `Background="#3F3000"`, `BorderBrush="#8F7000"`, and the body `Foreground="#FFE3A6"`. None binds a brush key, so the banner renders the same warm amber under all four built-in themes, including `flatline`, whose field is `#101010` and whose entire argument is that no hue carries meaning. **Corrected 2026-08-10 by re-verification, and this row was falsified by its own cycle.** The literal count is exact — three, at `MainWindow.xaml:1630`, `:1631`, `:1644`. The evidence claim is not. This row says the literals are invisible to both fences and that nothing would catch a fourth being added; item 3a of the same v1.17 cycle then shipped `ThemedStatusColourTests.NoColourLiteralIsWrittenIntoAppXamlOutsideTheAllowList` (`:383`), which walks App XAML, allow-lists this banner **by this row's own finding id** (`:290-295`), and enforces `AllowedXamlLiteralCeiling`. A fourth literal in this banner now fails that ceiling. **Ceiling re-measured 2026-08-10 after v1.18 item 10: 95, down from 97**, independently re-counted at HEAD as App.xaml 11 + MainWindow.xaml 8 + AboutWindow.xaml 10 + CookieCaptureWindow.xaml 14 + `Modals/` 52 = 95 exactly. The two that left are F-079's, not this row's — Stop all's raw fill and foreground going onto the governed path. This row's own three literals are untouched and still sit on the allow-list. **Written by item 6 and invalidated by item 3a, same day, same cycle.** Also: this row's fix direction points at F-068's shared banner recipe, and F-068 has **no banner primitive at all** — that half is 0% shipped, not partial. **RULING v1.21.0.0, 2026-08-11, recorded before the migration it authorises.** Both warning banners collapse onto the one themed warning recipe already shipping one Grid row above at `MainWindow.xaml:1570-1572` — `RowExpiredBgBrush` fill, `RowExpiredAccentBrush` edge and text, and a literal `▲ ` Run. **The distinction between the two is carried by text and the glyph, not by hue.** The Bloxstrap comment calls its amber deliberate, "distinct from the red-ish compat banner above it", and that distinction exists only because one banner ignores the theme: under `flatline` the compat banner goes grey and Bloxstrap stays amber, so what tells the reader these are different kinds of warning is the defect. **Precedent is F-032**, which faced the identical shape — `MutedTextBrush` vs `WhiteBrush` measured 2.42:1 in brand and **1.00:1 under flatline**, so colour could not carry "quiet" under a theme the app ships, and weight took over. Same ruling here: two warnings should look like two warnings, and what they say is what tells them apart. **Measured before migrating, not after** — `RowExpiredAccent` on `RowExpiredBg` through `ContrastGuard` over the shipped hexes is **7.13 brand / 6.58 midnight / 7.13 magenta-heat / 7.33 flatline**, clearing the 4.5 floor in all four, so the migration is a straight rebind and no foreground moves. No eleventh theme slot is added; invariant 6 holds. **CLEAN v1.21.0.0, 2026-08-12 (items 1 and 2).** All three literals bound to the compat banner's recipe — `RowExpiredBg` fill, `RowExpiredAccent` edge and text — plus the `▲` prefix it lacked. The ruling that made it possible is item 1's, recorded in this row before the migration that depended on it: **the two banners collapse onto one recipe and the difference is carried by text and the glyph, not by hue**, because the old distinction survived flatline only by ignoring the theme. Measured before migrating, not after: 7.13 / 6.58 / 7.13 / 7.33, all clear of 4.5, so no foreground moved. Gated by new `BannerRecipeTests` (no literal in either banner, both bind the same recipe, the pair clears AA in every theme) and `ExpiredRowRedundancyTests.TheBloxstrapBannerPrefixesTheSameWarnGlyph`, all proven failable. The allow-list entry was RETIRED rather than left matching nothing, which the deliberate-breakage run confirmed: a re-added literal was reported as an offender instead of inheriting this row's badge. | A colour-only banner that opted out of the theme system and stayed out. Not a legibility failure — amber on brown clears AA — but it is the one warm block left on the main window under a theme that has no other hue, and it is invisible to both fences v1.17 shipped: `ThemedStatusColourTests` walks `*.cs` and this is XAML, `ContrastPairGateTests` reads `{DynamicResource}` attributes and this is a raw hex. A literal matches neither pattern, so nothing would catch a fourth one being added tomorrow. | Bind a themed banner recipe rather than a one-off. Belongs with **F-068**'s shared button/banner style work — the mutex-recovery banner (F-066) and the compat banner are the same shape, and fixing one by hand is how three near-copies became three. Found during v1.17's `/spec` recon and deliberately left out of that cycle. | clean |
| F-086 | (v1.17 gate audit) | `ContrastPairGateTests` / every `MutedTextBrush` binding | a11y | 3 | 1 | The gate measures only elements declaring both `Background` and `Foreground` inline as `{DynamicResource}`. Scanned against `HEAD` with the gate's own regexes on 2026-08-10: **44 elements, 8 distinct pairs**, and `MutedTextBrush` is the foreground of none of them. It used to be. Commit `2c9ab16` (PR #100, F-032's own fix) rebound three `MutedTextBrush` foregrounds to `WhiteBrush`, merging `MutedTextBrush on NavyBrush` into the existing `WhiteBrush on NavyBrush` — same 44 elements, one fewer pair. The gate's three in-code claims of "9 distinct pairs" describe an app that has not existed since that merge; corrected in the same PR as this row. **Re-measured 2026-08-10: 105, grew by 1.** **Re-measured again at the end of v1.18: 113, grew by 8** — this cycle's own new hint text added them, counted as `Foreground="{DynamicResource MutedTextBrush}"` bindings in App XAML at the branch point (105) and at HEAD (113). Recorded because a fence that cannot see a token gets blinder as the token spreads, and this gap widened inside the very cycle that closed thirteen rows beside it. The same figure in `ContrastPairGateTests`' doc comment was corrected at item 12; this copy fell between item 11, which owns this file but not `src/`, and item 12, which owns `src/` but not this file. The scan was reproduced independently with the gate's own regexes — 44 elements across 18 files collapsing to 8 pairs, with `MutedTextBrush` the foreground of none of them, exact. **Re-measured again 2026-08-10 after v1.18 items 9 and 10, and the element count MOVED: 39 elements across 16 files, down from 44 across 18. Pairs are unchanged at 8.** The nine swept Close buttons stopped declaring `Background` and `Foreground` inline when they took `SecondaryStrongButtonStyle`, so they left the gate's field of view — `AboutWindow.xaml` and `SessionHistoryWindow.xaml` dropped out of the scanned set entirely. `MagentaBrush` on `WhiteBrush` is still 8 sites, so F-050's exemption measures exactly what it did. **This is the row's own thesis arriving again, from the opposite direction:** a fix that is unambiguously good shrank the gate's coverage by 5 elements and 2 files, `MinimumElements` is 30 so nothing failed, and nothing announced it. **Stale in code and not corrected here, because item 11 does not touch `src/`:** `ContrastPairGateTests.cs:65`, `:196`, `:205` still quote 44 as the current measurement. Those three are item 12's or the next PR's, and a stale number inside a fence's own message is this defect one level down. **CLEAN v1.21.0.0, 2026-08-12 (item 6), AND IT WENT RED ON ITS FIRST RUN ON A REAL DEFECT.** `ContrastPairGateTests.NamedPairs` now measures three pairs unconditionally, independent of what the element scan finds: `MutedText` on `RowBg`, on `Bg`, on `Navy`. Exactly this row's fix direction. The first run reported **midnight `MutedTextBrush` on `RowBgBrush` = 4.19:1**, under AA and shipped, invisible to every instrument in the suite since PR #100 removed the last declared pair. **Ruled FIXED, not exempted:** midnight's `MutedText` moved `#6F7E92` -> `#768598`, a 6% blend toward that theme's own `White` and the smallest step clearing the floor with headroom. RowBg 4.19 -> 4.60, and Bg/Navy 4.50 -> 4.95 — that pair had been sitting at exactly 4.504, the thinnest margin in the app. Fixed rather than exempted because nothing constrained it: F-050 is exempted because its best theme-derived foreground still only reaches 4.40 under brand, which is a real constraint, and this was one slot moving a few percent with every pair it touches improving. The floor was not lowered. Ratios are written to test output every run, which is this row's actual complaint — the numbers lived in prose in a spec and were re-derived by nobody. **F-050 does not close; this was its prerequisite.** | F-032's fix removed the only declared pair whose foreground was the prose token, so a token with roughly 104 bindings is now measured by nothing on any test run. `MinimumPairs` is 6, so the drop from 9 to 8 failed nothing and announced nothing. A gate that loses coverage silently is worse than one that never claimed it — this is the same defect class as a stale register row, wearing a test's clothes. | Measure a small named-pair list unconditionally — `MutedTextBrush` vs `WhiteBrush` and `MutedTextBrush` on `RowBgBrush` at minimum — so the numbers `spec.md > §4.3` states by hand get re-derived on every run instead of once, in prose. The inherited-fill composition problem the gate correctly refuses to guess at does not apply to a fixed pair somebody names deliberately. | clean |
| F-087 | (v1.17 fence, item 3) | Plugins — consent sheet | visual | 1 | 1 | `Plugins/ConsentSheet.xaml.cs:90-92` — `CapabilityRow.NamespaceBrush` returns `(Brush)(Application.Current.TryFindResource("CyanBrush") ?? new SolidColorBrush(Color.FromRgb(0x17, 0xD4, 0xFA)))` when the capability is host-enforced, and the `MagentaBrush` / `0xF2, 0x2F, 0x89` equivalent when it is not. `ConsentSheet.xaml:92` binds it as `Foreground="{Binding NamespaceBrush}"`. Surfaced by v1.17 item 3's colour-literal fence, which spec §7 never named; it sits on that fence's allow-list at `ThemedStatusColourTests.cs:84-89` with its reason inline, and this row is what makes that entry traceable to a finding instead of a silent permission. **CLEAN v1.21.0.0, 2026-08-12 (item 7).** `NamespaceBrush` is deleted and the branch is a `Style` + `DataTrigger` on `IsHostEnforced` in `ConsentSheet.xaml`. **Two defects, not one.** The literal `TryFindResource` fallbacks are the obvious half. The other is the property itself: a brush resolved at READ time hands back the instance it found, and `ThemeService.ApplySlot` REPLACES instances, so the sheet kept whatever was current when the row was built and would not have followed a theme change while open — the exact failure `ThemedStatusColourTests`' own header describes for the two converters it was built to delete, still shipping one file over. Also recorded at both ends: the tint is a SECOND carrier and never the only one, because magenta-heat sets Cyan and Magenta to the same value and the two namespaces render identically there. `NamespaceLabel` is what distinguishes them. The `.cs` allow-list entry was retired and its count comment re-derived from 11 to 9. | **Fallback-only reach, stated at its real size.** `TryFindResource` returns the themed brush whenever `App.xaml`'s dictionary is loaded, which is every path a user can reach, so in a running app these literals paint nothing. They are still two brand hexes hardcoded in App code, the same defect class as F-085, and a `??` fallback is the shape that survives a palette change unnoticed precisely because nothing ever renders it. Small, real, and worth a row rather than an allow-list entry nobody can trace. | Move the branch into XAML: a `Style` with a `DataTrigger` on `IsHostEnforced` setting `Foreground` to `{DynamicResource CyanBrush}` or `{DynamicResource MagentaBrush}` — the same idiom v1.17 item 3 used to replace the two status converters, so the sheet stops resolving brushes in a model class at all. | clean |
| F-088 | (v1.17 reconciliation) | main window status bar | visual | 2 | 3 | `MainWindow.xaml:1873-1888` — the live-process dot's `Fill` is a literal `<SolidColorBrush Color="#4FE08C" />` inside a `Setter.Value`, with a `DataTrigger` swapping to `#4A5C70` at zero clients. Shipped alongside F-080 in PR #96. Present on `main` at `:1776` before the v1.17 branch was cut, so it is not a regression from that cycle — it is a site that cycle did not know about. **Closed 2026-08-10 by v1.17 item 3a, added at `/build` rather than deferred.** Both literals are gone: the `Ellipse.Style` at `MainWindow.xaml:1871-1895` now sets `Fill` to `{DynamicResource WhiteBrush}` with a single `DataTrigger` on `LiveProcessCount == 0` setting `{DynamicResource MutedTextBrush}`. The arrangement is inverted from the per-row dot — live in the `Setter`, zero in the trigger — because `DataTrigger` matches on equality and the bound value is an `int`, so "zero" is expressible and "any number of clients" is not; inverting it would have cost a converter or a new view-model bool, which is the machinery §5.2 deleted. **The artifact that proves it** is `ThemedStatusColourTests.NoColourLiteralIsWrittenIntoAppXamlOutsideTheAllowList`, a third fact on item 3's fence that scans App **XAML** for literal `#RRGGBB` outside an allow-list. It was written first and watched fail on exactly `MainWindow.xaml:1877` and `:1884`, then watched pass. Its allow-list is the durable part: **a literal is permitted only when an OPEN register row already owns it**, each entry citing the id inline — F-085, F-066, F-079, F-063 and F-089 all appear there, and `App.xaml`'s brush dictionary is the one entry with no finding because it is the governed path's origin rather than an escape from it. That rule is what closes the hole this row is really about: not one dot, but that a sixth site could have hidden the same way. | The fifth status-colour site, and the one `spec.md > §5.3` never enumerated. v1.17 rebound four — row dot, idle chip, row memory chip, compact memory chip — and left this one, so under flatline the status bar still shows brand green while every row above it is achromatic. Both fences are structurally blind to it for different reasons: `ThemedStatusColourTests` walks `*.cs` and this is XAML, `ContrastPairGateTests` reads `Background=` / `Foreground=` attributes and this is a literal nested in a style setter. | Apply §5.3's mapping with the same `Style` + `DataTrigger` + `{DynamicResource}` shape the four rebound sites now use: running takes `WhiteBrush`, zero clients takes `MutedTextBrush`. One element, and it is what makes "no brand hue on the main window under flatline" true of the whole window rather than of the account rows. **Done as written.** The on-screen half is still owed: the dot reading correctly in all four themes, and specifically not green under flatline, is a checkpoint-C2 item for a human, because no test in this project loads a `Window`. | clean |
| F-089 | (v1.17 fence, item 3a) | main window, every account row — batch-selection toggle | visual | 2 | 4 | `App.xaml:53-94` — `SelectionDotStyle`'s `ControlTemplate` holds four un-themed hexes: the outer ring's `Stroke` at `:67`, the checked inner `Fill` at `:72`, the checked ring `Stroke` at `:77`, and the hover ring `Stroke` at `:81`. Three of the four duplicate a brush declared thirty lines above them **in the same file** — two are `CyanBrush`'s value and one is `MutedTextBrush`'s — and the fourth matches no slot in the dictionary at all. Surfaced by item 3a's XAML colour-literal fence, which is also the reason this is a row: that fence permits a literal only where an open finding already owns one, and nothing did. Spec §7 does not name this surface; `AX-4` and `AX-14` both touched the batch dot and were **refuted** as out of the campaign's scope, so neither left a row behind; `F-035` counts this style ("`App.xaml`'s ... 1 keyed control style") but is `clean` and is about the vocabulary not existing, not about what this one holds; `F-068` cites it the same way and its fix direction — three new button styles — would not reach a `ToggleButton` template. No ratio is quoted here on purpose: this cycle's own lesson is that a number nothing reproduces is worse than no number, and nothing measures this pair. | `ThemeService.ApplyTo` replaces brush instances in the resource dictionary; a hex written into a `ControlTemplate` is not one of them, so this toggle keeps brand cyan and a navy-grey ring under every theme including `flatline`, whose field is achromatic. It is on screen once per account, so an eight-alt user sees eight of them. Note what this is **not**: `spec.md > §6.4` checked this control and recorded it clean, because it carries its state in shape — filled versus hollow — and the label beside it reads "In batches" / "Skipped". The state survives with no colour at all. This is a theming defect, not a legibility one, which is why it is sev 2 and not higher. | Bind the four setters to `{DynamicResource}` — `CyanBrush` for the checked fill and stroke, `MutedTextBrush` for the hover stroke, and for the resting ring either `InteractiveEdgeBrush` (it is a `Cursor="Hand"` control, so WCAG 1.4.11 governs its boundary and that token is derived to clear 3:1 under any theme) or `DividerBrush` if the ring is judged decoration. Pick deliberately rather than by nearest-hex: the resting value matches no slot, so there is a real choice being made. Belongs with **F-068**'s shared-style work, which is already rewriting `App.xaml`'s control vocabulary. **Closed 2026-08-10 by v1.17 item 3b**, in the same session that opened it — the builder's call, on the grounds that this was the last always-visible brand hue on the main window and `prd.md > Story 3.1` would otherwise have shipped as a claim the cycle's own flatline captures contradicted. All four setters now bind `{DynamicResource}`: checked fill and checked ring take `CyanBrush`, hover ring takes `WhiteBrush`, resting ring takes `MutedTextBrush`. **The resting ring deviates from this row's own fix direction, and the reason is a fence, not a preference.** `InteractiveEdgeBrush` was written in first and reverted: `InteractiveEdgeBindingTests.TheDerivedEdgeIsNeverBoundToADecorativeSurface` went red on it, because that test classifies `Ellipse` as a shape and its `IsInteractive` escape reads only the element's own attributes — `Cursor="Hand"` sits on the enclosing `Style`, not on the ring. The classifier is narrow, but its own doc says it is tuned tight against precisely the *"just use the visible one everywhere, it looks cleaner"* edit, and widening an accessibility fence to admit the case being argued is that edit. So the ring took `MutedTextBrush` instead. Rest and hover stayed two distinct slots deliberately: the literals they replace were two different greys, and collapsing both to one would have deleted the hover affordance while reading as a faithful port. **Cost, named:** the resting ring is more visible than it was, on every unselected row — the value it now carries is the one the control previously used for hover. That is a visual-density change across a list of eight, and it is a C2 eyes-on item. **The artifact that proves the fix** is the same fence that opened the row: F-089's allow-list entry was retired *first* and `NoColourLiteralIsWrittenIntoAppXamlOutsideTheAllowList` watched go red on all four hexes, then green, and the ceiling dropped 101 → 97. Retiring the entry is the load-bearing half — an allow-list entry that outlives its finding is one layer up from what `NoExemptionOutlivesItsFinding` exists to catch, and leaving it would have let a future literal inherit a closed row's permission. | clean |
| F-090 | (v1.17+ rendered gate, Phase 2) | text and password inputs wherever they sit on a card surface, 8 of 14 call sites | a11y | 3 | 3 | `InteractiveEdgeBrush` is derived by `ContrastGuard.Ensure(theme.Navy, theme.Divider)` ([`ThemeService.cs:211`](../../../src/ROROROblox.App/Theming/ThemeService.cs#L211)) — against **Navy**, and only Navy. It clears WCAG 1.4.11's 3:1 there by the width of the derivation walk: 3.0699 brand, 3.0194 midnight, 3.0364 magenta-heat, 3.0258 flatline. Every built-in ships `RowBg` lighter than `Navy`, so the same edge on a card measures **2.8244 brand, 2.8075 midnight, 2.8149 magenta-heat, 2.2825 flatline** — under the floor in all four, worst in the accessibility theme. Measured as pixels by `RenderedStyleGateTests`, not computed: the numbers are sampled off a `RenderTargetBitmap` of the real control carrying the real style under the real resolved theme. Call sites counted against the tree 2026-08-10 — 14 total, 8 on `RowBgBrush`, 6 on `NavyBrush`, the two remaining matches being the style definitions themselves. | **Three separate checks each had a reason not to see this, which is why it survived.** Phase 1 (`ContrastPairGateTests`) pairs `Background` with `Foreground` and never reads a `BorderBrush`. `InteractiveEdgeBindingTests` polices which *role* the derived edge is bound to, deliberately not what value it lands at against a given surface. And **F-031 is `clean` and records 3.03:1**, which is true — of Navy. A number that is correct about one surface and silently generalised to another is the same defect class the v1.17 reconciliation spent an item removing, and this one was inside a fix rather than inside prose. Not a regression from any cycle: the derivation has been Navy-only since the edge was introduced. | **CLOSED 2026-08-20.** `ContrastGuard.EnsureAgainstAll` walks once and returns only when EVERY surface clears 3:1, and `ThemeService.EdgeSurfacesOf` hands it `[Navy, RowBg]`. Brand's edge moves `#5E6B7C` → `#707B8B` (navy 3.07 → 3.88, card 2.43 → 3.07); midnight and magenta-heat move the same small amount; no theme is repainted beyond the boundary role. `EdgeRemediation.Decide` now takes the surfaces too and uses the WORST — otherwise a divider clearing Navy and failing the card returns `LeaveAlone` and the shortfall is neither derived away nor asked about, which is this finding restated at the consent layer. **THE FINDING WAS INSIDE ITS OWN CONTROL GROUP:** `EdgeRemediationWiringTests` had a fixture constant literally named `GoodDivider = #5E6B7C`, standing for "a theme with nothing wrong with it" — it measures 3.07:1 on Navy and **2.89:1 on that same theme's cards**. The test asserting a compliant theme is never questioned was asserting that a theme which *should* be questioned is not. Fixture corrected, and a sibling test now pins the Navy-passes-card-fails shape. The pin `TheDerivedEdgeIsTunedToNavyAndFallsShortOnACard` failed exactly as designed and was deleted per its own instructions; the eight card cases are inside the general rule now. | clean |
| F-091 | (v1.17 C2 walkthrough) | every plugin's own UI, and every user-authored theme | visual | 3 | 4 | Plugins are separate processes with their own WPF windows, and the plugin contract has **no theme message** — the only colour in `plugin_contract.proto` is a `color_hex` badge override at `:244`. `626labs.ur-task` 0.5.0, whose manifest advertises *"theming that follows the host"*, instead reads `activeThemeId` out of the host's `settings.json` and looks it up in **its own hardcoded palette**. Confirmed by scanning the shipped binary: it carries the ids `brand`, `midnight` and `magenta-heat`, and the hexes `17D4FA`, `0F1F31`, `F22F89`, `3FB8D9`, `1A0F1F`. It carries no `flatline`, no `101010`, no `D4D4D4`. Found by Este walking the v1.17 C2 checkpoint on a running build: three themes changed the plugin's UI and flatline did not. | **CORRECTED 2026-08-10, and the correction is mine.** This row originally claimed *"every user-authored theme is already broken in every plugin."* **That is false.** `ur-task`'s `HostThemeReader` does read user themes: it takes `activeThemeId` from `settings.json`, and when the id is not one of its mirrored built-ins it loads and parses `themes\<id>.json` off disk (`HostThemeReader.cs:158-161`), snake_case slots and all. A user theme works. The claim was asserted from a binary string-scan without reading the plugin's source, and it reached a commit message, a PR body and a dashboard decision before anyone checked. **The real defect is narrower and more interesting:** built-in themes live in host *code* and are never written to disk, so the plugin has no way to read them and must **mirror** them — `BuiltIns = { Brand, Midnight, MagentaHeat }`. Flatline shipped after `ur-task` 0.5.0, so it is absent from the mirror, falls through to a `themes\flatline.json` that does not exist, and lands on Brand. The plugin's own source names the hazard: *"Mirror-drift risk: if RoRoRo's built-ins change, these copies need a matching bump."* **The coupling is worse than a palette copy.** To do this at all the plugin must know the host's settings filename, its camelCase key, the themes folder layout, the per-file snake_case naming policy, and the reader's comment/trailing-comma tolerance — five internal storage details, any of which can change without the plugin knowing. That, not the palette, is what a contract message removes. **CORRECTED AGAIN 2026-08-10, second pass, and this one is mine too.** The cell below this sentence used to repeat the very claim the correction above kills: *"any id absent from the plugin's table falls back to brand, so every user-authored theme is already broken in every plugin."* **Also false, and false for the same reason** — `ResolveActive` checks the mirror first and then falls through to `themes\<id>.json`, so an id absent from the table is exactly the case that DOES work. A corrected row that still carries the original error in a later sentence is worse than an uncorrected one, because the correction reads as verification. **This is not a flatline bug and fixing flatline would hide it.** The real shape is an asymmetry: a user theme is a **file**, so the plugin finds it; a built-in is a **record in host code**, so the plugin can only mirror it. Every built-in added after a plugin ships is therefore invisible to that plugin, silently, and flatline is simply the first one. The "+ Build a theme..." feature works *precisely because* it writes to disk, which is the one path the host does not use for its own themes. It is also a FOURTH copy of the palette, in a different repo, on top of the three hand-synced copies already recorded as an open issue — and the only copy that cannot be fixed by editing this repo. | Push the resolved palette from host to plugin over the existing gRPC channel, so a plugin never needs to know a theme id at all: a theme message in `plugin_contract.proto`, a `ROROROblox.PluginContract` version bump, a host-side push on theme change, and a matching `ur-task` release. That is a contract change to a NuGet external authors consume, and the manifest pins `contractVersion 1.0` / `minHostVersion 1.4.3`, so it wants its own design pass rather than an improvised patch. **CLOSED 2026-08-11, verified on a running build through the production path.** Five theme switches, each observed as a `host pushed a palette` line followed by an `applied 8 brushes` line in `ur-task.log`: brand `#0F1F31`, midnight `#0A1320`, magenta-heat `#1A0F1F`, **flatline `#101010`** and a user-authored theme `#151026`. The stream, not just the connect-time read. Plugin autostarted by the host after the startup gate, one instance, no sideloading. **Flatline reaching a plugin window is the thing this row was filed for and it now does.** Earlier status note, superseded: **STATUS 2026-08-11 — code landed on both legs, row deliberately NOT closed.** RoRoRo `feat/plugin-theme-feed` ships the message, both RPCs, the bus event, the adapter and the author-guide section, proven end-to-end over a real named pipe in `PluginTestHarness` (three tests written first and observed failing with `UNIMPLEMENTED`). `rororo-ur-task` `feat/theme-from-host` deletes the mirror and takes the feed. **What is not verified is the only thing this row is about:** four built-in themes plus a user theme repainting a real Ur Task window. No suite spans two processes, so that check is eyes-on and owed. Also blocking: `ROROROblox.PluginContract 0.8.0` is not on nuget.org, and neither are 0.5.0 through 0.7.0 — the plugin branch built here against a local pack. The row closes when someone has watched flatline reach the plugin, not when the code that should make it happen merged. **Adding `flatline` to the plugin's table is explicitly NOT the fix** — it is a fifth copy and leaves the user-theme case broken. | closed |
| F-092 | (v1.17 C2 walkthrough) | per-row status dot, active state, all themes | a11y | 2 | 3 | Reported by Este on a running v1.17 build: the active dot is hard to read as *on* at a glance. *"I still can't tell that it's on. It does look a little bit brighter, but it's fine."* Measured, it is doing what §5.3 designed it to: active `WhiteBrush` sits at 13.17:1 against the row under flatline and idle `MutedTextBrush` at 4.98:1, and `TriggeredStatusColourGateTests` confirms all four states resolve to distinct values in every built-in. The gap between active and idle specifically is about 2.6:1 — above the mutual-distinctness bar the gate enforces, below what reads instantly on an 8px circle. **Accepted for ship by the builder in the same breath as reporting it.** | This is the C1 trade arriving in practice, not a new defect. `CyanBrush` was rejected for active because it collides with `RowExpiredAccent` at 1.00:1 under flatline, and `WhiteBrush` was the only slot keeping all four states apart in every theme — so the dot is as distinct as the ten-slot contract allows without a new token. What makes it shippable is `SecondaryStatusText`, which states all four states in words directly beside the dot (§6.1), so the dot is a redundant echo rather than the carrier. That argument is exactly why §5.3's honest-limit paragraph declines to assert 3:1 on it. Worth recording that a human still reached for the dot first, because the redundancy argument says they should not have needed to. | Nothing this cycle. If it is revisited, the cheap levers are size and weight rather than colour — an 8px circle is small for a 2.6:1 step — and the expensive one is an eleventh slot for status, which the contract-does-not-grow invariant argues against. Do NOT reach for a second accent: that reopens the collision §5.3 rejected. | open |
| F-093 | (v1.18 fence, item 2) | `Core/IAppSettings.cs` — `DefaultPlaceUrl` | qol | 2 | 1 | `Core/IAppSettings.cs:7` says of `DefaultPlaceUrl` that *"the Preferences dialog allows editing."* It does not, and has not for more than a cycle: the App references the setting **zero** times, while `RobloxLauncher.cs:246,353` calls it "(legacy single-URL setting)" and "vestigial", and `RobloxLauncherTests.cs:362` pins that it must be ignored. Surfaced by `SettingsReachabilityTests` on its first run and exempted there with the reason inline, to keep old `settings.json` files deserializing — the exemption is a decision, not an oversight, which is the fence's whole rule. **UPDATED v1.21.0.0, 2026-08-12 (item 9). PARTLY SHIPPED, AND THIS CYCLE'S SPEC WAS WRONG ABOUT WHY IT COULD NOT GO FURTHER.** `SetDefaultPlaceUrlAsync` is deleted from `IAppSettings`, `AppSettings` and four test doubles; the getter stays so an old `settings.json` still deserializes and `SaveAsync` round-trips the value rather than erasing it. The false comment is replaced. **The spec's §0.2 claimed deleting the field would move legacy users from their saved place to Roblox home. That is false.** `IRobloxLauncher` has two `LaunchAsync` overloads: the legacy string one reads this setting as tier 3, and the typed `LaunchTarget` one resolves favourites then falls through to `LaunchTarget.Home`, ignoring it deliberately. **The app calls only the typed one** — `MainViewModel.cs:1568` is the single call site — and `RobloxLauncherTests.LaunchAsync_TypedApi_NoFavoriteDefault_IgnoresLegacySettingsUrl_ResolvesToHome` pins that the legacy place id must never surface in the launch URI. Those users already land on Home. So this row's *"zero App references"* was right about the app as well as the UI, and the spec's correction was the error. `SettingsReachabilityTests`' own entry, written a cycle earlier, already said it best: *"kept so an old settings.json still deserializes, not so anyone can set it"* — now enforced by the type rather than by convention. **Stays open** for the field's actual deletion, which this row's fix direction asks for and which is now a SMALLER change than believed: it goes together with the unused legacy overload. `JsonOptions` confirmed to set no `UnmappedMemberHandling.Disallow`, and a legacy round-trip test was added. | This is F-023's defect with the sign flipped. F-023 was a setting that persisted and could not be reached; this is a setting that persists, cannot be reached, is not supposed to be reached, and carries documentation claiming it is. The doc is what makes it a finding rather than dead weight: somebody reading `IAppSettings` to find out what Settings owns is told this one is editable, and the next person to act on that adds a control for a value `RobloxLauncher` deliberately ignores. A comment that lies about reachability is exactly the failure mode `RawTheme`'s camelCase comment produced under F-053 — documentation causing the bug it describes. | **Delete the field, do not add a control.** The honest end state is removing `DefaultPlaceUrl` from the settings record and the interface, with the deserialization question answered deliberately rather than by keeping a dead property alive: `System.Text.Json` ignores unknown keys by default, so an old file with the key loads clean without it. If the field is kept for any reason, the doc comment goes first. Deliberately not opened mid-build; recorded in `spec.md > §10` and made a row here so it stops living in a spec. | open |
| F-094 | (v1.18 item 7, spec §5) | Settings — Alerts and memory, `AlertsStatusLine` | visual | 2 | 3 | `PreferencesWindow.xaml:480-481` binds `AlertsStatusLine`'s `Foreground` to `{DynamicResource CyanBrush}` — the accent, the same treatment a success gets. **And it does report failures, which is the condition `spec.md > §10` set for opening this row.** `Core/Discord/AlertStatusLine.Compose` returns *"Your webhook was deleted, so nothing is reaching your phone. Alerts are falling back to desktop only"* and the clan-channel equivalent, and its own doc comment calls that state the loudest thing this line can say because it *"has NO other discovery path"* — Discord never tells the user they deleted the webhook, alerts silently fall back to desktop, and everything looks configured. Three further arms report degraded states in the same cyan: no webhook pasted, desktop-only, and nothing routed at all. | The one message on this page that has no other way of reaching the user is painted in the colour this app uses for "worked". v1.17 established `RowExpiredAccentBrush` plus the triangle as the warning vocabulary across expired rows, idle chips, memory chips and the compat banner; v1.18 added two more speakers of it — `MemorySettingsWarning` under F-023 and `ThemeStatusLine` under F-051 — and both were written as deliberate divergences FROM this line, mirroring its mechanism and refusing its colour. So the page now carries two failure lines in the warning accent and one in the success accent, which is worse than the uniformity it replaced. Noticed while writing spec §5 and left alone on purpose: retro-fitting it was not a v1.18 row, and doing it silently is how a register row goes wrong. | Route the failure arms of `AlertStatusLine.Compose` to `RowExpiredAccentBrush` with the triangle prefix and leave the "Sending to #channel" arms in `CyanBrush`, which means the composer has to return a severity alongside its sentence rather than a bare string — the same shape `ThemeStatusSummary.Line` already ships, so there is a precedent to copy rather than a mechanism to invent. Do NOT repaint the whole line one colour: "sending to your channel" is a success and reads correctly in the accent today. | open |
| F-095 | (v1.19 C2 setup, 2026-08-11) | plugin host, every installed plugin | qol | 4 | 1 | Two folders under the plugins root declaring the same manifest id **disabled plugin support entirely**. `InstalledPluginsLookupAdapter.Refresh()` built its index with `ToDictionary(p => p.Manifest.Id)`, which threw `ArgumentException` on the duplicate key — **one statement below the `catch` whose own comment promises "a corrupt plugins root must not stop the App from launching"**, so the guard was defeated by the next line. The constructor threw, resolving `PluginHostStartupService` threw, and `App` logged *"plugins disabled this session"* at **Debug**. Every plugin gone, no message anywhere a user would look. Found by copying a plugin folder to back it up before an update — an ordinary thing to do — during the v1.19 C2 setup. | *(why-column omitted when this row was written at v1.19 C2; restored empty 2026-08-11 rather than back-filled, because inventing the analysis after the fact is how a register stops being evidence. The column's absence pushed this row's status cell one place left, so it rendered blank in the table and was invisible to any count derived from it.)* | **FIXED 2026-08-11 in the v1.19 branch.** Duplicates now keep the first folder and log a **Warning** naming both paths so the offending copy is findable; the index build moved inside the guard that was supposed to cover it; the `StartPluginHostListener` swallow went Debug → Warning. Three tests in `InstalledPluginsLookupAdapterTests`, verified to fail against the old `ToDictionary` with the exact `ArgumentException` from the live log. **Still open:** the failure is a log line, not a user-visible message — a clan member who hits this still sees plugins silently missing. Surfacing it in the Plugins window is a UI change and its own row. **RULING v1.21.0.0, 2026-08-12 (item 11): a user-visible surface IS owed, and this row SPLITS rather than carrying it.** The log Warning is the right fix for the crash and the wrong answer for the audience — this product ships to a Pet Sim 99 clan of non-technical Windows users, and a log line is not a surface for them. The failure presents as *plugins silently missing*, which is indistinguishable from *plugins never installed*: the same shape as F-038, where History swallows a read failure into its empty-state copy and a broken file reads as "you have never launched anything". **This row goes `clean` on what it actually is — a crash fix, shipped and tested against the live ArgumentException.** `clean` rather than `closed`, because closed-as-ruled means a fix direction was overruled (F-040, F-069) and nothing here was overruled: the crash was fixed and verified, and the half that was not built moves to F-099 under its own id. Split rather than left open because an open row whose evidence describes a FIXED crash is exactly the stale-register failure this file's own reconciliation pass cost a build cycle to learn. | clean |
| F-096 | (v1.19 C2 walk, 2026-08-11) | `rororo-ur-task`, any plugin | qol | 2 | 2 | A second Ur Task instance starts, shows its **full window**, and reports *"Not connected to RoRoRo"* with 0 macros and *"Startup failed: Failed to register hotkeys"* — because the first instance already holds `Ctrl+Shift+R` (win32 1409) and the window it presents is indistinguishable from a genuine connection failure. Observed during the v1.19 C2 setup when a manual launch raced the host's own autostart: two windows, one working and one that looked broken, and the reasonable read was that the app was broken. RoRoRo guards itself with a singleton mutex; the plugin has nothing. **Not caused by the theme feed** — the same race existed before it, and the theme feed is what made it visible, since the working copy was themed and the loser was not. | *(why-column omitted when this row was written at v1.19 C2; restored empty 2026-08-11 rather than back-filled, because inventing the analysis after the fact is how a register stops being evidence. The column's absence pushed this row's status cell one place left, so it rendered blank in the table and was invisible to any count derived from it.)* | Single-instance guard in the plugin: a named mutex on the plugin id, and a second launch either focuses the existing window or exits quietly rather than presenting a UI that claims the host is unreachable. Worth checking whether the plugin author guide should require this of every plugin rather than leaving each author to rediscover it. | open |
| F-097 | (v1.20 item 8, state gate) | `CtaButtonStyle` 23 sites + `WarningButtonStyle` 3 sites, every theme | a11y | 4 | 2 | The shared `AppButtonTemplate` expressed disabled by swapping the label to `MutedTextBrush` and keeping the fill. Correct on a navy fill (6.88 / 4.50 / 6.55 / 6.60) and written when every rank had one; items 3 and 4 added two bright-filled ranks and nobody re-measured. Measured by `ButtonStateGateTests.EveryStatePair_ClearsAa` the first time it composited a state surface: **Cta 1.37 brand, 1.79 midnight, 1.34 magenta-heat, 1.95 flatline; Warning 1.29 / 1.60 / 1.50 / 1.95.** Not latent — `JoinByLinkWindow`, `ExportAccountsWindow` and `ImportAccountsWindow` each OPEN with their primary CTA disabled, so a 1.37:1 label was the first thing a user saw in three separate windows | A state setter written against one kind of fill, inherited unexamined by ranks with the opposite kind. Every gate in the suite measured the RESTING pair and reported it for all four states, so nothing could see it | **FIXED in the same cycle.** `AppFilledButtonTemplate` keeps the dark label and reveals a `WhiteBrush` wash under it, so legibility rises as the control becomes unavailable: **8.96 worst case across all eight pairs.** Dimming toward the background was measured first and fails at every opacity — it squeezes the dark label from the other side | clean |
| F-098 | (v1.20 close-out, cross-repo prompt) | every instrument in the suite that reads markup; `SessionHistoryWindow` Bookmark | qol | 4 | 2 | **A gate that reads markup is only ever evidence about markup.** Five buttons are built in C#: `FriendFollowWindow.xaml.cs:412`, `SessionHistoryWindow.xaml.cs:277`, `SquadLaunchWindow.xaml.cs:205` and `:224`, `CaptionColorPickerWindow.xaml.cs:50`. All five assign colours via `FindResource` and none took a rank. `SquadLaunchWindow`'s Remove has carried a comment since wave 5 saying exactly this — *"built in code, so wave 5's markup sweep never saw it, and neither does any test in that wave, which all parse XAML"* — and v1.20 still shipped a markup-only fence on top of it. The lesson was recorded at one site and not generalised. Verified, not assumed: these were NOT flashing Aero blue (`App.xaml` merges `ui:ControlsDictionary`, so a bare `Button` gets WPF-UI's implicit style, and its state setters are `DynamicResource`) — but those resolve against `ui:ThemesDictionary Theme="Dark"`, which `ThemeService` never touches, so they were correct at rest and frozen in every state in every theme. `SquadLaunchWindow`'s Launch all is the sharpest: cyan-filled AND `IsEnabled = _eligibleAccountCount > 0`, so it is F-097's 1.37:1 defect at a site F-097's fix could not reach | Every instrument here was built by reading the artefact that was easy to read. Nobody asked whether the behaviour asserted actually lives in that artefact — the question that separates a gate from a green number | **PARTLY FIXED.** Three sites migrated; `ButtonRankFenceTests` gained `NoCodeBehindButtonPaintsItself` plus a separate vacuity floor for the .cs walk, both proven failable. **Still open:** History's Bookmark is a cyan LABEL on a navy fill with a cyan edge and no rank provides that, so it is exempt-and-recorded rather than hand-assigned to a near-miss, per `spec.md > §3`. And the general audit is not done — every markup-reading instrument in the suite should be asked the same question **AUDIT RUN 2026-08-11 — see `docs/superpowers/research/2026-08-11-f098-instrument-scope-audit.md`.** Twelve instruments asked the question. Better than feared: **four had already asked it themselves** (`MutedTextFenceTests`, `InteractiveEdgeBindingTests`, `ThemedStatusColourTests`, and `WindowTitleConventionTests` for one of its two rules), three are correctly markup-scoped, two resolve real objects and are not exposed. **Three claim more than they measure**: `ContrastPairGateTests` (+`FlatlineLabGateTests`, inherited) — F-050's prerequisite, left for its own cycle; `UiRoutesSchemaTests` — shape never existence, now documented in the file itself; and `WindowTitleConventionTests`, whose repo-name rule read code-behind while its primary product-name rule did not — **fixed and proven failable**. Two real defects fell out: the caption palette's two copies **had already drifted** (`0x075885` vs `0xFF075985`, so the Settings swatch previewed a colour the title bar never painted), now fixed and guarded by `CaptionPaletteSyncTests`; and this audit's own first draft asserted a gap in `ThemedStatusColourTests` that did not exist, which that gate disproved by failing the build during the write-up. **Still open:** `scripts/*.ps1` and CI job filters were not audited, and the CI-filter case is the one shape this repo has never checked for itself. **SECOND PASS, same day — CI and scripts, the two the first pass left.** **CI is clean on all four doors** the cross-repo case comes through: no `--filter` or narrowing property (both jobs run the whole `.slnx`), no project on disk missing from the solution, no `Compile Remove`, and the single `continue-on-error` is a documented first-release tolerance. The one skipped test carries a reason and a deferral target; no `*Tests.cs` sits outside a test project. Recorded as checked rather than deleted, because an audit that only lists hits teaches the next reader the clean doors were never opened. **Two real findings in the scripts.** `count-button-sites.ps1` **is invoked by no workflow** — only `build-velopack-release.ps1` is — so F-068's *0 un-migrated* is a number somebody typed once and nothing re-checks; its own header advertises a `-Quiet` mode "for CI" that was never wired. `ButtonRankFenceTests` is the actual guard and covers strictly more. And **two committed instruments report different button totals** — 108 from the script, 112 plus 5 code-behind from the fence — with nothing stating the relationship, which rebuilds this cycle's own *55 vs 72 vs 115* trap with better paperwork. Both files now carry the reconciliation. **Still open:** `capture-ui.ps1` (auditing it means driving a live app, so it is a walk not a test pass) and the packaging scripts, which assert about MSIX contents. **RULING v1.21.0.0, 2026-08-12 (item 11): both WAIT, and they wait for a specific event rather than for attention.** Neither remaining item can be audited by reading. `capture-ui.ps1` has to drive a live app, and the packaging scripts assert about MSIX contents, so auditing them means building a Store MSIX purely to audit the script that builds it. Both attach to the **Store submission run** — the screenshot recapture is already scheduled after this cycle and is exactly the walk `capture-ui.ps1` needs. Doing them inside a colour-and-copy cycle that ships no packaging change would be auditing an artifact nobody built. **A TWELFTH INSTRUMENT JOINED THE LIST THIS CYCLE, found by item 5 rather than by the audit.** `ThemedStatusColourTests.AllowedXamlLiteralCeiling` read 95 against a real figure of 62 at this cycle's branch point. v1.20 added `StripXmlComment` — so commented hexes stopped counting — and v1.20's button sweep removed four more from `MainWindow.xaml`, and neither re-derived the constant. The assertion is `allowed <= ceiling`, so both improvements were invisible and the gate stayed green over 33 unaccounted occurrences. That is this row's own thesis from the side it was not watching: the audit asked which instruments claim more than they measure, and this one claimed *less*, which fails just as silently. Re-derived to 58 at item 5. | open |
| F-099 | (v1.21 item 11, split from F-095) | Plugins window, plugin host | qol | 3 | 2 | A plugins root that fails to index tells the user nothing. F-095 fixed the crash — two folders declaring one manifest id no longer disable plugin support — and the recovery is now *keep the first folder, log a Warning naming both paths*. That Warning goes to the log file. **The Plugins window shows the surviving plugins and says nothing about the one that was dropped**, so the user-visible state is identical to having installed it and to never having installed it. Found by copying a plugin folder to back it up before an update, which is an ordinary thing to do, so the duplicate case is not exotic. | A recovered failure that is invisible is a failure the user cannot act on. RoRoRo ships to non-technical Windows users and the recovery instruction — find the duplicate folder and delete it — is only reachable from a log file none of them will open. Same shape as F-038: a swallowed read failure presenting as an ordinary empty state, where the interface's silence is itself the wrong claim. | Surface the dropped duplicate in the Plugins window: name the manifest id, both folder paths, and which copy is live. The Warning already carries all three, so this is a presentation change and not new detection. Do NOT reach for a modal — a plugin the user still has is not an interruption. | **CLOSED 2026-08-20 — and the symptom this row recorded was understated.** Re-measured against the tree: only the HOST deduplicated. The window read the root through `PluginRegistry.ScanAsync` + `MarketplacePlan.Build`, neither of which deduplicates, so two folders drew the plugin TWICE — identical rows, both offering Launch, Revoke and Restart, nothing marking which copy was live, and acting on the wrong one operated on a folder the host had already discarded. So this was NOT only a presentation change, as the fix direction assumed. Keep-the-first moved into `PluginDuplicates`, shared by both readers — the two loops had already diverged, and that divergence WAS the bug. The dropped copies are now named in the window (id, both paths, which is live) in a banner separate from `StatusBanner`, since that one is cleared by the next install while this describes disk state. No modal, per the fix direction. Seven tests; three fail against the old behaviour, verified by reverting. | clean |
| F-100 | (v1.21 window-render spike, 2026-08-12) | `MainViewModel`, and every test that constructs one | qol | 3 | 1 | **Eight `Application.Current?.Dispatcher.Invoke(...)` call sites in `MainViewModel` have never executed in a test.** `:278`, `:2676`, `:2694`, `:2748`, `:2859` and three `Owner = Application.Current.MainWindow` sites. The whole suite runs with `Application.Current` **null** — `ThemedRender`'s header states that as a deliberate invariant — so every one of those null-conditional calls silently no-ops and the delegate inside is never invoked. `RosterSnapshotProjectionTests:203-206` already says this out loud about one of them: *"`Application.Current?.Dispatcher.Invoke(...)` silently no-ops and nothing under test would"* catch a defect inside it. That comment describes one site; the condition is general. Surfaced by the window-render harness, where `Application.Current` is deliberately live: the same calls became real cross-thread marshals, and building more than one `MainViewModel` on the render host wedged it past a 60s budget — presence, activity and session-expiry events fire on background threads and marshal onto a dispatcher that may already be inside a render. **CORRECTED 2026-08-12, same day, by the run that was thought to have hung.** This row was written partly on the claim that building more than one `MainViewModel` on the render host wedges it past its budget. **That is false.** The run had completed in the background: both banner render tests passed, 2 of 2, in one second, and the elapsed time was build and restore rather than rendering. `MainWindow` renders fine in the harness, `BannerPairRenderTests` ships, and v1.21's both-banners-at-once check is retired rather than blocked. **What remains true is the whole of the finding:** the eight call sites are real, `Application.Current` is null across the ordinary suite, and those delegate bodies have still never executed in a test. What was invented was a mechanism to explain a timeout that was never a timeout — the same reach-for-a-story error this cycle corrected in item 4, committed again three commits later, and caught only because a background task reported its result after the conclusion had already been filed. | A null-conditional on process-global state is an off switch the tests are always holding down. The bodies of those delegates — presence application, session-expiry handling, roster projection — are exactly the marshalled UI updates most likely to be wrong, and they are the part no test has run. This is F-098's thesis in a third form: not a gate that reads the wrong artefact, but a gate that runs the code with a global quietly set to the value that skips it. | Give the marshalling a seam — an injectable dispatcher abstraction defaulting to `Application.Current?.Dispatcher` — so tests can supply an immediate one and actually execute those bodies. This is about test COVERAGE of those bodies, not about the render harness: `MainWindow` renders there today and its banner pair is gated. Do NOT construct an `Application` in the ordinary suite to force them on; that reintroduces the process-global hazard `ThemedRender` documents. | **CLOSED 2026-08-20.** The seam is `Core.IUiDispatcher` + `App.Threading.WpfUiDispatcher`; nine marshalling sites go through it, and `MainViewModelMarshalledHandlerTests` exercises the five bodies through their real event handlers. TWO CORRECTIONS THIS ROW NEEDED. (1) The dispatcher was only the OUTER off switch — `FakeRobloxProcessTracker`, `FakeMemoryWatchdog` and `FakeActivityMonitor` declared the driving events as `{ add { } remove { } }`, discarding every subscription at bind time, so fixing the marshal alone would have bought exactly nothing. The bodies were unreachable twice over. (2) RE-MEASURED: the row counted eight sites (five dispatcher + three `Owner = Application.Current.MainWindow`). Today there are nine marshalling sites and TWELVE Owner sites — the Owner count rose by nine while this row sat open. Those are window ownership rather than marshalling and are NOT closed here; split out as F-106. The first test to enter the attach-failed body failed on a `NotImplementedException` in `FakeRobloxUpdateProbe.IsInstallerRunning()`, safe for as long as the double existed because nothing had ever reached the line that calls it. | clean |
| F-101 | (observed by Este 2026-08-12, confirmed by process inspection) | plugin process lifetime; every autostart plugin | qol | 4 | 2 | **Plugin processes outlive the host and accumulate, one per RoRoRo session, forever.** Observed live: **six `626labs.ur-task.exe` processes running while `ROROROblox.App` was not running at all**, holding roughly 950 MB between them. Este saw three earlier the same day, so they had doubled in a working session. Mechanism, verified in the tree rather than inferred: `App.xaml.cs:2075` calls `supervisor.StartAutostart(plugins)` on launch, and **nothing stops them on exit** — there is no shutdown teardown and **no Job Object anywhere in `src/`** (`grep JobObject\|AssignProcessToJobObject\|KILL_ON_JOB_CLOSE` returns nothing), so on Windows a child simply survives its parent, including on crash or a Task Manager kill. **The codebase already knows orphans exist and only sweeps them at the wrong moments.** `PluginProcessSupervisor.StopByInstallDirAsync`'s own doc says *"an orphan — a plugin process that outlived the RoRoRo session that launched it"*, and it kills by image path so orphans cannot hide. Its only callers are `App.xaml.cs:846` (install/update) and `PluginsViewModel.cs:375` (uninstall). So the sweep runs when a plugin's FILES need unlocking, never when a session starts or ends — which is exactly when orphans are created. | This lands on the one resource the product exists to manage. RoRoRo's pitch is running several Roblox clients side by side, it ships a memory watchdog and a launch-headroom modal, and F-082's whole point was warning a user before free memory crosses a reserve. Orphaned plugin processes eat that same headroom silently, and the watchdog counts them as ambient pressure rather than naming them — so the product's own instrument reports the symptom of a defect the product caused. Worst for the audience it ships to: a Pet Sim 99 clan on modest Windows machines, who will experience it as "RoRoRo makes my PC slow" with nothing on screen to explain it. Adjacent to F-095/F-099 in the same way twice over — that pair was duplicate plugin FOLDERS breaking the index, this is duplicate plugin PROCESSES; both are plugin-lifetime robustness, and both were found by a human noticing something odd rather than by any instrument. | Tie child lifetime to the host with a **Job Object** (`CreateJobObject` + `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`, assigned at spawn) — the only mechanism that also survives a host CRASH, which a shutdown hook does not. Belt and braces: also sweep orphans at STARTUP, before `StartAutostart`, reusing the existing image-path scan so a pre-existing pile is cleared on next launch rather than needing a plugin reinstall. Do NOT rely on a `ProcessExit` handler alone; it does not run on kill or crash, which is when this bites hardest. **Verify the fix by process count across a launch/exit/launch cycle, not by reading the teardown path** — reading the teardown path is how it looked correct for two cycles. **PARTLY SHIPPED 2026-08-12, and this row does NOT close.** Both halves of the fix direction are built. `PluginJobObject` creates an unnamed job with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` and `DefaultPluginProcessStarter` assigns every plugin to it immediately after `Process.Start`; the job is a DI singleton so its handle lives exactly as long as the host process, which is what makes the kernel do the killing however the host dies. `PluginProcessSupervisor.SweepOrphans` runs at startup **before** `StartAutostart`, scoped to the plugins root by image path, so a pile made before this shipped is cleared on next launch instead of needing a plugin reinstall. Every failure path degrades to the old behaviour rather than blocking a launch — a plugin that starts unparented is a leak, a plugin that refuses to start because job assignment failed is a broken feature. `PluginOrphanSweepTests` covers the sweep at five clauses including a kill that throws mid-list. **Why it stays open:** the sweep is tested and the JOB OBJECT IS NOT. Testing it means really spawning a process, really killing its parent, and asserting on the OS — none of which this suite does. So the half that covers the crash case, which is the case that made the orphans, is verified by construction and by reading, which is the exact standard this row's own fix direction rejects. **It closes when someone counts processes across a launch / exit / launch cycle on a real machine, and again after a forced kill of the host.** **CLEAN-EXIT CASE VERIFIED ON A REAL MACHINE 2026-08-12**, from a true zero after Este stopped the six. Launched from this branch: log shows `Plugin job object active`, then `job-managed: true` for pid 68648, then on a normal exit `07:39:05.194 ROROROblox exiting (code 0)` followed 110 ms later by `07:39:05.304 Plugin process exited: 626labs.ur-task pid 68648 code 0xFFFFFFFF`. Both absent from the process list afterwards. **The exit code is the evidence, not the timing:** `0xFFFFFFFF` is `TerminateProcess`, so the plugin was KILLED rather than noticing its pipe close and exiting gracefully, which would report 0. Nothing in the app kills it — the absence of any teardown is this row's own finding — so the job is the only candidate. **AND THE SAME LOG EXPLAINS WHERE THE SIX CAME FROM, which was not what this row assumed.** Five pre-fix sessions started a plugin (01:18, 01:26, 01:34, 01:53, 06:59) and **not one has a `Plugin process exited` line** — the orphans, in the log, by pid. But none of those five has a `ROROROblox exiting` line either, and `App.OnExit` logs that on every ordinary shutdown including the tray Quit (`TrayWiring` → `handlers.Quit()` → `Shutdown(0)` → `OnExit`, `App.xaml.cs:2138`). **So those sessions never reached OnExit: they were killed or crashed.** Este's own account matches — he quits from the tray and RoRoRo does stop — and he was right to be surprised, because the pile did not come from the way he closes it. **That relocates the severity onto the untested half.** Pre-fix NO path stopped plugins, so a clean quit leaked too; but the six that actually accumulated came from ABNORMAL termination, which is exactly the case a shutdown hook cannot cover and only the job object can. So the verified case is the one that mattered least, and **the forced-kill case is still unrun**. **FORCED-KILL CASE PROVEN 2026-08-12, and this row CLOSES.** Run deterministically rather than by hand, because a Task Manager tree-kill produces an identical observable and would have proven nothing: `taskkill /PID 55720 /F` with **no `/T`**, so exactly one process was terminated and the plugin was not touched. Session log: `08:13:41 Plugin job object active`, `08:13:42 Plugin process started: pid 59428; job-managed: true`. After the kill, **both processes gone**, and the log carries **neither `ROROROblox exiting` nor `Plugin process exited`** for that session. The absence of both lines is the evidence: `OnExit` never ran, the starter's `Process.Exited` handler never ran, **no application code executed at all** — and the plugin died regardless. Only the kernel can do that, via `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` firing when the host's handle closed. Compare the clean exit at 07:39, where the host outlived its child long enough to log `Plugin process exited ... code 0xFFFFFFFF`: same outcome, opposite amount of code running. **Both paths verified on a real machine, by process count, which is what this row's own fix direction demanded instead of a reading of the teardown path.** The startup sweep is now belt-and-braces rather than the only thing working: it covers a pile made before this shipped and the microseconds between `Process.Start` and job assignment. **Not closed on the tests** — `PluginOrphanSweepTests` only ever covered the sweep, and the job object still has no automated coverage because proving it needs a real spawn, a real parent kill and an assertion about the OS. It is closed on the measurement. | clean |
| F-102 | (Store capture run, 2026-08-12) | `PreferencesWindow` streamer-mode toggle | a11y | 3 | 1 | **Streamer mode cannot be switched on through UI Automation: the checkbox reports `On` and the feature stays off.** `OnStreamerModeToggle` (`PreferencesWindow.xaml.cs:1114`) is wired to **`Click`**, and `TogglePattern.Toggle()` — the only pattern a `CheckBox` exposes — routes through WPF's `ToggleButtonAutomationPeer`, which calls `OnToggle()` and raises `Checked`/`Unchecked`, **never `Click`**. So `IsChecked` flips, UIA reports `ToggleState.On`, and `_mainViewModel.StreamerModeOn` is never assigned. Reopening the page then reloads the box from the view model (`:176`, `:208`) and it silently reverts to `Off`. Observed end to end during the Store capture run: toggled via UIA, read back `On`, captured the account list, and the PNG showed **real Roblox account names**. Driving a genuine click instead — `SetFocus()` then Space — engaged it correctly and the same capture came back with the fake identities. **The handler's own comment is three lines from the answer:** *"Click is raised by ToggleButton.OnClick, which programmatic IsChecked assignment never reaches (that raises Checked/Unchecked instead)"*. It reasons about that split to justify not adding a suppress guard, and does not follow it to the case where the programmatic setter is UIA rather than our own populate. | This is a PRIVACY control that reports engaged while disengaged, which is the worst direction for this particular failure: the whole point of streamer mode is that a user trusts it before going live. Anyone driving it through automation or an assistive-technology path that uses `TogglePattern` gets a checked box and unmasked names. It is narrow — a mouse click or a Space keypress on a focused box both raise `Click` and work correctly — but the affected path is exactly the one a user who cannot easily click is on. Also the reason the tray checkbox exists as a second view of the same source of truth, so the same question is worth asking of it. | Bind `IsChecked` two-way to a view-model property and delete the `Click` handler. That is the idiomatic WPF answer and it is immune to the whole Click-versus-programmatic split, including future automation paths nobody has thought of. The `Checked`/`Unchecked` alternative needs a suppress flag around the populate, which is what `:1116-1124` argues against on the grounds that the flag is held across seven awaits — a binding needs no flag at all. **Verify by toggling through `TogglePattern` and asserting the rendered names change**, not by asserting `ToggleState`, which is the value that lied. | **CLOSED 2026-08-20, verified through the exact path the row describes.** Bound two-way; the `Click` handler is gone. Live: `state BEFORE = Off` -> `Toggle()` -> `state = On` **and `settings.json StreamerMode = True`**, then toggled back to Off/False. **AND THE TRAY HAD IT TOO** — this row said the same question was worth asking of the tray checkbox, and the answer, measured with a real `MenuItemAutomationPeer`, is that `Toggle()` raises **zero** Click events there as well. It was assumed to be fine because its handler reads provider state rather than `IsChecked`; that is defensive in the right way and irrelevant when nothing runs at all. Both surfaces are now bound. `TogglePatternReachesTheHandlerTests` pins the framework behaviour so nobody re-derives it from memory, which is how the original survived a code comment three lines from the answer. | clean |
| F-103 | (observed by Este 2026-08-12, mechanism confirmed in the tree) | `RobloxWindowDecorator`, `RunningRobloxScanner`; every client Roblox relaunches itself | qol | 4 | 4 | **A Roblox client that Roblox itself relaunches — after an update, or any self-restart — is permanently orphaned from RoRoRo: counted as running, attributable to no account, and unlabelled on screen.** Observed live: two clients up, one titled `Roblox - CaptainNoodle` and one titled just `Roblox`. Este identified the cause unprompted — *"that window doesn't have a title because it was after a Roblox update"* — and the tree agrees at every step. `RobloxWindowDecorator` keys its targets by **the pid RoRoRo launched** (`ConcurrentDictionary<int, DecoratedTarget>`), its reapply timer walks only `_targets.Values`, and `ApplyOnce` **prunes** a pid once `HasExited` (`:127-129`). Nothing adopts a pid RoRoRo did not start. So the update's replacement process is never decorated, and its title stays the bare `Roblox` forever. **And the title is the re-attach key**, which is what turns a cosmetic gap into a correctness one: `RunningRobloxScanner` maps a window back to an account by parsing `"Roblox - {name}"` (`:21-22, :97`) and already logs the failure it cannot recover from — `"Untagged Roblox process pid {Pid} title='{Title}'"` at `:102`. The app has been printing the symptom into its own log the whole time. | This lands on the product's entire premise. RoRoRo exists so several accounts can run at once and still be told apart; the window title IS that affordance, and the one event guaranteed to happen to every user — a Roblox update — silently removes it. The consequences compound: the client counts toward the running total and the memory watchdog, but no account row owns it, so the row's Stop cannot reach it and the user cannot tell which alt is in which window. It is also the exact class `CLAUDE.md` says to log as a Roblox-side compatibility event: the behaviour depends on how Roblox restarts itself, which is not our contract to hold. Found by a human noticing an odd window title, like F-095, F-099 and F-101 before it — four plugin- and process-lifetime defects in a row that no instrument in this suite was watching for. | **Adopt untagged clients rather than only decorating ones we launched.** The scanner already enumerates every `RobloxPlayerBeta` and already identifies the untagged ones, so the detection half exists — what is missing is attribution and a re-decorate. Attribution options, cheapest first: correlate a new pid appearing within seconds of a tracked pid exiting (a self-restart is a succession, and the session store knows which account owned the predecessor); failing that, ask. **Whatever is chosen, do not leave the window bare** — an "unknown account" label the user can resolve beats a window that silently claims to be nobody. Verify by killing a decorated client and starting a bare `RobloxPlayerBeta`, not by reading the decorator. | clean |
| F-105 | (found 2026-08-12 while running a base-vs-patch control for F-104) | `src/ROROROblox.Tests/Rendering/WindowRenderHost.cs`, and every `Rendering.*` gate that rides it | infra | 4 | 3 | **The window-render harness is nondeterministic on a fresh checkout, and when it fails it can print a green line.** In a fresh worktree at a clean HEAD, 103 `Rendering.*` tests fail with `System.NotSupportedException: The URI prefix is not recognized`, thrown from `Wpf.Ui.Controls.ContextMenuLoader.AddEditorContextMenuDefaultStyle` resolving a `pack://` resource dictionary on the harness's STA thread. Sometimes it does not fail as tests at all and instead takes down the test host (`Test host process crashed`), aborting the run wherever it happens to be. **Measured across identical `dotnet test` invocations in one worktree: totals of 584, 991, 1058, 1615, 1616 and 1642.** The run simply ends at the crash. **The dangerous shape is the reporting, not the failure:** an aborted run prints `Passed!  - Failed: 0, Passed: 1058` and then `Test Run Aborted.` on the next line. Read the first line alone — which is what a scroll-back, a log tail, or a grep for `Failed:` does — and a half-finished suite looks like a clean pass. **CORRECTION, same day: ~~the primary working tree is the one environment where it is reliable.~~** It is not. Three consecutive full-suite runs in the PRIMARY tree that evening all aborted, with three different totals and three different crash causes: 1637 passed / 0 failed, then 1634 / 1, then 1137 / 61. The row was filed after testing only in a fresh worktree and assuming the tree it was authored in was sound. It was not; nobody had run the full suite to completion there either. **And there is more than one mechanism, which the original row also got wrong by naming only the render harness.** At least two are distinct: **(a)** the `pack://` URI race on the render host's STA thread, above; and **(b)** a leaked `DispatcherTimer` with nothing to do with rendering — `MainViewModel` starts a 30s timer in its constructor whose tick calls `_activityMonitor.GetSnapshot()` (`MainViewModel.cs:320`, the v1.8 idle projection), and `MainViewModelTests`' `FakeActivityMonitor` threw `NotImplementedException` from that member. The view model outlives the test that built it, the timer fires on the dispatcher after the assertions are done, and nothing catches — so it killed the host rather than failing a test. **(b) is fixed** (`77d85c7`, completed in `e4646ed`): a live timer calling `GetSnapshot` is correct behaviour, so the double has to answer it, and returning empty is the fix rather than a workaround. The effect is the whole point of this row — full suite, primary tree, before: **1637 / 1635 / 1198, all aborted**; after: **1643 / 1643 / 1643, all completed**. **Finished properly 2026-08-13, after the release shipped.** Making the tick's callees benign one throwing double at a time took two rounds and only ever held until someone added a fifth collaborator, so `MainViewModel.StopPeriodicRefresh()` now stops the timer and all three test construction sites call it. Not named `Dispose` on purpose: nothing else is torn down and the view model stays usable, which is what the tests need. The precedent was already in the file — `MainViewModelTests.Build` has disposed `RobloxWindowDecorator`'s 1.5s reapply timer for this exact reason, with a comment saying so, since long before any of this. The second timer was simply never noticed. The benign doubles stay as belt and braces. Same total three times running is the first time this suite has been able to say what its own size is. **(a) remains open, but it is NOT what the row said it was, and the severity drops.** ~~Fresh worktrees fail ~103 `Rendering.*` tests on the pack:// race.~~ The worktree was a red herring — a coincidence of when the runs happened. **The actual trigger is a running RoRoRo instance**, and Este called the direction before the evidence did, asking whether the app being open was the variable. **Reproducer, 100% reliable both ways:** start `ROROROblox.App.exe`, run the suite → 103 failed, 98 failed, then a host crash, then 99 / 100 / 100. Kill it, run again → 1643 / 1643 clean, eight consecutive times. `Rendering` tests run ALONE with it up crash after 3 tests, so this is environmental and not a test-ordering race inside the suite. **Ruled out, each by measurement rather than argument:** *CPU load* — three runs under full 16-core saturation, genuinely 3x slower at 34-35s against 12s, all 1643 clean. *The "Roblox is already running" modal* — `RobloxAlreadyRunningWindow` is constructed at exactly one site, `App.xaml.cs:242`, inside `OnStartup`, and the harness never calls `Run()`, which is the only thing that raises it. *Test ordering* — see the isolation run above. *The pack-registration theory* — the obvious reading of `ThemedRender`'s static ctor is that `_ = Application.Current` runs the STATIC ctor while the WebRequest factory is installed by the INSTANCE one, so the type would be inheriting registration by luck from `WindowRenderHost`. **Tested and false.** Routing it through an explicit `WindowRenderHost.EnsureInitialized()` changed nothing: same exception, same line, and `AboutMarkRenderTests` — a WindowRenderHost test, which has a real Application by construction — fails too. The fix was reverted rather than kept as a plausible-looking no-op. **So the mechanism is still unknown.** What is known is that with RoRoRo up, `new Wpf.Ui.Markup.ThemesDictionary()` throws `NotSupportedException: The URI prefix is not recognized` from `WebRequest.Create` inside `PackWebRequest.GetRequest`, meaning the `pack` scheme IS registered and it is the INNER `application://` request that falls through to a real WebRequest. How a separate process changes that is the open question. **CORRECTION 2026-08-13, and this one was wrong in the reassuring direction, which is the worse one.** ~~CI is not affected.~~ It is. What was true is narrow and was over-generalised: the *RoRoRo-running* mechanism does not reach CI, because a runner has no instance running. That got stated as "CI is unaffected", from reasoning about a machine nobody had looked at. **`ci` had been green on `main` through 2026-08-12 04:23 and failed on every run after this cycle's merge.** The cause is the whole-window render harness, which landed 2026-08-12 and was never run on a CI runner before it merged: a single render wedges past the 60s budget having started immediately (`"AboutWindow mark [flatline] @96dpi" ... began rendering after 0.0s of queue wait and then did not finish`), where the same render takes milliseconds on a desktop. A different test drew the short straw each run — AboutMark, then HistoryRow, then BannerPair — because whichever goes first hits the wall, which is exactly why it read as flake. **Process failure worth recording next to it:** PR #111 was opened and merged in the same breath without waiting for its checks, and its CI was already red — on `.gitnexus/meta.json`, which would have been caught before it ever reached `main`. Not a tooling gap. **Taken 2026-08-13:** the nine whole-window gates now carry `[WindowRenderFact]`, which SKIPS with a reason on CI and runs everywhere else. Verified both directions: desktop 1643 passed / 0 skipped, `CI=true GITHUB_ACTIONS=true` 1634 passed / **9 skipped**. Skipped rather than quietly passed on purpose — this row exists because a suite printed `Failed: 0` on a run that had stopped early, and trading one silent green for another would be repeating it with extra steps. `RORORO_FORCE_WINDOW_RENDER=1` runs them on CI anyway, for whoever fixes the harness. **This is a real coverage reduction and the row stays open until the harness works headless.** The remaining local hazard is unchanged: run the suite with the app open and ~100 gate tests fail for reasons that have nothing to do with the code. **Practical guidance until the mechanism is found: close RoRoRo before running the suite.** **Four isolation probes run 2026-08-12, all against the same 108 `Rendering` tests, each with the probe verified alive before AND after the run.** Every one is a negative, which is the point: the suspect list is now short and these do not need repeating. *(1) A plain WPF app* — Application plus Window plus message pump, nothing else: **108/108 pass**. So it is not "any running WPF process". *(2) A WPF-UI app that is not ours* — same WPF-UI 4.3.0, merging `ThemesDictionary` then `ControlsDictionary` in App.xaml's order: **108/108 pass**. So it is not WPF-UI, and not the dictionaries whose loading is where the exception is actually thrown. *(3) RoRoRo with the plugin process gone* — installed plugins moved aside, verified `plugin procs: 0`: **identical crash after 3 tests**. So it is not the plugin host, Kestrel or the named pipe, which had been the leading suspect because the exception surfaces from `WebRequest.Create`. *(4) The Roblox singleton mutex held alone* — a console app holding `Local\ROBLOX_singletonEvent` and sleeping: **108/108 pass**. So it is not the mutex, which is the most distinctive thing the app does. **What remains:** something RoRoRo does at startup that none of WPF, WPF-UI, the plugin host or the mutex accounts for. Untested candidates, roughly in order of how unusual they are: the tray icon (Hardcodet.NotifyIcon.Wpf), WebView2 initialisation, `RobloxWindowDecorator`'s window enumeration and `SetWindowText` calls, Velopack's update check, and the Discord presence service. **The next step is a bisect of App.OnStartup**, disabling subsystems one at a time, rather than more guessing from the outside. **One unexplained regularity worth keeping:** the crash lands after exactly 3 tests every time, across many runs. A deterministic failure point inside a supposedly nondeterministic condition is a clue, and nobody has pulled on it. **On "self-inflicted":** `WindowRenderHost` landed 2026-08-12 and passed when written, which given mechanism (b) only ever meant "passed on a run nobody checked had finished." That much stands. But (a) is older than the harness and is not the harness's doing — `ThemedRender` and its pack URIs predate it, so blaming the render spike for both was two defects wearing one coat. | This is about whether the suite has any authority left. `release.yml` runs `dotnet test` on a fresh CI checkout, which is exactly the condition that reproduces this — so a release tag can fail on ~6% of the suite for reasons that have nothing to do with the change being released, and the fix for that is usually a re-run, which trains everyone to re-run until green. **CORRECTION, same day: the "CI goes green" half of that was wrong and is struck.** ~~If it aborts rather than fails, an incomplete run reports zero failures.~~ `dotnet test` exits **1** on an aborted run — measured, after first measuring it wrong by reading the exit code of a pipeline whose last stage was `tail` instead of the command itself. CI fails loudly and does not go green. The hazard is narrower than filed and entirely human: the console prints `Passed!  - Failed: 0, Passed: 1637` and then `Test Run Aborted.` on the NEXT line, so scroll-back, a log tail, or a grep for `Failed:` reads a suite that stopped early as a clean pass. That is a real trap, but it is not a green pipeline. **Three of the gates the keystone names by name live in this namespace** — `ContrastPairGateTests`, `ButtonStateGateTests`, `ButtonRankFenceTests` — and `ButtonStateGateTests` alone accounts for 79-81 of the failures. Those are the gates that stop a button repainting itself or a theme shipping below AA. A gate that cannot be relied on to run is not a gate, it is a comment. Note the shape of the discovery too: this surfaced only because F-104's verification ran a base-vs-patch control in a fresh worktree instead of trusting the primary tree. Nothing in the suite was watching for it, same as F-095, F-099, F-101 and F-103. | **Register the `pack://` scheme before any WPF-UI control resolves a theme dictionary.** The scheme is registered as a side effect of `Application`'s static initialisation; WPF-UI's `ContextMenuLoader` runs via the dispatcher and can win the race, which is why it is intermittent rather than constant. Force it deterministically on the render thread — touch `PackUriHelper` (or construct and pump the `Application`) and let the dispatcher drain BEFORE the first window is loaded — then re-check whether the abort path disappears with it. **Before chasing (a), re-read how (b) was found: nobody looked at the line under the green one.** The suite had been aborting for an unknown stretch, and every check of it stopped at `Failed: 0`. Any fix here that is verified by reading a pass line repeats the defect. **Verify in a FRESH worktree, never in the primary working tree**, which is where it passes and is the reason this shipped. The bar is N consecutive runs returning an identical total, not a green line — a green line is the symptom. If it cannot be made deterministic, the honest alternative is to mark the render gates explicitly and fail the run when they are skipped, so the suite never again reports a subset as a pass. | open |
| F-106 | (split from F-100 on re-measurement, 2026-08-20) | `MainViewModel`, twelve window-opening methods | qol | 2 | 1 | **Twelve `Owner = Application.Current.MainWindow` sites**, up from the three F-100 counted at audit. Unlike the marshalling sites — closed by F-100's `IUiDispatcher` — these are not null-conditional: `Application.Current.MainWindow` with no `?.` inside methods that construct a real `Window`. They cannot execute in the ordinary suite at all, and not because a global is set wrong: constructing the window is the untestable part. Counted honestly here rather than folded into F-100's close-out, whose fix direction covered marshalling only. | The same blind spot F-100 named, in the half its fix does not reach. Twelve methods that open windows have no test that proves the window is even constructible, and the count grew by nine while the row that mentioned them was open — new sites keep landing in the shape nothing can cover. Low severity because the failure mode is a crash on a user action rather than silent wrong state, and low visibility because these paths are exercised by hand constantly. | Wants a window-factory seam — an `IWindowHost`-shaped abstraction the view model asks for a window instead of newing one — so the open-a-window methods can be asserted without a WPF host. `[new-mechanism]`: nothing in the app consumes such an abstraction today, and it is NOT a re-style. Prices separately; do not batch it with a marshalling or copy wave. | open |
| F-107 | (found by misreading the guard's own output, 2026-08-20) | `RenderEnvironment`, and the shape of a failing run | qol | 2 | 2 | **When the render guard trips it produces ~100 identical failures, and the top line reads `Failed: 103`.** That looks like a code regression. It was read as one: a full-suite run was attributed in writing to a stale-DLL link from a locked build, when the actual cause was a running RoRoRo instance and the guard firing correctly — its own header records that exact signature ("App running: 103 failed, 98 failed, host crash") and had since 2026-08-12. The actionable message was present and correct, inside failure text nobody opened. Now applies to the Roblox-client condition too, which fails 81 at once. | The guard converts an unreadable failure into a readable one and then buries the readable one under a hundred copies. Same failure shape as F-099 (a recovery that only speaks to a log file) and F-103 (a symptom printing at Debug): the answer is right and lands where the reader is not. This is the instruments family's thesis turned on an instrument — a check is only as good as the place its answer lands. Low severity because the information IS there and the fix is one second once understood; the cost is diagnosis time and, demonstrably, wrong written conclusions. | Make a dirty environment arrive as ONE loud failure rather than a hundred. `[new-mechanism]`: xUnit 2.9.3 has no built-in `IAssemblyFixture`, so this needs a package, a custom runner hook, or a pre-test step outside the assembly — price it accordingly and do NOT batch it with a re-style. Cheaper half-measures worth weighing first: a single console banner on first detection, or a dedicated always-first guard test. | open |
| F-108 | (observed 2026-08-20; **first written up wrong, corrected the same hour under challenge**) | `App.OnStartup` startup gate, and what startup writes to the log | qol | 2 | 2 | **Startup logs nothing when it pauses on a modal, and nothing when it exits — so a startup waiting for a human is byte-identical in the log to one that died.** Six `ROROROblox starting` entries 11:06-11:13, ~80s apart, none reaching the lines a completed startup writes (job object, Discord, `Startup mutex:`, autostart); the seventh, the first with no leftover client, completes. **This row originally claimed that was six failures to start. That claim was wrong and is retained here rather than deleted.** `LeftoverProcessesWindow.ShowDialog()` blocks `OnStartup` in a nested message loop BY DESIGN — `App.xaml.cs:211` says so in as many words — so silence after `PluginHost listening` is exactly what waiting looks like. And `grep -ci 'shutting down|OnExit'` over the whole log returns **0**, so the absence of exit lines proves nothing either. Two silences that mean opposite things, indistinguishable. The user, who has answered this dialog many times, challenged the claim immediately and was right: it continues when you continue. What is still not established is why none of the six logged completion. | An operator cannot tell a paused startup from a dead one, and neither could I — I read six pauses as six failures and filed it sev 4 on that reading. That is the cost, and it is a diagnosis cost, not a user-facing one: the app is doing the right thing and saying nothing about it. Same family as F-107 (a correct answer landing where nobody reads it) and F-099 (a recovery only a log file hears), except here the answer is not written down at all. | Log the pause and the exit. One INF line before each blocking modal naming which gate opened it, one on `OnExit`. Both are one-liners and would have made the seven minutes above self-explanatory. Do NOT change the modal's behaviour — it blocks by design and the user confirms it works. | **CLOSED 2026-08-20, verified live.** Both gate modals now log a paused line before `ShowDialog` and a resumed line after, naming the answer; the deliberate Quit logs `Startup abandoned at the gate`. Proven by making it happen rather than by reading: `12:46:27 Startup paused… / 12:46:35 Startup resumed: answered "Quit" — proceed=false / Startup abandoned at the gate / ROROROblox exiting (code 0)`, and on the other path a 40-second gap between paused and resumed that is now legibly a human-wait. **A correction inside a correction:** this row's own text claimed the app logs nothing on exit. It does — `App.xaml.cs:2160`, `ROROROblox exiting (code {Code})` — and the grep that 'proved' otherwise searched for `shutting down|OnExit`, neither of which appears in that string. The line fired correctly in the verification above. What remains true and unexplained is narrower: **22 starts and 0 exits across 2026-08-20**, meaning no instance that day reached `OnExit`. Several were force-killed during this session's own testing, which accounts for some. The rest is not established, and is now diagnosable rather than invisible — a startup that dies at the dialog leaves a paused line with no resumed line, which is exactly the fingerprint that was missing. | clean |
| F-109 | (user-reported 2026-08-20: "Roblox keeps opening in full screen… it didn't need to do it before") | `RobloxInstanceStopper.KillAndWaitAll`, and every caller of Stop-all | qol | 3 | 4 | **Stop-all hard-kills Roblox, so Roblox never saves its own settings.** `KillAndWaitAll` calls `_killByPid(pid)` with no `CloseMainWindow` first (`RobloxInstanceStopper.cs:92`), and it backs three ordinary paths: the tray/toolbar **Stop all**, the LEFTOVER dialog's clean-up, and the BLOCKED modal's **"Close Roblox for me."** **CORRECTED 2026-08-20 by measurement — the per-account Stop IS affected.** This row originally cleared it on a reading of `MainViewModel.StopAccount:2620`. Live test: the first click does nothing at all, the second click force-kills within 2.2s. So Stop reaches the same `Kill` this row is about, just one click later. See F-111. A terminated process runs no exit path, and Roblox persists `%LOCALAPPDATA%\Roblox\GlobalBasicSettings_<N>.xml` on exit — the file holding `Fullscreen`, `StartMaximized`, `StartScreenSize`, `GraphicsQualityLevel`, `MasterVolume`, `MouseSensitivity`, `CameraMode` and ~60 more. So every Stop-all silently discards whatever the player changed that session. Confirmed on this machine: `<bool name="Fullscreen">true</bool>` with the file untouched since 11:19 despite repeated windowed-mode changes. **Measured vs inferred, stated honestly:** the kill-without-close and the graceful per-account path are both read directly from source; that a killed process cannot persist is inference from Windows `TerminateProcess` semantics, not a measurement — it is one experiment away (set windowed, Stop all, relaunch). Note the reporter's own instinct was that RoRoRo was involved, and it is, though today's specific instances were an agent's `Stop-Process -Force` during testing rather than the app. | The user does not experience this as RoRoRo doing something. They experience Roblox forgetting their settings, and they blame Roblox — which is exactly why it went unreported for so long and arrived as an aside. RoRoRo ships to non-technical players who will not connect "I pressed Stop all in the manager" to "my graphics quality reset", and fullscreen is merely the most VISIBLE of ~60 lost settings. This is also a trust issue for a tool whose pitch is that it sits politely beside Roblox and only holds a mutex. | Graceful-first with a short bounded timeout, then escalate to kill — the shape `StopAccount` already uses, applied to the bulk paths. **The tension is real and must not be waved away:** `KillAndWaitAll` is deliberately fast because its callers re-acquire the singleton mutex immediately after it returns, and the code comment says so; a graceful close is slower and an in-game client may ignore `WM_CLOSE` entirely. So the fix is a timeout budget, not a substitution, and the budget belongs to whoever owns the recovery UX. Verify by BEHAVIOUR: change a Roblox setting, Stop all, relaunch, read the file. |  **EXPERIMENT RUN 2026-08-20 — it PROVED one half and REFUTED the proposed fix.** (a) The file governs the behaviour, A/B measured on a 3440x1440 display: `Fullscreen=false` -> rect (23,27) 3440x1440 with `WS_CAPTION`+`WS_THICKFRAME` = a real window; `Fullscreen=true` -> rect (0,0), no caption, no border = fullscreen. (b) A hard kill produces NO write — hash and mtime identical across launch and kill. (c) **`CloseMainWindow()` DOES NOT CLOSE ROBLOX.** It returned `true` — the message posts — and the process was still alive 15 seconds later and had to be killed. So *graceful-first-then-escalate*, the fix direction this row shipped with, would add latency and then kill anyway. **That direction is withdrawn as unworkable, by measurement.** It also means `MainViewModel.StopAccount`'s per-account Stop is only nominally graceful: `RequestClose` returns true, the guard `if (!RequestClose)` therefore never escalates, and the client is left running — a separate defect this experiment surfaced and which explains nothing about fullscreen but plenty about Stop. (d) **PREMISE PROVEN 2026-08-20 13:47, by a user closing Roblox normally while the file was watched at 0.8s resolution.** The on-screen window was 816x638 at (882,255) and the file said 3440x1440 at (23,27) — mid-session Roblox had NOT persisted geometry. On close the file changed within one second and now reads `StartScreenPosition X=882 Y=255`, `StartScreenSize X=816 Y=638`: the exact on-screen values. **Roblox persists window state on clean exit and not before**, so a hard kill loses it, and the chain is closed end to end — the flag governs the mode (A/B, window styles), the exit writes it (measured), the kill does not (measured), and Stop-all kills (source). Superseded note: no clean Roblox exit had been observed — WM_CLOSE was ignored in every arm — so "the kill loses your settings" remains inference. Counter-evidence worth weighing: the file's mtime was 11:19:12 while a client was RUNNING, which suggests Roblox may write on change rather than only on exit; if so this row's premise is wrong and the fullscreen cause is elsewhere. | (verdict unchanged above) | **REVISED.** Do not implement graceful-first — measured, it does not close Roblox. Establish (d) first: have a human change a setting, watch whether the file changes immediately, on exit, or never. That single observation decides whether this row is a real defect or a mis-attribution. | **CLOSED 2026-08-20, verified live in-game.** Two halves. The real fix: Stop now sends the close Roblox actually acts on, so a confirmed exit persists settings the ordinary way. The fallback, for a forced stop: `ClientWindowState` reads rect and fullscreen BEFORE the kill and `WriteWindowStateAsync` puts them back. Measured — window moved to 1120x760 at (260,180), ONE Stop click, and the file came back reading exactly that. Fullscreen is decided by the title bar rather than by comparing rect to screen: a windowed client restored to a screen-sized rect is still a window, and the naive comparison would have recorded the wrong answer. **Stated limit:** anything changed inside Roblox's own settings menu is invisible from outside the process and is still lost when we force. | clean |
| F-110 | (observed 2026-08-20 while proving F-109; explains the report that started that thread) | `StartupGate`, `LeftoverProcessesWindow`, and what Roblox leaves behind | qol | 3 | 4 | **Closing Roblox normally leaves a windowless `RobloxPlayerBeta` running, and RoRoRo counts it as a leftover.** Measured: the user closed an in-game client (pid 34820, tracked, exited cleanly at 13:47:15) and pid 62832 started at 13:47:12 with **no window title at all** and was still alive 2 minutes later at 177 MB. RoRoRo's tracker never associated it with an account and logged nothing about it — `OrphanedClientSweeper` deliberately excludes empty-title clients, so F-103's sweep does not see it either. But `StartupGate` counts exactly this as a windowless leftover, which is the input to the LEFTOVER dialog. **So the dialog fires on the ORDINARY path**: play, close, reopen RoRoRo, get a modal. That is the "it popped up unexpectedly" report that opened this whole thread, and it was unexpected because it is not exceptional — it is what every normal session leaves behind. Mechanism for the spawn is NOT established: it appeared one second before the tracked client's exit, consistent with Roblox returning to its own app/home on leaving a game, but that is unverified. | A confirmation dialog that fires on the common path is not a confirmation, it is a speed bump, and users learn to dismiss it without reading — which is measurably what happened here across six launches. That also degrades the BLOCKED modal beside it, which is the one that genuinely matters, because both are trained on the same reflex. And the windowless client is invisible to every instrument RoRoRo has: not tracked, not swept, not logged — only counted, at startup, in a dialog. | Establish first whether the windowless process is Roblox's normal post-session state. If it is, the LEFTOVER dialog should not treat it as noteworthy at all — a leftover that happens every single time is the baseline, not an event — and `StopWindowless` already exists to clear it silently. Do NOT weaken the BLOCKED path, which is a different verdict and genuinely rare. | **CLOSED 2026-08-20, and the row's first instruction — establish whether this is normal post-session state — was carried out before anything was built.** It is. Controlled run: baseline zero clients, one launched through RoRoRo and stopped cleanly, and a fresh `RobloxPlayerBeta` appears at +2s with `hwnd=0`, no title, 122 MB, still alive 25s later and 164 MB by the time it was cleared. Deterministic, every clean stop. So a windowless-only leftover is now cleared WITHOUT a dialog, via the `ClearLeftoverStrays` path that already existed and was only reachable from the dialog itself. A windowed leftover still asks, unchanged. Decision is pure (`LeftoverStartupDecision`, beside `BlockedStartupDecision`, 6 tests); the wiring is in `OnStartup` and so was verified by behaviour: `17:01:44 1 windowless leftover -> clearing without asking -> cleared 1 -> mutex acquired`, no pause, 400ms, against a real stray from a real stop. **One correction to this row's own account:** the six modals that prompted it were `0 windowless + 1 windowed` — an agent's test client left running — not this. Today's 33 startups: 24 clean, 5 windowed-leftover, 2 windowless, 2 blocked. This removes a recurring interruption; it is not the whole of what was seen. | clean |
| F-111 | (live test 2026-08-20 14:14, in-game client, Pet Sim 99) | `MainViewModel.StopAccount`, `RobloxProcessTracker.RequestClose` | qol | 4 | 5 | **The Stop button does nothing on the first click. It takes two.** Measured against a real in-game client (pid 16632, 2.3 GB resident, decorated and tracked): click one at 14:14:18 logged `StopAccount` and the client was still running, still titled, 30+ seconds later. Click two at 14:15:21 killed it — exit at 14:15:23. Cause: `RequestClose` returns `Process.CloseMainWindow()`, which reports **whether the message was POSTED, not whether the window closed**. Roblox does not exit on `WM_CLOSE`, so the first click returns `true`, the guard `if (!RequestClose(…)) Kill(…)` never fires, and nothing happens. The second click returns `false` — main window gone or disabled — and escalates. The method's own doc comment says "Falls back to Kill if a second click arrives while still tracking", which is accidentally accurate about the OUTCOME while describing no mechanism that exists: `ExpectClose` only stamps a timestamp for alert suppression. Mechanism for the second call returning false is NOT established (a Roblox leave-game prompt disabling the main window would explain it; unverified). | The primary control on every running row silently does nothing when pressed. There is no busy state, no "asking Roblox to close", no error — the row just sits there, so the honest user response is to press it again, which is the only thing that works. And the thing that finally works is a hard kill, which is F-109: it discards the session's Roblox settings. So the two findings compound — Stop appears broken, and the workaround for it being broken is the behaviour that loses your settings. Highest visibility in the register: every user, every stop, every session. | Do not trust `CloseMainWindow`'s return value for anything; it answers a different question. Ask whether the process actually exited: post the close, wait a bounded moment, re-check `HasExited`, escalate if it is still alive. That also gives the row something to say while waiting. Verify by BEHAVIOUR against an in-game client — a bare `RobloxPlayerBeta` behaves differently and a test written against one will pass while the button stays broken. | **CLOSED 2026-08-20, verified live in-game.** `RequestClose` now sends `WM_SYSCOMMAND`/`SC_CLOSE` instead of `WM_CLOSE`, and `ClientStopSequence` asks `HasExited` on a one-second tick rather than trusting a return value that answers a different question. **Improved again the same day, on the user's observation that double-clicking the X closes Roblox without engaging its dialog at all** — likewise right-click -> close all windows, once to raise the prompts and once to close them. Measured: `SC_CLOSE` at 15:34:24 left the client alive, `SC_CLOSE` at 15:34:28 exited it, and the settings file came back holding the on-screen geometry written by Roblox itself. So the sequence asks a second time after two seconds, and the ordinary stop is now a CLEAN exit: `15:43:11 StopAccount (pid 56156)` then `15:43:16 finished: "ClosedItself"` — six seconds, no kill, nothing lost, and the write-back never ran. Exactly twice, never more; a test pins that. The countdown and the forced path remain as the fallback for a client that ignores both asks. The row counts down through those ten seconds, because the wait belongs to a Roblox dialog this process cannot see. Opt-out shipped as "Stop clients immediately, without waiting" in Settings, and auto still asks first so a free clean exit is never thrown away. Eight unit tests; one caught a real defect in my own countdown, which emitted 0 twice against a contract saying exactly once. | clean |
| F-112 | (split from F-054 on re-measurement, 2026-08-20) | app-wide keyboard shortcuts | qol | 2 | 2 | **RoRoRo has no keyboard shortcuts at all.** `grep -c 'InputBindings'` across every XAML returns 0, and `KeyBinding` and `Gesture` likewise. Esc dismissal is now universal (F-054), but that is WPF's built-in `IsCancel` behaviour, not a shortcut system: there is no way to reach Launch, Stop, Settings, or the Tools menu without a mouse. | Split from F-054 rather than closed with it, because F-054's measurable claim — four windows with no Esc path — is fixed and this one is not, and folding them would have recorded a keyboard-navigable app that does not exist. Low severity because every action has a visible control and WPF gives tab-order navigation for free; it is a floor issue, not a blocker. | `[new-mechanism]`: an app-level InputBindings vocabulary with a discoverable list, which nothing in the app consumes today. Prices separately and must not be batched into a re-style wave. Pairs naturally with the wave 11 accessibility floor (F-044/F-045/F-052/F-072/F-073) rather than with dialog conventions. | open |
| F-113 | (found by wave 13's own capture, 2026-08-20) | `About/AboutWindow.xaml` | visual | 3 | 3 | `Height="460"` with `ResizeMode="NoResize"` and content that no longer fitted it. The capture taken to verify F-039 shows the DPAPI paragraph cut mid-first-line and the trademark disclaimer absent entirely; raising to 500 recovered the DPAPI paragraph and revealed the disclaimer had been clipped too — "…is not affiliated with, endorsed by, or" with "sponsored by Roblox Corporation." below the fold. | Wave 13 added one 11px line to the Links list and pushed the window over, so the DPAPI clip is this wave's. The disclaimer clip is NOT: at 460 that text rendered below the bottom edge and had done for as long as the current content has, on a product listed in the Microsoft Store. A fixed-height no-resize window has no way to report that it has outgrown itself — nothing throws, nothing logs, and every render test measures pixels inside the frame rather than content past it. | **CLOSED 2026-08-20, and NOT by the number.** The first fix raised the height 460 → 540 and the window rendered whole — then Este photographed the clipped panel from an instance still running the old build, which is the right question asked by accident: a height is a guess about font metrics, DPI and how one particular sentence happens to wrap, and the next line of copy re-breaks it. About now carries `SizeToContent="Height"` with every grid row `Auto`, so the frame asks the content instead. There is no height left to be wrong. This is the app's own existing pattern, not an invention — `EdgeRemediationWindow` and `LaunchHeadroomWindow` already do it. The star row was half the defect and is named here because it is the part that made the clipping silent: a `*` row absorbs the shortfall by giving its child less than it asked for, so the layout succeeds and the text disappears. Verified by capture. **The class is measured, not fixed — see F-115.** | clean |
| F-114 | (found by wave 13 while fixing F-061, 2026-08-20) | Add a Roblox account (`CookieCapture/CookieCaptureWindow.xaml`) | consistency | 2 | 3 | `grep -c DynamicResource` returns **0** and there are **14 hardcoded hex literals** across 14 lines: `#0F1F31` ×3, `#FFFFFF` ×4, `#17D4FA` ×3, `#15263A` ×2, `#5CE6FF`, `#F22F89`. The window binds no theme brush at all. | The one surface every new user meets — it is where a first account is added — renders the brand palette on a field the user may have themed to something else entirely, and under `flatline` it is the only window in the app that does not change. Not covered by F-066: that row narrowed itself to "in-scope residue" and counted this file in the out-of-scope modal bucket, then closed clean against the narrowed scope. Correct at the time; this window came into scope when F-061 put its copy on the table. | **CLOSED 2026-08-20**, one day after it was opened. All 14 literals bound; `grep -c DynamicResource` goes 0 → 14. Two calls worth stating because both had a plausible nearest-match that would have been wrong: `#5CE6FF` was brighter than any slot holds and takes `CyanBrush` rather than an eleventh slot, since "brighter than the theme's cyan" is not something a user theme can promise; and `#F22F89` on the rejection hint takes `RowExpiredAccentBrush`, not `MagentaBrush` — that line is a FAILURE message and the expired accent is the app's failure colour. It is byte-identical in brand, which is exactly why binding it to the brand duo would have looked right and meant nothing. **This closure emptied an allow-list entry that had already outlived its row by eight days:** `ThemedStatusColourTests` permitted this file's literals citing F-066, which went clean on 2026-08-12 against a scope narrowed to exclude it. Entry retired, ceiling re-derived 36 → 22. | clean |
| F-115 | (split from F-113 on measurement, 2026-08-20) | 12 windows across `Modals/`, `JoinByLink`, `Plugins`, `Preferences` | visual | 2 | 2 | `ResizeMode="NoResize"` plus a fixed `Height` and no `SizeToContent`, counted 2026-08-20: `JoinByLinkWindow` 360, `DpapiCorruptWindow` 320, `JoinRequestWindow` 220, `LeftoverProcessesWindow` 240, `RenameWindow` 220, `RobloxAlreadyRunningWindow` 440, `RobloxNotInstalledWindow` 320, `StopAllConfirmWindow` 230, `WebView2NotInstalledWindow` 280, `ConsentSheet` 560, `CaptionColorPickerWindow` 320. Two windows already do the right thing — `EdgeRemediationWindow` and `LaunchHeadroomWindow` both use `SizeToContent="Height"`. | F-113 proves this is not theoretical: About clipped a trademark disclaimer mid-sentence for as long as its current copy existed, and nothing caught it — no exception, no log line, and no render gate, because they all measure pixels inside the frame rather than content past its bottom edge. Every window in this list holds wrapping text, and several hold text whose length depends on runtime data (a process list, an account name, an exception message), which is the case a fixed height cannot be checked against at all. This is NOT a claim that all twelve clip today; it is a claim that none of them can report it if they do. | **CLOSED 2026-08-21**, the day after it was opened. All eleven converted to `SizeToContent="Height"` with `Auto` rows, joining About (F-113) and the two windows that already did it. `ConsentSheet` is the one that is not like the others and is treated that way: row 3 holds a real scroller whose length is the plugin's manifest, so it cannot become `Auto` and grow without bound — but a star row under `SizeToContent` collapses to its minimum and would have hidden the list entirely, so the cap moved onto the Border. A short manifest now gets a compact sheet instead of 560px of empty. **TWO instruments, because the condition and the consequence are different questions.** `WindowSizingFenceTests` refuses a `NoResize` window carrying a fixed `Height` without `SizeToContent` — markup-only, runs on CI. `WindowContentFitsTests` is the instrument F-113 said did not exist: it measures how tall the content WANTS to be against the frame it gets. Pointed at About's shipped 460px it reports *"content wants 488.6px and the frame gives it 460.0px, so 28.6px of it is not drawn"* — the two lines of trademark disclaimer, quantified for the first time. Both proved failable. The fence deliberately stops at height and does not ban star rows: ConsentSheet is correct BECAUSE it kept one and capped it elsewhere, and a gate banning them would ban the window that thought about it. | clean |
| F-116 | (observed during wave 14, 2026-08-20) | `src/ROROROblox.Tests/AppStorageDefenderTests.cs` | infra | 2 | 2 | `Drift_RestampsBackToLaunchedIdentity` failed twice in roughly eight full-suite runs and passed every time in isolation (3 consecutive) and on immediate re-run. **RE-MEASURED SAME DAY, 2026-08-21, AND IT IS WORSE THAN THIS ROW SAID: it is four tests, not one, and one of them failed CI.** `RobloxProcessTrackerTests.InstallerRunning_ButRpbNeverAppears_BoundedByExtendedCap_ThenFails` failed on PR #122's x64 runner — *"Extension was not bounded — ran 2854ms, well past the 600ms cap"* — while the **arm64 runner passed the identical commit** and the test passes locally. Add the two `FpsCapSettlerTests` intermittents (`PostWriteQuietWait_…`, `PermanentlyBusyFile_…`), both of which also surfaced during wave 14. **MEASURED UNDER REAL GAME LOAD, 2026-08-21, and the first answer was wrong.** Este asked whether playing another game affects the suite. Three consecutive full runs with Palworld resident (4.3GB, machine at 71-76% CPU) came back 1782/1782 in 9-10s — identical to idle — and that was reported as "not touching it". **The fourth run failed three `FpsCapSettlerTests` and took 1m32s.** Same load, same commit. So load is A factor and not a deterministic one, which is exactly what an intermittent is, and three clean runs is not a sample. **The 10x duration jump is the useful part: a diagnostic signature nobody had named.** These tests burn their full ~30s timeout when they fail, so three failures dominate the run — a suite that normally finishes in 9 seconds taking a minute and a half IS the tell, visible before anyone reads a single line of output. Worth pairing with F-107, which is about the shape of a failing run: duration is the cheapest signal in it and nothing currently reads it. | Not one flaky test — a class, and one that now blocks merges rather than just annoying a local run. **Two distinct sub-shapes, and they do not have the same fix.** (1) WAIT-FOR-AN-EFFECT: a `FileSystemWatcher` notification or a settled write that must arrive inside a window. (2) ASSERT-A-BOUND: a deadline the code must not exceed — the tracker's 600ms extension cap, the settler's overall budget. The second is the one that matters here, because **you cannot poll your way out of "must not exceed"**: the assertion IS the wall clock, so on a loaded runner the test measures the runner. Nothing in wave 14 touches any of it; the CI failure is a shared agent under load. The real cost is not the red build — this repo has already spent a wave chasing a "regression" that was a guard firing correctly, and an intermittent teaches everyone to press re-run instead of read. | Split by sub-shape rather than raising any timeout. **Wait-for-an-effect** takes condition-based polling on the observable effect (the `condition-based-waiting` note in the systematic-debugging skill) — a poll surfaces load sensitivity where a longer timeout hides it. **Assert-a-bound** takes an injected clock or budget, so the test asserts the DECISION ("it stopped extending after N attempts") instead of the elapsed milliseconds; that is the only version that is true on any machine. Pairs with F-107, which is about the shape of a failing run: a suite whose failures are sometimes real and sometimes not is what makes a hundred identical failures unreadable. | open |
| F-117 | (measured while closing F-114, 2026-08-20) | `ThemedStatusColourTests.XamlAllowList`, the `Modals/` entry | infra | 2 | 1 | That entry cites F-079 and F-066 and describes six windows "that opt out of theming and render brand colours on a themed field". Both rows are `clean`. The folder now holds **one** colour literal in total: `Foreground="#F1B232"` at `RobloxAlreadyRunningWindow.xaml:65`. Counted 2026-08-20 while re-deriving the ceiling after F-114. | An allow-list entry that outlives the finding it cites is the exact defect `NoExemptionOutlivesItsFinding` exists to catch one layer up — and this one is invisible to it, because that check reads the exemption list in `ContrastPairGateTests`, not this allow-list. Two permissions in the same repo, one policed and one not. The consequence is small today (a single literal) and the shape is not: a directory-prefix entry grants permission to every file added under it, so the next modal inherits a permission from two closed rows. | Retire the entry and give `#F1B232` a home — it is a warning amber with no slot, so it wants the same judgement `#5CE6FF` got in F-114. Then extend `NoExemptionOutlivesItsFinding` to read this allow-list too, since the whole point of that test is that permissions expire with their rows. Deliberately not done in passing while closing F-114: retiring a permission is a measurement, not a tidy-up. | open |
| F-118 | (split from F-044 and F-057 on measurement, 2026-08-21) | 163 declarations across 22 XAML files | a11y | 3 | 4 | `FontSize="11"` x163 app-wide — 50% of every text declaration in the app, and the de facto body size. Concentrated in `PreferencesWindow` (41), `MainWindow` (29), `GamesWindow` (17), `PluginsWindow` (13), `WelcomeWindow` (12). Four sizes also remain off-ladder: 13 x27, 18 x10, 20 x3, 24 x3. Counted against the tree 2026-08-21 after the ladder landed. | F-044's unmeasurable half, and the reason it is unmeasurable is that the judgement is per-site: the ladder reserves 11px for SHORT, UPPERCASE, TRACKED labels (convention C8), and prose belongs at Body 12. Deciding which of 163 sites is which cannot be done by regex — "Last launched Aug 11" is prose at 11px, "DEFAULT" is a meta label at 11px, and they are the same declaration today. **This is where the accessibility claim actually lives:** F-044 closed the floor at 11, and 11 is still under the 12px readability floor its own fix direction sets for anything a person reads as a sentence. | **CLOSED 2026-08-21.** The row expected per-site judgement across 163 sites; **measuring first collapsed it.** Classifying every 11px site by the ladder's own definition of meta — JetBrains Mono, uppercase, tracked — returned **2 mono and 0 uppercase**, and one of those two was a false positive from the scanner's lookback. So the 11px tier was not a mix of prose and labels at all: it was **162 prose and 1 label**. The genuine meta labels were the mono/uppercase ones at 10px, which F-044 had already raised. The judgement call the row priced for did not exist, and it took one script to find that out rather than 22 files of reading. Migration went further than the row asked, because stopping at 11px would have left the ladder half-used and the next developer typing `12`: 12 → Body, 13 and 14 → Heading, 22 → Display. **326 raw declarations in 11 sizes became 16 in 3.** The three that remain — 18, 20, 24 — are hero chrome in About and Welcome where the exact value carries brand identity rather than a role, and collapsing them is a judgement about the mark that a type sweep does not get to make. `TypeLadderFenceTests` gained a third rule, `TheLadderIsActuallyUsed`, because the other two could be satisfied by an app with three literals and no ladder at all. | clean |
| F-119 | (caused by F-044's floor raise, 2026-08-21) | `MainWindow.xaml:576` — the per-row follow-chip strip | visual | 2 | 3 | The chips are an `ItemsControl` whose `ItemsPanel` is a horizontal `StackPanel`, which gives its children infinite width and lets the container clip whatever does not fit — it never wraps and never scrolls. Captures before and after the floor raise, same theme and same window width: the first row's fourth chip read `ItsjusteAgain` complete before and is cut mid-name after. Rows 2-4 were already clipping before the change. | **Recorded as caused, not discovered.** Raising the follow chips from 9px is exactly the F-044 fix and it is right — 9px account names were the defect. The cost is that a strip which was already truncating names now truncates one more. Not filed as someone else's problem: it is this wave's consequence, and the honest version of "we fixed the type" includes what the type cost. The underlying defect predates the change — a horizontal `StackPanel` silently dropping the end of a name is the same class as F-113, text not drawn with nothing reporting it. | **CLOSED 2026-08-21, and the row badly understated what it was.** It recorded a name truncated by a few characters. **The strip was hiding entire accounts.** With seven follow targets saved, the row drew four and clipped three — `PapasbbBri`, `ItsJustEstePapa` and `ItsJustEste` were not shortened, they were absent, so following those accounts was impossible from the main window. A cosmetic row was a functional one. **TWO WRONG FIXES BEFORE THE RIGHT ONE, and the first is the instructive one.** Swapping the `StackPanel` for a `WrapPanel` changed nothing at all: a horizontal StackPanel measures its children with INFINITE width, so the WrapPanel never saw a boundary to wrap at. The fix had to be the PARENT — a `DockPanel` gives its last child the width actually left, which is the number the WrapPanel needed. Verified by capture, not by reasoning; the WrapPanel-only version looked correct in the diff and was inert on screen. Cost, stated: rows grow from ~127px to ~170px for a user following seven accounts, so three rows fit where four did. That buys back three accounts that could not be reached at all. | clean |
| F-104 | (Este 2026-08-12: "self-update is very difficult when you're multi-launching, it goes crazy") | `PreWarmGate`, `RobloxUpdateProbe`, `RobloxCompatChecker.GetInstalledRobloxVersion` | qol | 5 | 3 | **The v1.7.0 pre-warm gate reads a version that is not the one about to launch, so it can report "no update pending" moments before every client in a batch self-updates at once.** `GetInstalledRobloxVersion` orders `version-*` folders by `LastWriteTimeUtc` and returns the NEWEST one's file version (`RobloxCompatChecker.cs:240-253`). But a launch does not run the newest installed version — it runs whatever the `roblox-player` handler points at, and those are different things. **Measured live on this machine 2026-08-12:** handler = `version-082eb75e16714844` at file-version `0,733,448`, while `version-ddf602d9cfe44005` at `0,734,0` was installed the same minute. The probe reads 0,734,0; the launch runs 0,733,448. If the CDN's latest is 0,734,0 the gate concludes no update is pending, returns `LaunchAllNow`, and the batch fans out N clients that each independently discover they are stale. **CORRECTION 2026-08-12, made while fixing this row: the churn evidence below was wrong and is struck.** It read "three versions written inside four minutes" off `LastWriteTimeUtc`. That clock moves when a client RUNS, not when one installs — the four-minute window was an eight-client launch batch for the Store screenshots, not an install storm. The same three folders' real install dates (`CreationTimeUtc`) are 08-09, 08-10 and 08-12: roughly one every day or two, which is ordinary. ~~Three versions written inside four minutes.~~ **The correction makes the finding stronger, not weaker.** Chasing the right clock surfaced that NO folder timestamp can answer "which version is current": on this box `version-7d4de67b` is `0,733,603` created 08-09 while `version-082eb75e` is the LOWER `0,733,448` created 08-10, so creation order does not track version order either. And because write time bumps on run, the existing `OrderByDescending(LastWriteTimeUtc)` is a coin flip mid-batch — re-measured at 00:11:56 UTC it would have returned a THIRD version, matching neither the handler nor the newest install. The handler pin is therefore not merely the better signal, it is the only one that answers the question. Every orphaned client this produces is then an F-103. **Not the strap short-circuit:** `IsStrapHandlingLaunches()` is false here — the handler is `RobloxPlayerBeta.exe` directly — so `Decide` was on its update-pending branch and still let the batch go. | The gate is the right design and it is looking at the wrong number. Its whole purpose is to serialise one update before fanning out, and the one input it needs — *what will actually run* — is the one it does not read. This fires on the single most common real-world event in the product's headline action: an update landing while a user batch-launches their alts. It also explains why the failure feels random rather than reproducible, since it only bites when the handler's pin and the newest install disagree, which is exactly the window an update opens. | **Compare the HANDLER's version, not the newest installed.** Read `HKCU\SOFTWARE\Classes\roblox-player\shell\open\command`, take the `version-*` segment out of the path, and read that binary's `FileVersion`. That is a registry read and a file read — no new network surface, no bootstrapper takeover, posture-clean under spec §7.1. Feed it to `IsUpdatePendingAsync` alongside the CDN check, and pre-warm when EITHER disagrees. **Second, cheaper signal, no network at all:** count `version-*` folders written in the last few minutes. More than one means updates are already thrashing, which is a reason to hold a batch regardless of what any version comparison says. **SHIPPED 2026-08-12.** `RobloxCompatChecker.GetHandlerRobloxVersion()` reads the handler's pinned `version-*` and returns that binary's `FileVersion`; `ExtractVersionFolder` is the pure parse and is tested against the verbatim live registry string, against forward slashes, against a strap-owned handler (no `version-*` segment → null, so the caller falls back), and against lookalikes like `notaversion`. `IsUpdatePendingAsync` now compares the CDN's latest against BOTH the handler version and the newest install and holds when EITHER disagrees; an unreadable side contributes nothing rather than voting "no update". `CountRecentVersionInstalls` counts on `CreationTimeUtc` — deliberately, since counting on write time would report churn during every multilaunch, firing hardest exactly when it is most wrong — and `IsUpdateChurnActive()` is true above one install in ten minutes. `PreWarmGate.Decide` takes a third argument and holds on either signal; the strap short-circuit deliberately still wins, because churn under a bootstrapper is the bootstrapper's own update loop. **The regression test carries the measured numbers:** handler `0,733,448` + newest install `0,734,0` + CDN `0,734,0` must read update-pending; before the fix that assertion reads False. 12 new signal tests, 9 new probe tests, 3 new gate tests, all green, and a base-vs-patch failure-set diff over the whole suite shows no regression. The `Decide` signature widened rather than defaulting, so no call site could keep the old behaviour by accident. **Not verified end-to-end against a live mid-update batch** — that needs the handler pinned to an older install while eight clients launch, which is a manual smoke, not something to automate against real Roblox. Verify by pinning the handler to an older installed version and batch-launching, not by reading the gate — the gate's own unit tests pass today because they test `Decide`, and `Decide` is correct. **The defect is in what it is told, not in what it decides.** | clean |

## Rulings by the user (these override the register)

- **The two shipped emoji stay.** Este, 2026-08-04, on approving this register: the die on
  `Reroll identity` is thematic to streamer mode's deliberately silly naming, not decoration, and
  the same goes for the clipboard on `Copy AI prompt`. Any finding whose fix is "delete the emoji"
  is **closed as ruled**, not open. What those findings were really reacting to is **placement** —
  `Reroll identity` is a settings-shaped action sitting on the main dashboard — and that is already
  covered by the goal-1 relocation work. Invariant 5 is amended in the brief accordingly.

## Absorbed

- **VC-2** → absorbed into **VC-1** (F-025) — same root cause (fill == page bg, no shape affordance), same one-control-style-dictionary fix.
- **VC-5** → absorbed into **VC-4** (F-026) — fix direction is VC-4's fix restated; also carries a factual error (AboutWindow is also NoResize/500px, so Preferences isn't uniquely fixed or narrow).
- **VC-9** → absorbed into **VC-8** (F-064) — same missing SectionHeading treatment, same one-line fix ("deleting a local variant"); its rendered claim is also false (About's "Links" heading is visibly bolder/accent even under flatline).
- **VC-14** → absorbed into **VC-15** (F-037) — one of the four meanings VC-15 already enumerates for accent fill.
- **VC-20** → absorbed into **VC-11** (F-027) — its own fix direction defers entirely to VC-11 ("whatever replaces the two-tone header per VC-11, Welcome adopts it").
- **CO-12** → absorbed into **CO-1** (F-035) — gutter/margin drift is exact, but the entire remedy is "put the numbers in the dictionary CO-1 creates"; its independent button-padding claim was also refuted on recount.
- **CO-13** → absorbed into **CO-1** (F-035) — radius census is exact, but remedy is again "two named resources in CO-1's dictionary"; carries one bad citation (conflicts with CO-8 on what's at `MainWindow.xaml:1360`).
- **AX-9** → absorbed into **AX-1** (F-052) — same naming absence restated per-row; AX-1's fix ("no control ships without AutomationProperties.Name") already covers it; what's left sits on account rows and the Plugins list, out of scope.

## Refuted

- **VC-7** — out of scope (contested-mutex recovery, Bloxstrap strip — not settings/nav/chrome); the cited hex values render identically to their theme tokens by default, so the visible defect is nil.
- **VC-19** — out of scope (transient session-state banners, not settings/nav/chrome); cites "Rubric — similar components sharing a visual language," which is not in the measuring stick.
- **QF-14** — a feature request in a UX costume; no C1-C9 convention requires "Check for updates" and none of the four goals asks for it.
- **QF-16** — new capability request (per-row "Launch again," a filter row); no navigation or settings convention is violated.
- **CV-15** — out of scope (a launch-time interruption modal, not settings/nav/chrome); its own `convention violated` field cites CLAUDE.md rather than C1-C9 or the four goals.
- **CV-22** — merely different, not better; the proposed rewrite keeps the exact "promise-then-reassure" cadence it objects to — taste, not conformance or goal progress.
- **AX-2** — out of scope; the follow-chip strip is an account-row control, explicitly named in the brief as a carve-out example.
- **AX-3** — out of scope; the color swatch, add-tag plus, and drag grip are all account-row controls (the brief's own carve-out examples).
- **AX-4** — out of scope; the batch-selection dot is an account-row control bearing on an action, not on navigation or settings.
- **AX-10** — out of scope; container naming lives inside the account list and Plugins list contents, not in navigation, Settings, or page chrome.
- **AX-14** — the in-scope measurement is wrong. Pixel-measured off the brand capture, the nav band and per-row secondary buttons clear 24×24 at ~29px; the padding-derived "≈23px" undercounts by ignoring the text line box. What's left (the 14×14 batch dot, the swatch) is real but already refused under AX-3/AX-4.
- **AX-16** — out of scope; Diagnostics' internal label/value structure is not a navigation, Settings, or page-chrome claim, and it concedes it's the same grouping primitive AX-12 already carries in-scope.

## Corrections to the brief and the campaign state

- **Title-convention count is seven, not six.** `FriendFollowWindow.xaml` sets no `Title` in XAML, but `Friends/FriendFollowWindow.xaml.cs:133` sets `Title = $"ROROROblox -- Friends -- {ChromeName(current)}"` at runtime — a seventh convention, the only three-part title in the app, and the only one built on the repo name. The brief's XAML-only sweep missed it.
- **Invariant 5's "no emoji" rule is not already enforced — two emoji ship.** `MainWindow.xaml:302` `Header="🎲 Reroll identity"` (U+1F3B2) and `Theming/ThemeBuilderWindow.xaml:52` `Content="&#x1F4CB; Copy AI prompt"` (U+1F4CB) are both `Emoji_Presentation=Yes` codepoints, rendered in full color by Segoe UI Emoji inside monochrome navy chrome.
- **Invariant 2 ("the 626 Labs duo is never split") is breached in two shipped windows, not zero.** `JoinByLink/JoinByLinkWindow.xaml:27-33` ships cyan + white with no magenta separator, and `About/WelcomeWindow.xaml:38-43` ships white + cyan with no magenta either — the second instance the originating lens missed entirely.
- **`ROROROblox` (the repo name) leaks into user-facing copy in four places, not two or three.** `Friends/FriendFollowWindow.xaml.cs:133` (window title / taskbar / Alt-Tab), `Tray/TrayService.cs:221` ("Open ROROROblox," tray menu), `Tray/TrayService.cs:98-100` (tray tooltip, all three states — the surface a tray-resident app shows most often), and `Diagnostics/DiagnosticsWindow.xaml.cs:220` ("ROROROblox support snapshot," first line of a file users forward). All four violate the memory rule that user-facing brand is RoRoRo; ROROROblox stays for code identifiers only.

## Notes carried forward

- **The suite has pre-existing wall-clock flakiness, and it is not a register row.** Measured across
  the v1.18 cycle: **3 of 14 full runs failed on real-time-bounded tests across two classes**, every
  failure showing a roughly 30-second wall clock against a normal 10. It fired again on the
  verification run for item 11 itself — `FpsCapSettlerTests.QuietFileThenSurvivingWrite_Settles`,
  30 s, *"Pump stalled: the settler never re-read GetLastWriteTimeUtc() within 00:00:30 of real
  time"* — and the immediate re-run went green in 10 s at 1490 passed / 0 skipped. **So the number is
  4 of 15 and the direction is unchanged**; `FpsCapSettlerTests` documents the behaviour in its own
  source, and a 4-way bisect during item 7 initially implicated a new test before the identical
  config went 6/6 green. Not caused by this cycle. **No row, on the precedent directly below this
  one:** the "no test loads any XAML" note was declined a row for being test infrastructure rather
  than a UI finding, and this is the same class. F-086 is not a counter-example — that row is about a
  gate silently losing sight of a shipped UI token, a defect in the app's coverage rather than in the
  runner. The cost is real and worth naming anyway: an acceptance criterion of the form "exactly 1490
  passed / 0 skipped" is not verifiable on a single run, so any checkpoint that reads a suite result
  has to re-run before it believes a red. It belongs on the repo's own issue list, not here.

- **The reachability fence cannot express "reachable from where a user would look."** v1.18 item 2
  shipped `SettingsReachabilityTests`, which asserts that *something somewhere* in the App view layer
  can change each persisted setting. `CarefulSquadLaunch` already satisfied that from inside the
  Squad Launch modal, so **the fence would never have caught F-020** — the row whose entire complaint
  is that the only way to change squad-launch behaviour is to begin one. Item 4 added a second
  reachable edge, not a first. Its own docstring calls it a fence and not a gate; this is the
  concrete instance. **No row:** it names no defect in the app, it has no live victim now that F-020
  is closed, and it is already recorded in the test's own source and in `spec.md > §10`. Worth
  carrying here because the next cycle that reads a green fence as "every setting is findable" will
  be wrong, and the fence cannot tell them so.

- **`EdgeRemediationAnswers` is structurally invisible to any name-derived fence.** The persisted
  property is plural and its accessor is singular (`Get`/`SetEdgeRemediationAnswerAsync`), so the
  names cannot match. Exempted in `SettingsReachabilityTests` with the reason inline rather than
  papered over. No row — it is a naming asymmetry in a working feature, and the exemption is the
  decision.

- **No test loads any XAML, so CI cannot catch a window that fails to render.** Raised by wave 2's review gate. `src/ROROROblox.Tests` contains no window instantiation and no `LoadComponent` call — every test targets view-models and services. That is precisely why the `GamesWindow` `TargetType="UIElement"` bug survived from 2026-05-04 to now: a resource-resolution failure at template-apply is invisible to 1268 green tests. Wave 2's own blank-page guard is also runtime-only and uncoverable today. Fix direction is a smoke test that constructs each `Window` on an STA thread inside an `Application`, which turns "does this window even load" into a CI question. Not a UI finding, so no register row — but it is the reason two of this campaign's bugs were found by eye instead of by build.

- **Welcome-tour sentinel bug, found incidentally (QF-13 skeptic).** `MainWindow.xaml.cs:103-110` marks the `.welcome-shown` sentinel *before* checking `Accounts.Count == 0`. An upgrading user who already has accounts burns the sentinel without ever seeing the tour — worse than the original finding stated, since it means the one surface documenting six unlabelled row affordances silently never fires for that user class at all.
- **Flatline fixture definition, load-bearing for several downgrades.** Per `docs/ui-capture-checklist.md:21-23`, flatline collapses backgrounds and text colors but keeps exactly one accent — it does not collapse accent-colored elements into the text color. Several original findings (VC-8, VC-9, VC-10, and to a lesser degree others) claimed accent-colored headings or dividers "disappeared" or became indistinguishable under flatline; the captures disprove that, and those findings were downgraded or absorbed accordingly rather than refuted outright, since the non-accent half of each claim still held. **Corrected 2026-08-10:** this note cited `:8-9` until now, which is the nav-rail correction in that file's own banner, not the fixture definition. Same defect class as the rows above, so it was fixed in the same PR. **And read "flatline" here as the adversarial capture fixture**, preserved since v1.17 as `flatline-lab` in `src/ROROROblox.Tests/FlatlineLabGateTests.cs` — not the built-in theme that now ships under the id `flatline`, which is a different set of hexes and a different set of numbers.
- **QF-12 evidence error.** `Settings/SettingsWindow.xaml`'s seven `IsDefault` hits are `{Binding IsDefault}` / `DataTrigger` bindings against the game library's default-game flag, not `IsDefault="True"` button defaults. The "Enter behaves differently depending on focus scope" argument built on that citation does not hold and was struck from the surviving finding.
- **CO-12 evidence error.** The original button-padding claim ("16,8 leads with 26 uses") mislabeled all-`Padding` counts (Buttons + Borders + TextBoxes) as Button-only counts; `10,8` never appears on a Button at all. The corrected Button-only leaders are 16,8=25 / 10,6=19 / 14,8=12 / 12,6=7. Moot for the register since CO-12 absorbed into CO-1, but worth knowing if the shared dictionary's padding scale gets picked from this data.
- **AX-6's residual gap.** Even the best available theme-derived CTA foreground (navy) only reaches 4.40:1 on magenta in the shipped brand theme — still short of WCAG's 4.5:1 floor. The fix direction in F-050 needs a validation step, not just a resolver, to catch that the "pick the better of two slots" mechanism doesn't clear the bar on its own for magenta fills.
- **CO-6 is stronger than originally written.** `Navy == Bg` holds in all three built-in themes (`ThemeStore.cs:208/217, 224/233, 240/249`), not just the shipped default — the secondary-button affordance problem is structural to the app's theme model, not an artifact of one theme's specific values.

## F-031 verified and widened, 2026-08-05 — it fails in every shipped theme

Computed from the verbatim slot values in `ThemeStore.cs:202-250`, not from the
register's own evidence column. WCAG 1.4.11 requires a non-text interactive
boundary to reach **3:1**.

| theme | Navy == Bg | secondary button edge (Divider on Navy) | primary button edge (Cyan on Navy) |
|---|---|---|---|
| brand (shipped default) | yes | **1.26:1 — FAIL** | 9.39:1 pass |
| midnight | yes | **1.16:1 — FAIL** | 8.05:1 pass |
| magenta-heat | yes | **1.14:1 — FAIL** | 4.90:1 pass |

Three things this settles:

1. **It is not a flatline artifact.** The adversarial theme made it obvious, but
   the failure ships in the default theme every user has right now, at a fifth of
   the required contrast.
2. **`Navy == Bg` in all three built-ins**, so a secondary button's fill
   contributes exactly zero separation. The entire affordance rests on a
   1.2:1 hairline. Any user theme that leaves those slots equal — which every
   built-in does, so it reads as the intended pattern — inherits the same failure.
3. **The primary recipe passes comfortably.** This is not "the palette is too
   dark"; it is one recipe, used on the nav band, Remove, Reroll all identities,
   and 8+ other buttons, whose only boundary token was never contrast-checked.

This is why F-025, F-029, F-030, F-031, F-026 and C5 are one finding wearing six
numbers: they all reduce to *grouping and affordance are carried by a fill, and
the fill is the same colour as what it sits on.* Three waves have now worked
around it — wave 2's rail selection had to be a shape, wave 3's popup had to
carry a border — and each workaround was correct, local, and re-derived from
scratch because there is no shared vocabulary to fix once.

`App.xaml` holds 10 brushes, 9 converters, and **one** keyed control style
(F-035). That is the reason every fix has been a hand-copy.
