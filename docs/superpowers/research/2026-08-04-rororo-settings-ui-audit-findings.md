# Findings register — Settings + navigation audit

> Stage 1 of a scoped vibe-glow campaign. 95 raw findings from five Opus lenses;
> every one faced a fresh Opus skeptic prompted to refute it. 75 survived, 12 were
> refuted, 8 were absorbed into another finding's fix.
>
> Measuring stick: docs/superpowers/specs/2026-08-04-rororo-settings-navigation-conventions.md
> Ranked by goal-service, then severity x visibility.

## Counts

| lens | raw | survived | downgraded | absorbed | refuted |
|---|---|---|---|---|---|
| visual | 20 | 6 | 7 | 5 | 2 |
| consistency | 16 | 10 | 4 | 2 | 0 |
| qol | 21 | 7 | 12 | 0 | 2 |
| copy | 22 | 15 | 5 | 0 | 2 |
| a11y | 16 | 5 | 4 | 1 | 6 |
| **total** | **95** | **43** | **32** | **8** | **12** |

`survived` + `downgraded` = 75 rows from the audit. Later waves may add rows they
discover; the table above counts the audit only. Register total is now **94**
(F-076, F-077–F-083, F-084, F-085–F-092, F-093–F-094 added after the audit — see
the wave-additions note below).

**Wave additions.** A wave ships only its own batch; anything it discovers becomes
a row here, not a drive-by fix.

- **F-076** — added by wave 2, whose own capture tooling put a live webhook
  credential in a PNG.
- **F-077–F-082** — added by waves 4–6 and the memory investigations.
- **F-083** — added 2026-08-09 as the follow-up to PR #96: `ExpectedClientMb` is a
  constant measured on one machine.
- **F-084** — added 2026-08-09 by F-001's final whole-branch review: eight
  owner-resolution sites gate on `IsLoaded` without `IsVisible`.
- **F-085–F-088** — added 2026-08-10 by the v1.17 flatline cycle's reconciliation
  item. Three are colour literals the theme cannot reach — the Bloxstrap banner,
  the consent sheet's `TryFindResource` fallbacks, the status bar's live-process
  dot. The fourth is a blind spot in the contrast gate itself. Three of the four
  were left unfixed by that item and are rows precisely so they stop living in a
  spec; **F-088 was then fixed and closed by v1.17 item 3a**, added at `/build`
  because the builder chose to fix it in-cycle rather than defer it.
- **F-089** — added 2026-08-10 by item 3a's own fence, on its first run. Four
  un-themed hexes in `App.xaml`'s `SelectionDotStyle` that no open row owned and
  spec §7 does not name. It exists because that fence permits a literal only where
  an open finding already owns one — so the choice was a row or a silent
  permission, and a silent permission is the thing the fence was built to stop.
  **Fixed and closed by v1.17 item 3b**, the same session that opened it: it was
  the last always-visible brand hue on the main window, so leaving it open would
  have shipped a cycle whose headline claim its own captures contradicted.
- **F-090** — added 2026-08-10 by the Phase 2 rendered gate, on its first full
  matrix run. The derived `InteractiveEdgeBrush` clears 1.4.11 against `Navy` and
  lands under it on `RowBg`, where 8 of 14 input call sites actually sit. Notable
  for how it survived: it is not un-themed, not un-tested and not un-reviewed —
  three separate checks each had a principled reason not to look at it, and F-031
  records a ratio that is correct about `Navy` and was read as general. **The first
  row in this register opened by a pixel rather than by arithmetic or by eye.**
- **F-093–F-094** — added 2026-08-10 by the v1.18 Settings cycle's reconciliation
  item. **F-093** is `DefaultPlaceUrl`, documented as editable and referenced by
  the App zero times, surfaced by that cycle's own new reachability fence on its
  first run and exempted there rather than papered over. **F-094** is
  `AlertsStatusLine` painting webhook-deleted failures in the accent; `spec.md > §10`
  set the condition — *"worth a row if it turns out to report failures too"* — and
  reading `AlertStatusLine.Compose` settled it, so the condition is met and the row
  is open. Three further v1.18 findings were considered and **declined** rows with
  reasons on the record; see **Notes carried forward**.

## Status, derived from the rows above on 2026-08-10

**51 clean · 41 open · 2 closed-as-ruled · 94 total.**

> ### v1.18 Settings remediation, 2026-08-10 — twelve rows closed, one refused
>
> Counted from the status cells with the same parse `ContrastPairGateTests.NoExemptionOutlivesItsFinding`
> uses, not carried forward from the line above it. The cycle scoped **thirteen** rows to close;
> **twelve** did, each verified against the tree rather than against a commit message.
>
> **Closed:** F-019, F-020, F-023, F-024, F-026, F-033, F-037, F-043, F-051, F-053, F-062, F-078.
>
> **Refused: F-046 stays `open`.** Its destructive rank shipped and three enumerated sites carry it,
> but the row's own headline comparison still renders — `Plugins/PluginsWindow.xaml:258-268` Remove is
> a hand-rolled magenta fill and still the loudest control on its screen, and the 2026-08-10
> re-verification defined this row's "assigned by consequence" clause by exactly that site. The cycle
> held its line at three sites deliberately rather than dragging F-068's forward, which was correct;
> it is still not a closed row. Same handling F-001 got when wave 3 left three items outstanding.
>
> **Counts that moved, recorded with direction per this file's own rule.** F-068 **61 to 55** sites,
> **24 to 22** files — the first movement in five days, and five of the six came from a sweep that
> adopted an existing rank rather than hand-copying attributes, which would have moved it the other
> way. F-079 `Background="#"` literals **12 to 11**, `Modals/` total hex **54 to 52**.
> `AllowedXamlLiteralCeiling` **97 to 95**. F-086's gate coverage **44 elements across 18 files to 39
> across 16**, pairs unchanged at 8 — a good fix shrank a gate's field of view by 5 elements and
> nothing announced it, which is F-086's own thesis arriving from the opposite direction.
>
> **F-050 was not touched and its row is byte-identical.** Verified by diff, not by eye.
> **F-052's 137-declaration backlog is whole** — the cycle named only the controls it added, stated
> as a rule in `spec.md > §3.4` so the number stays honest. F-068 and F-091 likewise un-nibbled.

The pass the v1.18 cycle was scoped off, hours before it started, is preserved below it.

> ### Re-verified 2026-08-10 — all 51+6 open rows checked against the tree
>
> A full re-verification pass ran over every open row before scoping the next remediation cycle.
> Per-row detail is preserved in
> [`2026-08-10-register-reverification/`](2026-08-10-register-reverification/). Verdicts across 57
> rows: **24 accurate · 22 drifted · 5 partly shipped · 5 already fixed · 1 superseded.**
>
> **Six rows should not have been open.** F-027, F-042, F-055, F-056 and F-058 were fixed by other
> waves' artifacts and nobody flipped them; F-069 was closed as a class by the 2026-08-04 ruling and
> is F-040's twin, the precedent `CLAUDE.md` already cites for *rulings override rows*. Five more are
> partly shipped. **Eleven of 57 rows — nearly a fifth — would have mis-scoped a cycle read off this
> table.** That is the F-001 failure at scale, and it is why this pass exists.
>
> **The drift is faster than the re-reads.** F-018's citation was reconciled on 2026-08-09 and was
> already stale by 2026-08-10. F-085 was written by v1.17 item 6 and falsified by item 3a of the same
> cycle, the same day. A row is not stale after a month; it is stale after an afternoon.
>
> **Rows whose numbers moved are annotated in place with the new figure and the direction**, per this
> file's own rule. The 22 drifted rows' line citations were verified and are recorded in the batch
> reports; they are not all transcribed back into the table, because line numbers drift again on the
> next commit and the batch reports name the content to search for instead.
 Counted from the status
cells, not carried forward from a previous summary. The v1.17 flatline cycle's
reconciliation item changed no row's status and added four; item 3a then closed
one of those four (F-088) and added F-089; item 3b closed F-089. The rendered gate
built after that cycle added F-090, and the C2 walkthrough added F-091 and F-092. Net: total +7,
`clean` +2, `open` +5.
**F-050 stays `open` on purpose** —
that cycle shipped a theme that does not need the exemption, which is not the same
thing as implementing F-050's fix direction. See its row.

### Reconciliation pass, 2026-08-09

The register had drifted from the tree, and it drifted in a way worth naming:
**glow waves maintain their own rows; work that lands outside a wave does not.**
Five rows were describing problems that no longer existed, every one fixed by
work outside a wave's own batch — the memory wave (PR #96), the Settings→Games
rename, and wave 3 closing F-036 sideways. A sixth was sitting open against a
ruling.

This is not bookkeeping. It cost a build cycle: F-001 was picked as the top open
finding at 5/5, and roughly 60% of it had already shipped in wave 3. The scope was
three menu items, not a container. Anyone picking from a stale register pays that
again.

**Flipped to clean** (each verified against the tree, not a changelog):

- **F-028** — the Settings→Games rename settled button/title/class/folder, and the
  ~310-char subtitle is gone. Fixed outside any wave.
- **F-080** — `LiveProcessSummary` is bound in `MainWindow.xaml`. Shipped in PR #96.
- **F-081** — the exit log now carries exit code and uptime. Shipped in PR #96.
- **F-082** — `BelowReserve` exists on the snapshot and the watchdog logs
  `memory headroom crossed`. Shipped in PR #96, and watched firing on a real
  oversubscribed machine on 2026-08-09 (545 MB free against a 2615 MB reserve).
- **F-036** — both the header pitch and the status-bar tagline are gone;
  `MainWindow.xaml:988-1000` is now a comment explaining the removal. Closed
  sideways by wave 3.
- **F-032** — the label token moved to `WhiteBrush`, quiet secondary now carried by
  weight plus the F-031 edge, fenced by `MutedTextFenceTests`.

**Credit and a caveat on method.** F-036 was not found by this pass. It was found
by **PR #93**, an earlier reconciliation opened 2026-08-05 that never merged and
went stale while the register drifted further. This pass filtered on three things
— rows citing files that no longer exist, rows whose claims are mechanically
countable, and the top rows by severity. F-036 fell outside all three. A fourth
filter caught the rest: **finding IDs cited in source comments**, which is a strong
signal that code addressed a row. Every open row cited in code was checked; only
F-026 came back genuinely open, and its comment says so explicitly.

**So this pass is not proof the other 52 are real.** It is proof that six were not.
The honest position is that the register is now materially more accurate than it
was, not that it is verified end to end.

**Closed as ruled:**

- **F-040** — its fix direction is “Delete both codepoints, no replacement.” The
  rulings section below closed that entire class on 2026-08-04 and states it
  overrides the register. The row should never have stayed open; it was sitting
  against a decision already made.

**Still open, evidence corrected** (see the reconciliation notes appended to each
row): F-018 and F-039 are each half-shipped by F-001; F-021's citation moved with
the Games rename; F-042 has drifted **worse** since the audit.

**F-032 deserved separate mention while it was open.** It measured 11 controls binding
`MutedTextBrush` as `Foreground`; by the reconciliation pass `MainWindow.xaml` alone had 15.
Waves 5 and 6 added contrast machinery around it without stopping the leak spreading. It has
since been fixed and fenced — but an open row is not a static thing, and nothing was watching
that one grow.

**Added:** F-084, the hidden-owner defect class, surfaced by F-001's final review.

**The rule this suggests:** any PR that closes a register row flips the row in the
same PR, wave or not. Otherwise the register measures wave activity rather than the
app.

Closed by wave 1, verified 2026-08-04: F-006, F-008.
Closed by wave 2 (merged `159fe4b`): F-002, F-003.
Closed by wave 3 (merged `3fc838b`): F-009, F-010, F-011, F-012.
Closed standalone (merged `9587ee6b`): F-076.
Closed by wave 4: F-004, F-005, F-007, F-014, F-015, F-016, F-017 — all four campaign goals now have shipped work.

> This line said **6 clean · 70 open** for about ten minutes. It was accurate when
> written and wrong by the time it merged, because F-076 landed in a second PR
> right behind this one. Re-derived from the rows rather than patched by one —
> which is the whole reason the rule says derive, never carry forward. Leaving the
> note because a scoreboard that silently self-corrects teaches nobody anything.

**Wave 1's rows (F-006, F-008) are now verified and closed** — against the tree,
not the changelog:

- **F-006** — `GamesWindow.xaml:6` `Title="RoRoRo -- Games"`, `:344` header
  `Text="Games"`, and the exact prescribed tooltip ("Saved games and private
  servers for the launch picker.") now on the Tools menu item at
  `MainWindow.xaml:1102`. The only surviving "Library" in the app is inside a
  code comment.
- **F-008** — the streamer toggle is at `PreferencesWindow.xaml:142` and *Reroll
  all identities* at `:154`, both in Settings. The tray checkbox survives
  (`TrayService.cs`, `_streamerModeItem`). The main-window band and its duplicated
  helper line are gone; the only streamer string left on MainWindow is the
  per-row *Reroll identity* context item, which is a per-account action, not the
  band the finding was about.

**F-001 did not close with wave 3.** Its fix direction lists Stop all, Open log
folder and the Welcome tour as Tools items alongside History/Diagnostics/Plugins/
Library. The first three have no view-model command — the tray raises events
handled in `App.xaml.cs` — so they need new plumbing rather than a menu entry.
F-001 stays open with exactly those three outstanding.

**Residue carried out of wave 2, still open:** C5 is unmet *inside* a Settings
page. The nine section cards remain fill-only and collapse under flatline;
splitting them across five pages did not make cards load-bearing.

**Residue carried out of wave 3, still open:** item hover and pressed fills
inside the Tools popup come from WPF-UI's own dictionary, which `ThemeService`
never touches — so they ignore the user's theme. That is true of every
`ContextMenu` in the app, not just this one, which is why wave 3 themed only what
it introduced (the popup border and the separator) and left the rest.

## The register

| id | lens id | surface | lens | sev | vis | evidence | verdict | fix direction | status |
|---|---|---|---|---|---|---|---|---|---|
| F-001 | QF-19 | History/Diagnostics/Settings IA | qol | 5 | 5 | `AppSettings.cs:418-433` SettingsBlob has no history/diagnostics field; History writes only favorites+Clear; Diagnostics writes only a user-chosen zip | Tools, not preferences — verb-shaped, episodic, zero overlap with settings.json; tray precedent (leg 4) actually argues against a Settings/Tools split, not for it | Build Tools container on main window (pairs with QF-21): History, Diagnostics, Plugins, Library, Stop all, Open log folder, Welcome tour; Settings keeps only what writes settings.json/discord.dat [new-mechanism] | clean |
| F-002 | QF-20 | Preferences | qol | 5 | 5 | `PreferencesWindow.xaml:6-7,37-330` fixed 500x600 NoResize, 8 cards/~30 controls in one ScrollViewer; RowBgBrush vs BgBrush = 1.09:1 brand / 1.00:1 flatline **Reconciled 2026-08-10:** the 1.00:1 is `flatline-lab`'s, the adversarial fixture at `src/ROROROblox.Tests/FlatlineLabGateTests.cs` — not the `flatline` theme v1.17 ships, which measures **1.33:1**, better than brand's 1.09:1. Both numbers are real and they belong to different themes. | Split into a 5-page left nav rail — but "survives flatline" is a property of the selection indicator you build, not of rails per se; 5 pages over ~30 controls trades scroll for near-empty rooms | SettingsShell: ListBox rail + ContentControl, one UserControl/page, MinWidth ~760, resizable; shape-based selection indicator (3px bar + weight), not a fill; DiscordConfig single-owner refactor lands with/before it [new-mechanism] | clean |
| F-003 | QF-2 | Preferences | qol | 5 | 5 | `PreferencesWindow.xaml:6-7` Height=600 Width=500 NoResize; flatline scrollbar thumb covers ~1/5 of track | The tallest, densest surface in the app is the one window the user cannot resize | Split into named pages behind a nav rail and drop ResizeMode=NoResize; MinHeight/MinWidth as an interim one-line fix [new-mechanism] | clean |
| F-004 | CV-1 | cross-surface, all 25 windows | copy | 5 | 5 | 25 `Title=` + `FriendFollowWindow.xaml.cs:133` runtime title = 7 competing conventions; 18/25 already fit a destination/interruption split, 7 break | No stated title rule exists — most common invention (prefixing product name) is the "looks like an ad" defect, literally | One 3-line rule: title bar names destination only; heading matches title bar; destinations take a noun, interruptions state the problem. Apply to the 7 breaks; reserve two-tone wordmark for MainWindow + About [new-mechanism] | clean |
| F-005 | CV-8 | main toolbar / Preferences / tray | copy | 5 | 5 | `MainWindow.xaml:1020` "Settings" → `PreferencesWindow.xaml` Title="RoRoRo -- Preferences", header prints "Preferences / Settings" (:21,27); tray :227 "Preferences..." | Same destination called Settings, Preferences, both-at-once, and Preferences again — nobody chose | "Settings" everywhere; retire "Preferences" as a user-facing word; replace stale tooltip (:1028, omits Discord/alerts/idle/transport) | clean |
| F-006 | CV-7 | main toolbar → Games/Library | copy | 5 | 4 | `MainWindow.xaml:1056` Content="Games" → `SettingsWindow.xaml` Title="RoRoRo -- Library", header "Library" (:343); tooltip covers only half the window | One destination, three names; tooltip describes only games, not private servers — goal-3 verbatim | Pick "Games" everywhere visible (title, header, tooltip: "Saved games and private servers for the launch picker.") | clean |
| F-007 | CO-3 | 12 destination windows + 7 modals | consistency | 4 | 5 | Two-tone header copied 12x at 4 sizes/4 grammars — 22px "Page/descriptor" (6 of 12), 24px About, reversed no-separator Welcome, 20px/18px singles | Twelve near-copies with no agreement on size, grammar, or whether the product name belongs in the title — the mechanism behind goal 4 | One PageHeader control taking Page + Descriptor, separator/size/casing fixed inside; title bar carries product name once, not twice [new-mechanism] | clean |
| F-008 | QF-5 | main window, Row 3 band | qol | 4 | 5 | `MainWindow.xaml:1365` tooltip and `:1379` body text identical, word for word; toggle already has a tray home (`TrayService.cs:211`) | A setting already reachable from the tray occupies a full band of the main window and explains itself twice | Move toggle + Reroll all identities into the Settings shell; keep the tray checkbox; delete the duplicated helper line | clean |
| F-009 | QF-21 | main window, Row 2 toolbar | qol | 4 | 5 | `MainWindow.xaml:1019-1073` six identical buttons at MinWidth=860, band already edge-to-edge; Games opens a library at Settings' weight (C4 drift) | Collapse six buttons to Settings + Tools▾ — capacity/heterogeneity hold; "tray already proves this" does not, since the tray groups Settings inside Tools, not apart | Two buttons where six were; Tools▾ mirrors the tray's item list; rename Games→Library in the menu; keep Launch multiple/Squad Launch in place [new-mechanism] | clean |
| F-010 | CV-2 | main window | copy | 4 | 5 | `MainWindow.xaml:992` "Run multiple Roblox clients side by side. Add a saved account, click Launch As to open it." | Store-listing copy pasted into the app, read on every launch by a user with 8 accounts already on screen | Delete the line — WelcomeWindow's first-run guide and the empty-state text (:1597-1601) already carry it to the user who needs it | clean |
| F-011 | CV-3 | main window footer status bar | copy | 4 | 5 | `MainWindow.xaml:1809` "Multi-launcher for Windows. A 626 Labs product." — the other three status-bar cells report live state | Positioning copy in a status bar — the literal instance of goal 4's "ad" reading | Delete it; the line already lives in About (:73,77) where it's the actual subject | clean |
| F-012 | CV-11 | main window toolbar | copy | 3 | 5 | `MainWindow.xaml:1020` gear glyph (U+2699, text-presentation, survives invariant 5 on the letter) against 5 bare-text peer nav buttons | Not an emoji violation — a consistency violation; one of six identically-weighted buttons wears an icon for no distinction the other five lack | Content="Settings"; all six carry icons or none do — "none" is cheaper and more theme-durable | clean |
| F-013 | QF-1 | every secondary window | qol | 4 | 3 | `MainViewModel.cs:3188-3547` every opener is `window.ShowDialog()`; `PreferencesWindow.xaml.cs:43-55` documents DiscordConfig is safe only because the dialog is modal | Six modal islands off one toolbar — every cross-surface task costs a close-and-reopen round trip; going modeless has a hard prerequisite the finding correctly names | Fold six into one non-modal shell with a persistent left nav; DiscordConfig gets a single owner first [new-mechanism] | open |
| F-014 | CV-5 | Games/Library | copy | 4 | 3 | `SettingsWindow.xaml:347` — 55-word, four-sentence subtitle including a rhetorical question, above the content it describes | A help article wearing a subtitle's clothes — C7's subordinate-by-position rule violated | Delete the subtitle; move each sentence to the control it explains (:427, :443); drop the rename note (already the tooltip at :182) | clean |
| F-015 | CO-16 | Settings/Library, About, Preferences subtitles | consistency | 3 | 4 | Preferences/Diagnostics/History hold the one-line grey contract; `SettingsWindow.xaml:347` is a 340-char paragraph in the same slot; About stacks two subtitle roles; Welcome uses 12px/0,6,0,0 vs everyone's 11px/0,4,0,0 | Subtitle slot has no length/count contract — absorbed a help paragraph on one page, a tagline stack on another; the concrete shape of goal 4's "ad" reading | Give the shared PageHeader a single optional one-line descriptor with a hard character budget; relocate the Library paragraph into page body [new-mechanism] | clean |
| F-016 | CV-4 | Preferences | copy | 3 | 4 | `PreferencesWindow.xaml:31` "How RoRoRo behaves — startup, idle, Discord, alerts, and theme." — omits Recycle and Accounts transport (2 of 8 cards) | A table of contents for a page six inches below it, and one that rots every release | Delete the line; with CV-1 applied the header reads "Settings" and the first card starts | clean |
| F-017 | CV-6 | cross-surface, 11 page subtitles | copy | 3 | 4 | Diagnostics (:35), History (:34), Plugins (:77) plus Squad Launch/Join by link/Friends/Theme builder/Export/Import | Every window subtitles itself, so the device carries no information — states the test and applies it: delete 4, shorten 2, keep 6 | DELETE Accounts/Settings/Library/History subtitles; SHORTEN Diagnostics and Plugins to their behavior-changing clause only; KEEP the other 6 as-is | clean |
| F-018 | QF-6 | main window / tray | qol | 3 | 3 | `TrayService.cs:203,215` Multi-Instance state + Stop all are tray-only; grep of MainWindow.xaml/MainViewModel.cs returns only banner-copy references **Reconciled 2026-08-09:** the Stop-all half shipped with F-001 (`MainWindow.xaml` Tools group, bound to `StopAllCommand`). Multi-Instance state is still tray-only — the one `multi-instance` hit in `MainWindow.xaml:1471` is a comment on the contested-lock banner, i.e. still banner copy. | The product's core switch and most destructive action exist only in a right-click tray menu Windows hides by default | Surface Multi-Instance state on the main window status bar; put Stop all in the Tools group (QF-21); tray keeps both | open |
| F-019 | AX-12 | Preferences (Settings) | a11y | 3 | 3 | `PreferencesWindow.xaml:37-330` one ScrollViewer, fixed NoResize; 19 sequential focus stops, no headings/groups; 3 section labels are plain Text; RowBgBrush vs BgBrush = 1.09:1 brand **Closed 2026-08-10 by v1.18 item 1**, verified against the tree. Every leg of this row is now answered by a different artifact and no single one of them did it alone. `NoResize` went with the nav rail (F-002/F-003): `PreferencesWindow.xaml:7-8` sets `Height 640 Width 860 MinHeight 480 MinWidth 760` and the file carries no `ResizeMode` attribute. The section labels are the part item 1 owns — **all 8 headings now carry `SectionHeadingStyle` plus `AutomationProperties.Name` and `AutomationProperties.HeadingLevel="Level2"`** at `:117, :197, :256, :289, :333, :515, :661, :695`, standing OUTSIDE the cards rather than hand-rolled inside them, and `HeadingLevel` is the property that actually lets a screen reader move group-to-group. Plain-Text section labels: **3 to 0.** The linear run is re-measured rather than assumed: 23 declared focusable controls before this cycle, more now, but collapsed pages are out of the tab order so the run a user faces is per-page — worst case is Alerts and memory at **14**, or 15 while the muted-accounts block is visible, down from the audited 19. Item 3's own commit records that growth past item 1's stated 12 rather than contorting around it. | Settings being "too tall" is a 19-stop linear tab run with no headings and no bypass — confirms "a group that is only a fill is not a group" | Whatever the visual answer, each group needs a real container with a real heading so keyboard/screen-reader users move group-to-group [new-mechanism, shared with QF-20] | clean |
| F-020 | QF-4 | Squad Launch modal / Preferences | qol | 2 | 2 | `SquadLaunchWindow.xaml.cs:59,73,79` read/write a global persisted preference reachable only from inside the modal it modifies **Closed 2026-08-10 by v1.18 item 4**, verified against the tree. `CarefulSquadLaunchToggle` sits at `PreferencesWindow.xaml:174-181` as the third ROW inside Startup's card, binding the same persisted `CarefulSquadLaunch` through the same `IAppSettings` accessors; the in-modal toggle stays, both surfaces re-read on open and neither caches. It is a row and not a card because item 1 had just collapsed this page's single-control cards, and a third card would have restored the shape that collapse removed. The rail has no launch page and none was added — Startup already holds a launch setting, so the fix landed on an existing page rather than a sixth one. **What this row proves about the cycle's own new fence, recorded because it is uncomfortable:** `SettingsReachabilityTests` could never have caught F-020. `CarefulSquadLaunch` was already fence-reachable from the modal, so the fence was green on the exact defect this row describes. See the note carried forward below. | The only way to change how squad launches behave is to begin one | Mirror the in-modal toggle into the Settings shell's launch page, binding the same IAppSettings value | clean |
| F-021 | CV-20 | Games/Library empty state | copy | 2 | 2 | `SettingsWindow.xaml:452` "Use the Squad Launch toolbar button to add one." — sends user to a closed window; the server actually saves itself automatically (`SquadLaunchWindow.xaml.cs:367`) **Reconciled 2026-08-09:** citation stale, defect intact — the copy now lives at `Games/GamesWindow.xaml:404` after the Settings→Games rename. | An empty state issuing an errand for a mechanism that's actually automatic — a goal-3 defect | "Private servers save themselves the first time you launch one from Squad Launch." | open |
| F-022 | CV-21 | main window FPS-mismatch banner | copy | 2 | 2 | `MultiInstanceCopy.cs:23-26` — 45 words leading with diagnosis, actionable clause buried last; banner is conditional and dismissible | Correct, load-bearing content, but unpacks before the verdict | Invert — lead with "Set every account to the same FPS cap to launch at full speed," mechanism follows, same length | open |
| F-023 | QF-3 | Preferences (absent), memory watchdog | qol | 3 | 1 | `AppSettings.cs:277-347` persists 4 watchdog settings; `IAppSettings.cs:104` "no dialog wiring exists yet"; zero .xaml hits **Closed 2026-08-10 by v1.18 item 3**, verified against the tree. All four are editable on the page already named for them — a Memory section at `PreferencesWindow.xaml:514-654` on "Alerts and memory", holding `MemoryWatchdogEnabledToggle :521`, `MemoryReserveMbInput :568`, `MemoryCapMbInput :591` and `ProjectionWarnMinutesInput :614`, each round-tripping through the existing `IAppSettings` accessors with no second source of truth. Free text rather than a preset picker, forced by the acceptance criterion: a picker cannot produce an out-of-range value, so "refused visibly" would be unfalsifiable by construction. Refusal is its own line, `MemorySettingsWarning :645`, in `RowExpiredAccentBrush` with the triangle and `LiveSetting Assertive` — a setter that clamps quietly is indistinguishable from one that worked. **Item 4a extended it at C1:** `AutomaticMemoryLine :554` says what a blank box resolves to on this machine, derived through the same `MemoryDefaults` calls `App.WireMemoryWatchdogAsync` runs, so the number on screen cannot disagree with the number the watchdog uses. **The durable artifact is not the controls.** `SettingsReachabilityTests` was written first and watched go red naming exactly these four and nothing else, then green — the fence is what stops this class regrowing, and it is why this row survived months. | RoRoRo routes the memory alert but gives no control over when it fires or what's too much — editor is a text editor | Give the memory watchdog a section in the new Settings shell next to the alert routing that already exists | clean |
| F-024 | QF-18 | Preferences → Alerts / account rows | qol | 2 | 1 | `MainWindow.xaml:282-291` mute persists per-account (`MainViewModel.cs:2883`); Alerts card (`:168-251`) never mentions muted accounts **Closed 2026-08-10 by v1.18 item 5**, verified against the tree. `MutedAccountsRow` at `PreferencesWindow.xaml:394-410` carries the count line and `UnmuteAllButton`, sitting directly under the two routing rows because it is the exception to them. **The count reads the ROWS, not the persisted id list**, and the asymmetry is deliberate and pinned by `MutedAccountsSummaryTests`: an id in `DiscordConfig.MutedAccountIds` outlives the removal of its account and nothing prunes it, so a file-derived count would report accounts the user does not have and cannot find. Unmute all still empties the whole list, orphans included, because "all" means all, and it writes through `SaveDiscordConfigAsync` rather than the per-row writer whose own doc names it as the other writer of this record. Zero muted collapses the whole block rather than rendering a stray `0`. Unmute all takes the ordinary secondary treatment on purpose — it is destructive-adjacent and not on item 10's enumerated list of three, and a fourth site was the signal to drop that work rather than widen it. **Citation fix carried in the same item:** items 3 and 4 had attributed careful mode to F-024 in two source comments. Careful mode is F-020. A wrong comment would have handed this row the wrong closure evidence today. | Per-account muting is right-click-only, and the Alerts page owning every other alert decision doesn't mention it | Add a muted-account count + unmute-all affordance to the Alerts section — data already in the VM | clean |
| F-025 | VC-1 | main-window | visual | 5 | 5 | `App.xaml:27,36` BgBrush==NavyBrush; 8 secondary controls rely on a DividerBrush border alone; flatline shows all 8 collapse to text while Launch multiple's cyan border survives | C8's secondary recipe is one-legged — fill contributes zero contrast even in the shipped theme; a single collapsible brush takes out 8 controls on the most-seen screen | Affordance from a property the theme can't flatten — accent-set outline or geometry; needs an app-wide control-style dictionary [new-mechanism] | clean |
| F-026 | VC-4 | preferences | visual | 4 | 5 | `PreferencesWindow.xaml` 8 sibling cards — 5 hold one setting, 3 (incl. Alerts, 13 controls) sit at identical weight **Reconciled 2026-08-09:** still open and deliberately so — `Controls/ControlStyles.xaml:119-120` states it was not smuggled into wave 6. Effort has dropped though: that wave made this **a one-place edit instead of a nine-place one**. Preferences currently holds 9 card borders under a 5-page nav rail, so the nav rail (F-002/F-003) supplied the level above the card; what remains is the card-vs-section weight distinction itself. **Closed 2026-08-10 by v1.18 item 1**, verified against the tree, and the answer was already in the dictionary. `SectionHeadingStyle` ships 13px SemiBold White at `Margin="0,18,0,6"` — which is exactly what this page had been hand-writing at three sites — so the 18-above/6-below rhythm IS the missing level, placed BETWEEN cards rather than inside one. No second container primitive was added; `SubCardBorderStyle`, `GroupBox` and `Expander` appear in `PreferencesWindow.xaml:98-100` only in the comment forbidding them, because a second container rank restates the two-meanings defect this row is about. **Counts re-measured at HEAD, per this file's own rule.** Item 1 took cards **9 to 7** and single-control cards **3 to 0**, control count conserved at 22; item 3 then added the Memory section, so the tree today holds **8 cards** at `:120, :200, :259, :292, :336, :518, :664, :700` and **8 headings**, with **zero** single-control cards. Net direction: cards shrank 9 to 8, single-control cards shrank 3 to 0. The weight collision the row is actually about is gone — a one-checkbox card and the nine-control Alerts card no longer sit as siblings at identical weight, because the level above them is now a heading and not another border. **And the framing this item was handed was wrong, corrected here:** the checklist said the hierarchy must survive colour removal, "check flatline, not just brand", implying flatline is the weak case. Measured, RowBg on Bg is 1.09 brand, 1.08 midnight, 1.08 magenta-heat, **1.33 flatline** — flatline has the STRONGEST card fill and all four are under 3:1. Grouping-by-fill was thin everywhere, not thin in one theme. Second time in two cycles that `flatline` was treated as adversarial when the adversarial numbers belong to `flatline-lab`. | One grouping primitive carries two incompatible meanings at identical weight — why goal 2's "split it up" can't be answered with more cards | Separate the two jobs structurally — a section needs a level above the card, a setting stays a row inside it [new-mechanism] | clean |
| F-027 | VC-11 | main-window | visual | 4 | 5 | Cyan/slash/white lockup reused 9x with 6 different second-token meanings — product/page, product/version, page/instruction, etc. **Closed 2026-08-10 by re-verification.** The lockup went 9 sites / 6 second-token meanings to **2 / 2**, shrank. `Controls/PageHeader.xaml` fixed size and grammar across 11 windows with the wordmark off by default (`:30` sets `Visibility=Collapsed`, revealed only by a `ShowWordmark=True` trigger). The two survivors, `MainWindow.xaml:1078` and `AboutWindow.xaml:60-71`, are exactly the two **F-004** (`clean`) reserves by name, and `PageHeader.xaml:17-20` states that rule in a comment citing F-004. Fixed by F-007's artifact; nobody flipped the row. Second instance of the F-001 pattern. | The lockup is the brand wordmark; reusing it as page chrome forces a second token with no consistent job | Retire the lockup from page chrome (About, splash, tray only); page chrome names the destination once, at one size [new-mechanism] | clean |
| F-028 | VC-13 | library | visual | 4 | 5 | `SettingsWindow.xaml:347` ~310-char/4-sentence subtitle vs 62 chars in Preferences; Title="RoRoRo -- Library" on class SettingsWindow in folder Settings/, opened via button "Games" | The one-line subtitle convention becomes a manual on the one window whose name/class/folder/button already disagree four ways | Move the rules to the controls they govern; settle the name (button/title/class/folder) in the same pass | clean |
| F-029 | CO-4 | Plugins/Preferences/Library/JoinByLink/SquadLaunch/ThemeBuilder/Import/Rename/CaptionColorPicker vs MainWindow | consistency | 4 | 5 | 2 ui:TextBox (MainWindow only) vs 11 plain TextBox across 10 windows, disagreeing on fill; flatline shows the install-URL field vanish while the main-window filter survives | Two input systems; the one used on 11 of 12 surfaces disappears under a theme the app ships support for | One AppTextBox style — standardize on ui:TextBox (invariant 4 already assigns chrome to WPF-UI) or give the shared style a shape cue independent of a collapsing fill/border | clean |
| F-030 | CO-6 | cross-surface, every secondary button | consistency | 4 | 5 | C8's secondary recipe is Navy==Bg in all three built-in themes; nav band, Remove, Reroll all, and 8+ other buttons rely on it; Launch multiple's cyan border is the sole survivor | Four secondary-button recipes exist; the most-used one binds its only shape cue to the divider brush | One SecondaryButton style whose outline reads at any brush setting; new token optional-with-fallback per invariant 6 [new-mechanism] | clean |
| F-031 | AX-5 | nav band + per-row Friends/Remove/Reroll all/Open themes folder/follow chips | a11y | 4 | 5 | DividerBrush vs NavyBrush = 1.26:1 brand / 1.00:1 flatline (fails WCAG 1.4.11's 3:1 in both); CyanBrush vs NavyBrush (Launch multiple) = 9.39:1/4.34:1, passes both **Reconciled 2026-08-10:** both flatline columns belong to `flatline-lab`, the adversarial fixture at `src/ROROROblox.Tests/FlatlineLabGateTests.cs`, which reproduces the 1.00:1 and the 4.34:1 on every test run. The `flatline` theme v1.17 ships is a different theme and measures **1.51:1** and **12.84:1** on the same two pairs — the divider still under 3:1 by design, so `EdgeRemediation.Decide` derives `InteractiveEdgeBrush` at 3.03:1 exactly as it does for brand. Both stated; conflating them is the defect being fixed. | The border was never a legible boundary even in the shipped brand theme — fix is binding the boundary to a contrast-guaranteed token, not "add a border" | Interactive boundaries bind a token guaranteed ≥3:1; add a load-time validation step that warns on a theme falling under 3:1 [new-mechanism] | clean |
| F-032 | AX-7 | nav band + Friends/Remove/Reroll all/star/Open themes folder | a11y | 4 | 5 | 11 controls bind MutedTextBrush as Foreground (incl. Remove at `:915`) — same token as helper prose; MutedTextBrush vs WhiteBrush = 2.42x brand but 1.00:1 flatline **Reconciled 2026-08-09: worse, not better.** `MainWindow.xaml` alone now binds `MutedTextBrush` as `Foreground` on **15** controls, up from the 11 measured at audit. Waves 5-6 added contrast machinery (`ContrastGuard`, `InteractiveEdgeBrush`) without stopping this leak spreading. **Reconciled 2026-08-10:** the 1.00:1 is `flatline-lab`'s, reproduced by `src/ROROROblox.Tests/FlatlineLabGateTests.cs`. Shipped `flatline` measures **2.65:1** muted-vs-white, better separation than brand's 2.42:1, so the token carries "secondary" on ratio there without a weight change. Note what neither number is watched by: PR #100's own fix removed the last declared pair whose foreground was the prose token, so the contrast gate cannot see `MutedTextBrush` at all any more — F-086. | The app expresses "control" and "explanatory prose" with the same color token — C7's collapse and C6's affordance loss share one root cause | Bind control labels to the primary text token; carry "secondary" in weight + the AX-5 boundary; extend Preferences' indent device to the main window | clean |
| F-033 | QF-7 | main window, Compact mode | qol | 4 | 4 | `MainViewModel.cs:663` plain SetField property; nothing writes it to disk; SettingsBlob has no compact field **Closed 2026-08-10 by v1.18 item 6**, verified against the tree. `CompactMode` is on the persisted record at `Core/AppSettings.cs:508` with a `false` default, accessors at `:394` and `:401`, interface at `Core/IAppSettings.cs:151-152`, and `MainWindow.xaml.cs:105` calls `RestoreCompactModeAsync` on load. No migration, asserted rather than assumed — an existing `settings.json` with no such key loads and defaults off. **Restore deliberately does NOT route through the public setter**, which is why `ApplyCompactMode` is split out at `MainViewModel.cs:725`: a restore through the setter would write back the value it just read on every launch, and on a read-only `settings.json` would log a failure the user never caused. A test asserts zero writes on restore. Persist is the fire-and-forget-into-try/catch shape the neighbouring `PersistIsSelectedAsync` and `PersistTagsAsync` already use, so nothing awaits the disk in front of a click; a failed persist keeps the toggle for the session and loses only the memory across a restart, which is the failure the disk actually had. | Compact mode — pitched by the Welcome tour as a second-monitor workflow — is forgotten on every restart | Add CompactMode bool to SettingsBlob (no migration needed) and restore it in MainWindow.OnLoaded | clean |
| F-034 | CV-9 | tray menu | copy | 4 | 4 | `TrayService.cs:221` "Open ROROROblox"; also tooltip :98-100 "ROROROblox — Multi-Instance ON/OFF/ERROR"; `DiagnosticsWindow.xaml.cs:220` "ROROROblox support snapshot" **Reconciled 2026-08-09:** 1 of the 4 leak sites is fixed — `FriendFollowWindow.xaml.cs` no longer builds a repo-name title. Three remain: `TrayService.cs:98-101` (tooltip, all three states), `:221` (“Open ROROROblox”), `DiagnosticsWindow.xaml.cs:222`. | The repo name shipped into the menu and tooltip a tray-resident app shows most often, violating the RoRoRo-brand memory rule | "Open RoRoRo" in menu + tooltip; "RoRoRo support snapshot" in the bundle header; same fix covers FriendFollowWindow's title (CV-1 row 6) | open |
| F-035 | CO-1 | cross-surface (root cause) | consistency | 5 | 3 | `App.xaml` has 10 brushes, 9 converters, 1 keyed control style total; 6 nav buttons and 8 Preferences cards repeat the same attribute sets verbatim | C8 describes a component vocabulary that exists only as habit — every window is free to reinvent it, and most have | Promote the vocabulary into a real merged ResourceDictionary — CardBorder, PageHeader, SectionHeading, PrimaryButton, SecondaryButton, DialogFooter, AppTextBox [new-mechanism] | clean |
| F-036 | VC-12 | main-window | visual | 3 | 5 | `MainWindow.xaml:992-996` header subtitle pitches the product, `:1808-1814` permanent status-bar tagline — both visible simultaneously **Reconciled 2026-08-09:** clean. Both lines are gone — `MainWindow.xaml:988-1000` is now a comment documenting the removal and why the empty state does not repeat the pitch. Closed sideways by wave 3 (F-010, F-011); caught by the unmerged PR #93, not by this pass's own filters. | The main window explains what the product is twice, in permanent chrome, to a user already looking at their own account list | Pitch belongs on About and the Store listing; recover the header row and footer slot for content that changes | clean |
| F-037 | VC-15 | preferences | visual | 3 | 5 | Accent fill on Send test, "+ Build a theme...", and Close (loudest control on the page) in Preferences; same cyan Close is loudest on About/History/Plugins; Diagnostics reverses it **Closed 2026-08-10 by v1.18 item 9**, verified against the tree: **all nine Close buttons now carry `SecondaryStrongButtonStyle` and zero accent-filled Close buttons remain** — `AboutWindow.xaml:138`, `DiagnosticsWindow.xaml:60`, `FriendFollowWindow.xaml:72`, `GamesWindow.xaml:413`, `SessionHistoryWindow.xaml:62`, `PluginsWindow.xaml:376`, `PreferencesWindow.xaml:806`, `SquadLaunchWindow.xaml:96`, `CaptionColorPickerWindow.xaml:84`. Diagnostics was the precedent and did not change. `IsDefault` and `IsCancel` kept as authored on all nine: which button Enter fires is a keyboard affordance, not a rank. Verified WPF-UI's own Button template has no default-button accent trigger to fight the sweep — zero occurrences of `IsDefaulted` in `Wpf.Ui.dll`. **Re-measured before the sweep and the audit figure had moved:** 5 accent-filled / 4 secondary, not 4/1 — `CaptionColorPickerWindow` had joined the loud side while FriendFollow, Games and SquadLaunch joined Diagnostics on the quiet one. So this was a decide-and-sweep across an almost even split, not a fix-one-window. **Residue named rather than swept:** `SendTestButton :472` and `BuildThemeButton :722` keep their magenta fill. Both are actions their page exists to perform, which is what the fix direction reserves fill for, but both are hand-copied inline recipes rather than a rank — that half belongs to F-068. | Accent fill marks four different things — on 4 of 5 windows the loudest control on screen does nothing | Reserve filled treatment for the action the window exists to perform; dismissal takes the secondary treatment everywhere, as Diagnostics already does | clean |
| F-038 | QF-11 | History | qol | 4 | 3 | `SessionHistoryWindow.xaml.cs:65-72` swallows read/Clear failures to the same empty-state copy | A history file that can't be read presents as "you have never launched anything"; a failed Clear looks identical to a successful one | Distinguish empty/loaded/failed states with distinct copy; Diagnostics already models this correctly (`:29-39`) | open |
| F-039 | QF-13 | Welcome tour / About | qol | 4 | 3 | `WelcomeWindow.xaml.cs:26-38` one-shot sentinel; only documentation of 6 unlabelled row affordances; sentinel marked before the account-count check, so upgrading users burn it silently **Reconciled 2026-08-09:** half shipped with F-001 — the Tools-group entry exists and the sentinel bug named in this row is fixed (`MarkShown()` now sits inside the account-count branch). The **About** entry this row also asks for is still outstanding, which is the only reason it stays open. | The only explanation of RoRoRo's least self-evident controls is shown once, before the user has accounts, then unreachable forever | Add "Show the welcome tour" to the Tools group (QF-21) and to About | open |
| F-040 | CV-10 | row context menu / Theme builder | copy | 4 | 3 | `MainWindow.xaml:302` "🎲 Reroll identity"; `ThemeBuilderWindow.xaml:52` "&#x1F4CB; Copy AI prompt" — the only two emoji-presentation codepoints in the app | Two decorative full-color emoji in monochrome chrome — a live invariant-5 breach the brief wrongly assumed was already enforced | Delete both codepoints, no replacement | closed |
| F-041 | CO-5 | all nine dialogs with a Close button | consistency | 3 | 4 | Nine Close buttons across five paddings — accent-filled/IsDefault (22,8/20,8/14,6) vs bordered secondary/no-default (16,8/14,8) | The most-repeated element in the app has no agreed weight, size, or default-button behavior | One DialogFooter composite owning Close's rank, padding, IsDefault/IsCancel, right-alignment | open |
| F-042 | CO-8 | MainWindow / Preferences | consistency | 3 | 4 | `MainWindow.xaml:1360` Streamer-mode is the only ui:ToggleSwitch; all 6 Preferences booleans are CheckBox **Reconciled 2026-08-09:** count drifted — Preferences now holds **7** `CheckBox` controls, up from the 6 measured at audit. **Closed 2026-08-10 by re-verification.** Zero `ToggleSwitch` elements exist app-wide. The streamer toggle was converted to a `CheckBox` by **F-008**, and `PreferencesWindow.xaml:125-130` says so in a comment. The 2026-08-09 reconciliation counted the seven CheckBoxes and missed that the seventh **is** this defect, resolved. | Two mutually exclusive boolean representations; the setting goal 1 wants relocated is the sole user of the minority one | Pick one boolean control before the move — convert Preferences to ToggleSwitch, or Streamer mode to CheckBox on the way in | clean |
| F-043 | CV-13 | Preferences | copy | 3 | 4 | Six checkbox labels across three grammatical persons — second-person imperative (:46,83,102), first person "my"/"I'm" (:63,139,155) **Closed 2026-08-10 by v1.18 item 8**, verified against the tree. **Both of this row's numbers had moved and both are corrected here.** "Six settings" was right at audit and is **nine** today — F-008's streamer relocation plus items 3 and 4 added three — and the first-person count was **three**, not one, which is what this row's own citations and its fix direction always said. All three are fixed, in the exact words the fix direction wrote by example: `PreferencesWindow.xaml:147` "Launch your main account when RoRoRo starts.", `:669` "Show what you're playing on Discord.", `:685` "Let friends join your server from Discord." — and that last one also gained the terminal period it was missing, which this row never counted because the label was not among its six. The nine labels today are at `:128, :147, :180, :214, :240, :299, :527, :669, :685`; zero first-person pronouns, all terminal periods. `PreferencesCopyTests` pins four rules over labels and hints with a partition assertion so nothing falls through a gap. **Its scope boundary is deliberate and load-bearing:** button content is NOT covered, because a button can legitimately speak in the user's own first person — pressing it IS the user saying the thing, so "I don't have a Discord server" `:475` is correct copy and a blanket ban would need an exemption for the one string that is right. Which leaves "My channel" as the one first-person string still on the page, deliberately unreached: it is a routing destination name that persists as a `Tag` and that `AlertStatusLine` composes around, so renaming it is a vocabulary decision rather than a copy pass. | Six settings, three voices, one page — first person is a different speaker from the rest of the app's "you" | All second person, all terminal periods — "Launch your main account...", "Show what you're playing...", "Let friends join your server..." | clean |
| F-044 | AX-8 | cross-surface | a11y | 3 | 4 | 11px is the default body size app-wide; drops to 10px (helper/chip text), 9px (MAIN pill), 8px (popup badge) | Body content sits under the 12px floor and drops to 8-10px in six places, stacking with the contrast failures | Split roles — prose gets a 12px floor, meta labels keep 11px (short/uppercase/tracked); 8-9px cases rise to the meta floor | open |
| F-045 | AX-11 | main window + row context menu | a11y | 3 | 4 | `MainWindow.xaml:1020` "⚙ Settings" announces as "gear Settings"; star-only button has no name beyond the glyph; `:302` literal die emoji in menu copy | Five glyphs carry meaning only sighted users receive; the star toggle has no accessible name beyond a Unicode shape | Mark glyphs as decoration, carry meaning in a text name; promote the existing tooltip text (:687-691) to the star's accessible name | open |
| F-046 | CO-7 | MainWindow/Games/Plugins/Preferences | consistency | 3 | 3 | "Remove" is quiet bordered secondary on MainWindow/Settings but filled-accent SemiBold on Plugins; accent fill also encodes test/launch/creation elsewhere **PARTLY SHIPPED by v1.18 item 10, and this row does NOT close.** The v1.18 cycle scoped it to close and the tree does not support that, so it stays open with the remainder named — same handling F-001 got when wave 3 left three items outstanding. **Shipped:** `DestructiveButtonStyle` now exists in `Controls/ControlStyles.xaml:106-112`, the rank this row said was missing, and it is assigned by enumeration to exactly three sites — Remove on the account row `MainWindow.xaml:1016`, Clear in History `SessionHistoryWindow.xaml:60`, Stop all `StopAllConfirmWindow.xaml:61`. **The rank carries no hue, and the reasoning is measured rather than aesthetic:** `MagentaBrush` fails in two of four shipped themes (magenta-heat sets Cyan and Magenta to the same value, so a magenta destructive button and a cyan primary render identically; under flatline Magenta is the dark achromatic accent at 3.73:1 against a derived edge already at 3.03:1, and a dim grey outline reads as disabled rather than dangerous), and `RowExpiredAccentBrush` fails the mirror image. So the rank is two non-hue relationships — a doubled edge on `InteractiveEdgeBrush`, the one boundary token `ContrastGuard` derives, measured 3.0699 / 3.0194 / 3.0364 / 3.0258 across the four built-ins; and Bold, one weight step above SecondaryStrong's SemiBold, itself one above secondary's Normal. **Outstanding, measured against the tree 2026-08-10 — this row's own headline comparison still renders.** `Plugins/PluginsWindow.xaml:258-268` Remove is still a hand-rolled `MagentaBrush` fill with `WhiteBrush` and SemiBold, still the loudest control on its screen; the 2026-08-10 re-verification defined the "assigned by consequence" clause by exactly that site. `Games/GamesWindow.xaml:175` and `:287` Remove are quiet `SecondaryButtonStyle`, and `SquadLaunch/SquadLaunchWindow.xaml.cs:224-235` builds a fifth Remove in C# where a style change cannot reach it. **Direction: "Remove" treatments across four surfaces grew 2 to 3** — Destructive on MainWindow, Secondary on Games, hand-rolled accent on Plugins. The cycle held its line on purpose rather than dragging F-068's sites forward, which was the right call; it just is not a closed row. What is left is the assignment pass across the other three surfaces. | Fill weight tracks neither consequence nor frequency — no "filled means important" lesson carries across screens | Define two ranks + one destructive variant in shared button styles; assign by consequence, not by window | open |
| F-047 | CO-9 | Preferences/History/Diagnostics/Plugins | consistency | 3 | 3 | Three grouping systems — filled card, outlined unfilled container (History), no primitive at all (Diagnostics code-behind, 12px CyanBrush vs C8's 13px SemiBold White) | C5's "filled card is the only grouping primitive" isn't true in code — one variant hides in C# with its own spec | One Card style + one SectionHeading style in the shared dictionary; migrate Diagnostics code-behind to consume them | clean |
| F-048 | CO-10 | all 25 windows | consistency | 3 | 3 | MainWindow is the only ui:FluentWindow; ResizeMode spread is arbitrary — 7 windows no-ResizeMode, 12 NoResize (incl. all 8 modals), 2 CanResize; Preferences fixed while sibling Library resizes | Whether a window can resize isn't derived from window kind, and Preferences — goal 2's subject — is the one locked | Two chrome classes: destinations (resizable, MinHeight/MinWidth, maximize) and interruptions (fixed, no maximize); assign every window to one | open |
| F-049 | CO-11 | Preferences | consistency | 3 | 3 | Two ComboBoxes explicitly themed (:114-123,263-270); two newer alert-routing dropdowns (:183-189,199-205) fall through to WPF-UI defaults, diverging under the default theme | The alerts block was added without picking up the theming recipe used twenty lines above it | Fold into the shared dictionary as an implicit ComboBox style so new controls inherit it by default | clean |
| F-050 | AX-6 | every primary CTA | a11y | 3 | 3 | Squad Launch/Send test/MAIN pill (White on Magenta) = 3.79:1 brand/2.99:1 flatline; best theme-derived foreground (navy-on-magenta) only reaches 4.40:1 brand, still under 4.5:1 **Gated 2026-08-09:** `ContrastPairGateTests` now measures this pair under every built-in theme on every test run — brand, midnight, magenta-heat at the time of writing, and flatline since v1.17, which enrols itself because that test iterates whatever `ThemeStore` returns with `IsBuiltIn`. It is the one exemption in that gate, citing this row; `NoExemptionOutlivesItsFinding` deletes the exemption automatically when this row stops being `open`. **Reconciled 2026-08-10:** the 2.99:1 is `flatline-lab`'s, reproduced by `src/ROROROblox.Tests/FlatlineLabGateTests.cs` and asserted there below the exemption's own 3.20 floor. Shipped `flatline` measures **4.68:1** on white-on-magenta, above AA outright, so the exemption is not load-bearing for that theme. **This row does not close.** v1.17 shipped a theme that does not need the exemption; it did not implement the fix direction below, which is the thing that would close it. The two are different claims and only the second one closes a row. Flipping this to anything but `open` deletes the exemption on the next test run and turns brand (3.79:1), midnight (4.16:1) and magenta-heat (3.29:1) red — **all three** pre-flatline built-ins. **Corrected 2026-08-10 by re-verification:** this row, `spec.md` §10.1 and `checklist.md` all said two and omitted midnight. `AaThreshold` is 4.5 (`ContrastPairGateTests.cs:62`) and `spec.md` §4.3 records midnight at 4.16:1 on this exact pair. Only flatline (4.68:1) survives without the exemption. The guard was understating itself by a third, which is the direction that matters most for whoever later decides this row looks cheap to close. | Every filled CTA fails 4.5:1 in at least one shipped theme because foreground is chosen at author time, fill at theme time | Resolve CTA foreground at brush-application time in ThemeService, pick the higher-ratio of the two required slots; pair with a validation step; no new token needed [new-mechanism] | open |
| F-051 | QF-9 | Preferences → Theme | qol | 4 | 2 | `ThemeService.cs:82-91` swallows a persist failure to a log warning, applies live anyway; `PreferencesWindow.xaml.cs:198-206` bare catch{} **Closed 2026-08-10 by v1.18 item 7**, verified against the tree. `ThemeService.SetActiveAsync` now returns a `ThemeChange` (`ThemeService.cs:99`, record at `:366-375`) telling the caller which of three things happened, and the Theme section carries `ThemeStatusLine` at `PreferencesWindow.xaml:786`, composed by `ThemeStatusSummary.ForThemeChange` at `PreferencesWindow.xaml.cs:323`. **Three states, not two.** The third — the theme is gone from the folder entirely — never claims it is on, which a two-state version would have; and a built-in id collision gets its OWN sentence rather than being reported as unreadable, because telling an author their file has a syntax error when the real problem is its name sends them hunting something that is not there. **One deliberate divergence from the pattern this row points at:** the line takes `RowExpiredAccentBrush` and the triangle, not `CyanBrush`. Cyan is the accent, the same treatment a success gets, so a failure painted cyan would look like a save; `RowExpiredAccentBrush` plus the triangle is the warning vocabulary v1.17 established across expired rows, idle chips, memory chips and the compat banner. Success stays silent — but falls back to the folder report rather than blanking, because a bad file is still a bad file after a save that worked. `AlertsStatusLine` itself is untouched and is now its own row, F-094. | If the settings write fails, the user watches the theme apply, sees no message, finds the old theme back after restart | On persist failure, apply live and say so in the Theme section's status line, mirroring the AlertsStatusLine pattern already on the page | clean |
| F-052 | AX-1 | all 26 XAML files | a11y | 4 | 2 | grep for AutomationProperties\|FocusVisualStyle\|TabIndex\|KeyboardNavigation\|IsTabStop across src/ returns zero; 70 unnamed Buttons, 16 unnamed ComboBoxes in the main-window tree | No accessible-naming layer exists in the app's own composition (focus visuals come from WPF-UI per invariant 4, so that limb doesn't independently stand) | A naming rule — no Button/ToggleButton/ComboBox/TextBox ships without AutomationProperties.Name when Content isn't literal text [new-mechanism] | open |
| F-053 | QF-10 | Preferences → Theme → "Open themes folder" | qol | 3 | 2 | `PreferencesWindow.xaml:293` tooltip claims a dropped theme needs a restart; `ThemeStore.cs:42-72` re-enumerates on every ListAsync(), no restart needed; malformed themes drop silently **Closed 2026-08-10 by v1.18 item 7**, verified against the tree — and **this row's own fix direction was false and is corrected here.** The tooltip at `PreferencesWindow.xaml:740` now reads "Drop a *.json file in here, then close and reopen Settings to see it in the picker." Not "reopen this page", which the fix direction and this cycle's PRD and checklist all prescribed and which is equally wrong: the rail's five pages are one window with toggled `Visibility`, so switching pages re-lists nothing. `ThemeStore.ListAsync` re-enumerates per call and `BuildPreferencesWindow` constructs a fresh window per open, pinned by a test that drops a file between two calls. The window is the refresh unit; the page is not. Unreadable files are reported inline through `ThemeStatusSummary.ForFolder` (`PreferencesWindow.xaml.cs:350`) into the same `ThemeStatusLine` F-051 built, and a folder with one bad file among good ones still loads the good ones. **A second instance of this row's own defect, found and fixed the same session and worth recording here because documentation caused it:** `RawTheme`'s doc comment said the on-disk format was camelCase when `JsonSerializerOptions` sets `SnakeCaseLower`. A camelCase key deserializes to null, `ToTheme` throws on the null check, `TryLoadFileAsync` catches and drops the file — so a theme written to that comment's own instructions vanished with no error, which is verbatim this row's complaint. Caught at the C2 walk when a hand-written test theme was rejected; the rejection was correct and item 7's new status line named the file and said it could not be read, which is exactly the behaviour that item shipped. The feature worked, the fixture was wrong, and the comment that made it wrong is now right. No separate row: it is one comment, already fixed, and it is this row's evidence rather than a new finding. | Copy sends users through a restart the code doesn't require; a bad theme file vanishes with no signal | Correct the tooltip to "reopen this page"; report unreadable files inline | clean |
| F-054 | QF-12 | every window | qol | 3 | 2 | grep for KeyBinding\|InputBindings\|Gesture across all XAML/CS returns zero; `CookieCaptureWindow.xaml` has no Button element at all, so Esc doesn't dismiss it | RoRoRo has no keyboard shortcuts, and the modal a first-run user is guaranteed to hit can't be dismissed with Esc | Add window-level InputBindings for the handful that matter; give CookieCaptureWindow a Cancel with IsCancel=True [new-mechanism] | open |
| F-055 | VC-3 | main-window | visual | 2 | 3 | All six nav destinations use MutedTextBrush at 11px, same role as helper prose — but the FPS-banner body is also MutedTextBrush/11px, so nav is at parity, not quieter **Closed 2026-08-10 by re-verification.** The nav band is two controls, `MainWindow.xaml:1144` and `:1161`, both on `SecondaryButtonStyle` resolving to `WhiteBrush`. `MutedTextBrush` is off the nav band entirely and `MutedTextFenceTests` now fences that token to prose. | Navigation shares its text role with de-emphasized prose (the "quieter than" comparative doesn't hold against the brand capture) | Give destinations a content-label foreground role; let grouping (container/rule/rail) carry the "chrome not content" distinction | clean |
| F-056 | VC-16 | main-window | visual | 2 | 3 | Gear glyph on Settings (:1020) is the only glyph among six otherwise-identical peer nav buttons; 8 ad-hoc glyphs vs one real WPF-UI SymbolIcon **Closed 2026-08-10 by re-verification.** The gear is gone. `MainWindow.xaml:1144` reads `Content="Settings"`. This row's fix direction was scoped to the nav-band gear only, and that surface no longer exists. Note the row's citation pointed at `:1020`, which is stale twice over. | One decorated button in a row of six reads as an accident, not a hierarchy signal | Standardize on WPF-UI's symbol set; decide icons per group, not per button (in-scope: nav-band gear only) | clean |
| F-057 | VC-17 | main-window | visual | 2 | 3 | MainWindow alone uses 11px x33, 12px x11, 10px x10, 9px x4, 8px x2 — nine sizes with no ladder | Nine type sizes, no ladder — but the dominant 11px body size (148 uses app-wide) is the app's own established default, so a hard "12px floor" overreaches | Collapse to a four-step ladder (display/heading/body/meta); reserve the smallest step for uppercase meta labels, per C8 | open |
| F-058 | VC-18 | main-window | visual | 2 | 3 | `MainWindow.xaml:1353-1385` Streamer-mode toggle in column 0, explanation in column 2 right-aligned, ~250px gap; duplicates the toggle's own ToolTip **Closed 2026-08-10 by re-verification.** The streamer band is gone from `MainWindow.xaml` and the relocated control at `PreferencesWindow.xaml:131-155` already ships this row's exact fix, helper prose beneath the control at `Margin="22,4,0,8"` with the tooltip duplication removed. Fixed by **F-008**. | Explanation isn't spatially bound to its control (though it is already subordinate by size, 10px) | Put helper prose directly beneath its control, indented, as Preferences already does (Margin=22,4,0,0) | clean |
| F-059 | CO-15 | cross-surface, iconography | consistency | 2 | 3 | ui:ImageIcon appears once (title bar); everything else is a literal text-run glyph; of six nav buttons only Settings carries one | A single decorated button in a row of six reads as an accident rather than a hierarchy signal | All six nav entries take an icon from one source or none do; decide as part of any Tools-container restructure | open |
| F-060 | QF-8 | main window, size and placement | qol | 2 | 3 | `MainWindow.xaml:9-11` hardcodes Height=600 Width=900, CenterScreen; nothing reads/writes placement; compact-restore fallbacks are unreachable in normal operation | A user who resizes the window gets it back at 900x600 centered every launch | Persist width/height/left/top + maximized flag alongside the QF-7 compact flag, with an on-screen-bounds check on restore | open |
| F-061 | CV-16 | Add Roblox account (cookie capture) | copy | 2 | 3 | `CookieCaptureWindow.xaml:19` "Add Roblox Account" (Title Case) vs `:6` "Add Roblox account — log in" (sentence case) vs `MainWindow.xaml:1000` "+ Add Account" — three casings on screen at once | One action, three casings, all visible during the same task | Sentence case throughout — button "+ Add account", title bar "Add account", header "Add a Roblox account"; drop "— log in" | open |
| F-062 | CV-19 | Preferences — run-on-login card | copy | 2 | 3 | `PreferencesWindow.xaml:52` "Adds a value under HKCU Run. Removes it when unchecked." — first helper line of the page, only string assuming Windows-internals literacy **Closed 2026-08-10 by v1.18 item 8**, verified against the tree. The hint is now at `PreferencesWindow.xaml:134` and reads "This applies to your Windows account only, and it does not start Roblox: that is the setting below. Unchecking this undoes it completely." No registry path; the label at `:128` already states the effect, so the hint carries only what the label cannot — scope, boundary, reversibility. Both claims verified in source rather than assumed: `StartupRegistration` writes to `Registry.CurrentUser`, and Disable deletes the value outright. **One half of this row's verdict does not hold and was nearly repeated as fact:** "first clause duplicates the checkbox label above it" is false textually — "Adds a value under HKCU Run" and "Start RoRoRo when Windows starts." share no wording. The duplication was semantic. The load-bearing half, a registry path being the wrong first read for a clan audience, is what got fixed. | The registry path is implementation, not effect — wrong information for a non-technical clan audience's first read; first clause duplicates the checkbox label above it | "Unchecking this undoes it completely." (trimmed to the reassurance the checkbox label doesn't already give) | clean |
| F-063 | VC-6 | about | visual | 2 | 2 | `AboutWindow.xaml:13-20` 8 literal SolidColorBrush resources; `:34` unbound canvas; `:96` `#15263A` = RowBgBrush's literal value, not bound; flatline shows a hard dark rectangle behind the icon | About opts out of the theme system — but the logo's own brand hex must stay fixed per invariant 2, so only the canvas background and card binding are fixable | Bind the card to RowBgBrush (already that value), drop the canvas background so the hero sits on the page field; logo tint steps stay fixed hex | open |
| F-064 | VC-8 | diagnostics | visual | 2 | 2 | `DiagnosticsWindow.xaml.cs:90-95` section headers at 12px SemiBold CyanBrush vs C8's 13px SemiBold WhiteBrush — real code-behind deviation, though flatline still shows the 4 headings clearly distinct in accent | Diagnostics defines section heading a third way, in C# where no markup review would catch it | Move the heading to the established 13px SemiBold body-foreground treatment; add non-color group separation | clean |
| F-065 | VC-10 | history | visual | 2 | 2 | `SessionHistoryWindow.xaml.cs:150-155` rows built on RowBgBrush alone, vanish under flatline — though the date-group heading (10px SemiBold Cyan) does survive, contra the original "no bucketing" claim | Row-card boundaries carry meaning and vanish under flatline together; the bucketing half of the structure does survive via accent color | Give the row a non-fill boundary (baseline rule or fixed leading rhythm) so "which session is which" holds without fill | open |
| F-066 | CO-2 | Modals/CookieCapture vs rest (in-scope residue) | consistency | 2 | 2 | 62 hardcoded hex duplicate an existing brush, mostly in out-of-scope modals; in-scope residue is `MainWindow.xaml:1474,1477` (mutex-recovery banner) and `AboutWindow.xaml:96` (#15263A = RowBgBrush) **Reconciled 2026-08-10: citation stale, count low, defect intact.** The mutex-recovery literals sit at `MainWindow.xaml:1572,1575` today, and they had already drifted to `:1479,1482` on `main` before the v1.17 branch existed, so this is pre-existing drift rather than branch churn. The same banner carries a **third** literal this row never counted — `Foreground="#F1B232"` at `:1568`. `AboutWindow.xaml:96` still holds exactly as written. Two further un-themed literal sites in the same file were given their own rows rather than folded in here, because their fixes belong elsewhere: F-085 and F-088. | Most of the 62-count sits out of scope; residue is three literals that should bind existing brush keys | Replace the three in-scope literals with their existing brush keys | open |
| F-067 | CO-14 | cross-surface, type | consistency | 2 | 2 | Mono role declared two ways — fallback stack (16 sites) vs bare "Consolas" (7 sites, 3 in-scope); `RenameWindow.xaml:27` comment confirms the stack is the intended contract; zero display-family declarations app-wide | Seven meta-label sites skip JetBrains Mono where installed; the display role (Space Grotesk) has no expression anywhere and must not be abandoned per invariant 3 | One MonoFontFamily resource referenced everywhere; ship and reference a display-family resource (the "declare it intentionally system-font" branch is refused) | open |
| F-068 | QF-15 | every button in the app | qol | 2 | 2 | 115 Button declarations, ~73 set Background inline; only keyed button style app-wide is CyanCtaButton; App.xaml's only control style is a ToggleButton style **Re-counted 2026-08-10, and the number is FLAT.** 61 un-migrated button call sites across 24 files. Measured with one scanner at three commits: pre-wave-5 (`bf1fdef^`) 101 buttons / 0 styled / **96** hand-copied; at `bf1fdef` (2026-08-05, the commit that wrote this file's own "63 across 15 files" comment) 103 / 34 / **61**; at HEAD 105 / 36 / **61**. Wave 5 migrated 35 in a single pass and **nothing has moved in the five days since**. The "63 across 15 files" figure reproduces at neither snapshot and should be treated as unsourced. **PARTLY SHIPPED, and the two halves are very uneven.** `Controls/ControlStyles.xaml` ships `PrimaryButtonStyle`, `SecondaryButtonStyle` and `SecondaryStrongButtonStyle` (not `Tertiary`), merged at `App.xaml:15`. But the **template-trigger half is 0% shipped** — zero `ControlTemplate` and zero `Style.Triggers` in that file, so hover and pressed still come from WPF-UI's dictionary, which `ThemeService` never touches. The row's literal clause *"App.xaml's only control style is a ToggleButton style"* is **still true of App.xaml itself** (`SelectionDotStyle:61` is its only `Style`), so a reader who does not open `Controls/` concludes nothing shipped. The 61 break down as **22 cyan CTAs with no style proposed for them at all**, 8 magenta CTAs, 13 raw-hex recipes, 18 other; `MainWindow.xaml` alone holds 19. Across buttons plus inputs plus borders it is 121 sites in 26 files — inputs are 11 of 11 done, borders 60 of 76 still hand-themed. `CyanCtaButton` also survives at `PluginsWindow.xaml:23` and 3 more sites. **Re-counted 2026-08-10 after v1.18, and the number finally MOVED: 61 to 55 un-migrated button call sites, 24 to 22 files.** Six, and the accounting matters more than the six. **Five came from item 9's Close sweep, and the direction is the point** — those nine Close buttons adopted `SecondaryStrongButtonStyle`, an existing rank, rather than hand-copying attributes inline to reach the same look. Hand-copying would have ADDED four new recipes, which is this row's actual defect, so a sweep that moved the counter down instead of up is evidence the rank vocabulary is doing its job. The sixth is Stop all giving up its raw `#F22F89` fill and `#FFFFFF` foreground when it took `DestructiveButtonStyle`. **The delta was reproduced independently at the branch point and at HEAD with one scanner: minus 6 sites, 24 files to 22, `AboutWindow.xaml` and `SessionHistoryWindow.xaml` leaving the offender list entirely.** **This row does NOT close.** 55 sites is still 55, the template-trigger half is still 0% shipped — zero `ControlTemplate` and zero `Style.Triggers` in `Controls/ControlStyles.xaml`, so hover and pressed still come from WPF-UI's dictionary, which `ThemeService` never touches — and `App.xaml`'s only `Style` is still `SelectionDotStyle`, so a reader who does not open `Controls/` still concludes nothing shipped. Five days of flat, then six in one cycle, is a rate, not a fix. | The primary/secondary distinction the brief describes is dozens of hand-copied attribute sets with no shared style behind them — same root cause as C6's flatline collapse | Define PrimaryButtonStyle/SecondaryButtonStyle/TertiaryButtonStyle in App.xaml with template-level triggers bound to DynamicResource brushes [new-mechanism] | open |
| F-069 | QF-17 | account row context menu | qol | 2 | 2 | `MainWindow.xaml:302` "🎲 Reroll identity" — the sole emoji hit in a targeted app-wide grep; sibling menu items carry no glyph **Closed-as-ruled 2026-08-10 by re-verification.** This row's fix direction is to drop the glyph. The **Rulings by the user** section closed that entire class on 2026-08-04, and `CLAUDE.md` cites the same ruling's other victim, F-040, as its worked example of *rulings override rows*. F-040 sat open five days against a decision already made; this one sat open six. Second instance of the identical failure. | Exactly one emoji survives in shipped copy, against a rule the brief assumed was already enforced | Header="Reroll identity" — drop the glyph, matching glyphless siblings | closed |
| F-070 | CV-14 | Join by link | copy | 2 | 2 | `JoinByLinkWindow.xaml:27-33` cyan+white, no magenta, vs 12 siblings pairing Cyan/Magenta/White; `WelcomeWindow.xaml:38-43` also ships half the duo — a second instance the original finding missed | Two windows ship the header device with half the 626 Labs duo — invariant 2, broader than originally counted | Resolved for free by CV-1's chrome rule; if deferred, restore the magenta " / " run in both windows | open |
| F-071 | CV-17 | tray menu / Diagnostics | copy | 1 | 3 | "Multi-Instance" surfaces in 8 places (tray tooltip x3, tray menu x3, `:203`, Diagnostics row + bundle line), not the 2 originally claimed | A clan member can't map "Multi-Instance: OFF" to anything they did — but renaming across 8 surfaces is a product decision the finding underprices | If undertaken, rename consistently across all 8 sites to "Side-by-side clients" — not a single-string fix | open |
| F-072 | AX-13 | History | a11y | 2 | 1 | 503 unpaired Text nodes, 100 sessions, zero DataItem/List containers; only two real focus stops (Clear history, Close), both terminal | History reads to assistive tech as ~500 loose strings with no session boundaries; its total lack of interactive surface is placement evidence for QF-19's "tool not preference" conclusion | A repeated five-field record is a table — give each session a row container whose name composes the fields (placement evidence only, not a rebuild this wave) | open |
| F-073 | AX-15 | main window, FPS-cap banner | a11y | 2 | 1 | UIA tree shows Button 'Dismiss' declared before the banner's own text; visual right-alignment via DockPanel.Dock means a sighted user sees nothing wrong | A screen-reader user is offered "Dismiss" before being told what there is to dismiss | Declare banner content before the dismiss action in markup; name what each Dismiss button dismisses | open |
| F-074 | CV-18 | Stop all confirmation | copy | 1 | 2 | `StopAllConfirmWindow.xaml:36` "UNSAVED GAME STATE WILL BE LOST" in the JetBrains Mono 10px uppercase label style at #5A6982, the dimmest text on the surface | Shape says "label," words say "you will lose work" — the only warning in a destructive confirmation dressed as a section header | Move into the body as ordinary 12px prose: "Anything not saved in those games is lost." Reserve mono-uppercase for genuine labels | open |
| F-076 | (wave 2) | Settings -- Alerts & memory | qol | 3 | 3 | Wave-2 flatline capture of the Alerts page rendered both webhook URLs in full plaintext, including the token segment, in a screenshot taken by a scripted capture with no consent step. Capture deleted; `docs/ui-evidence/` is gitignored, so nothing was committed | A Discord webhook URL is a bearer credential — the token is the whole auth. This app ships a streamer mode whose entire job is to keep identity off a live stream, and Settings > Alerts puts a working credential on screen at full contrast. A stream, a screen-share, or a support screenshot leaks post-access to the user's clan channel. Found by this wave's own tooling, which is the proof that it is easy to hit | Mask the saved value by default (show scheme + host + last 4, e.g. `https://discord.com/api/webhooks/...BeqXP`) with an explicit reveal; keep the field editable by paste-over. Pairs with the streamer-mode work already in the campaign | clean |
| F-077 | (wave 5 gate) | main-window per-account row | a11y | 3 | 3 | `MainWindow.xaml:657` — the caption-colour swatch is a `Border` with `Cursor="Hand"`, a `MouseLeftButtonUp` handler and a ToolTip, still bound to `DividerBrush`: 1.26:1 in brand | A UI component under WCAG 1.4.11 that looks like decoration in the markup. Wave 5's fence originally hard-listed `Border` as decorative and would have failed the build on this exact fix; the fence is now role-based, so the fix is unblocked but was not taken | Bind `InteractiveEdgeBrush`. One attribute; the fence already permits it | clean |
| F-078 | (wave 5 gate) | Settings -- Appearance | qol | 2 | 2 | Once the edge question is answered for a theme, `AppSettings.EdgeRemediationAnswers` holds it forever; nothing in the UI re-opens it. `EdgeRemediationWiringTests` notes the store supports re-answering, but no control calls it **Closed 2026-08-10 by v1.18 item 5**, verified against the tree. `ReviewEdgeButton` at `PreferencesWindow.xaml:755-763` on the Appearance page, handled at `PreferencesWindow.xaml.cs:1084`, which calls `ThemeService.ReopenEdgeQuestion()` and then puts the same question through the same `EdgeRemediationWindow.AskIfPendingAsync` and records the answer through the same existing setter. It reads "Review button outlines" rather than "ask me again" on purpose: Story 1.4 requires it to be discoverable WITHOUT knowing the prompt ever happened, and "again" is a word only somebody who saw the first one can parse. **A limit on this fix, recorded rather than glossed:** `EdgeRemediation.Decide` returns `DeriveSilently` for every `IsBuiltIn` theme by design, so brand, midnight, magenta-heat and flatline all correctly report "nothing to choose" through the explanatory dialog at `:1088-1096`. Exercising the real path needs a hand-authored theme whose divider measures under 3:1 against its navy, so the affordance is unverifiable on a default install. Not a defect; a testing note for whoever walks it next. | A consent prompt with no way back is a one-shot decision made in the first ten seconds of a launch. Someone who declined to get on with playing has no route to change their mind short of editing settings.json | A "re-ask about my theme" affordance on the Appearance page, calling the existing setter | clean |
| F-079 | (wave 5 gate) | Modals/ (6 windows) | consistency | 2 | 3 | Twelve `Background="#..."` literals across `StopAllConfirmWindow`, `LeftoverProcessesWindow`, `RobloxAlreadyRunningWindow`, `RobloxNotInstalledWindow`, `WebView2NotInstalledWindow`, `RenameWindow`; none uses the three styles wave 5 created | These modals opt out of theming on their buttons, so under a user theme they render brand colours on a themed field. `EdgeRemediationWindow` is the only window in that folder speaking the new vocabulary **Re-counted 2026-08-10 after v1.18 item 10: `Background="#..."` literals 12 to 11, and `Modals/`'s total hex occurrences 54 to 52.** `StopAllConfirmWindow`'s Stop all took `DestructiveButtonStyle` and gave up both its raw `Background="#F22F89"` and its `Foreground="#FFFFFF"`, so that file goes 2 to 1 on the `Background` count and 7 to 5 on total hexes. `ThemedStatusColourTests`' `AllowedXamlLiteralCeiling` moved 97 to 95 with them, on the standing rule that slack left inside an allow-listed region is room for a new literal to arrive wearing an old row's badge. **This row does NOT close** — its other six modals are untouched. Current per-file `Background="#"` split: `RobloxAlreadyRunningWindow` 4, `LeftoverProcessesWindow` 3, `StopAllConfirmWindow` 1, `DpapiCorruptWindow` 1, `RobloxNotInstalledWindow` 1, `WebView2NotInstalledWindow` 1. **Two file-set errors from the 2026-08-10 re-verification, written back here because nobody did:** `RenameWindow.xaml` is named by this row and holds ZERO `Background="#"` (its only hex is a `BorderBrush` at `:67`), and `DpapiCorruptWindow.xaml` holds one this row never names — `ThemedStatusColourTests.cs:313-318` already knew that half and it was never carried across. Style-adopting windows in `Modals/` also grew 1 to 2: `LaunchHeadroomWindow.xaml:56,61` arrived with PR #96 after this row was written. | Convert to `PrimaryButtonStyle` / `SecondaryStrongButtonStyle`, keeping the deliberate magenta on destructive confirms | open |
| F-075 | CV-12 | cross-surface, row controls, empty states, Welcome | copy | 0 | 4 | `MainWindow.xaml:131,334,700,704,578,1141` — all Emoji_Presentation=No text glyphs; `WelcomeWindow.xaml:123,130` teaches ☰/★ by showing the same glyphs | These are functional glyphs, not decoration — sole content of controls too small for a label, or state indicators the Welcome tour teaches by name | No change — recorded as a deliberate keep so a later pass doesn't strip them; ▶ Start (:1682) flagged as the one marginal case | open |
| F-080 | (field request 2026-08-07) | main-window status bar | qol | 3 | 4 | Status bar reads "8 Roblox clients running" and says nothing about what they cost. During the 733 investigation the answer — 23.0 GB across 8 clients — was only obtainable by running a PowerShell probe against the process list | The number a multi-client user actually needs to decide whether to launch another alt is total RAM in use, and today it lives nowhere in the app. The memory watchdog already samples exactly this every 15s; it is surfaced only as a warning banner after the fact, never as a running figure | Add total private bytes across tracked clients to the status bar, between the client count and Compact. Reuse `MemoryWatchdog`'s existing samples — no new probe. Este's request, from live use | clean |
| F-081 | (investigation 2026-08-07) | `RobloxProcessTracker.cs:385` | qol | 4 | 2 | The exit log line carries pid + accountId and nothing else: no exit code, no last-known private bytes, no session duration. **CORRECTED 2026-08-07:** the original wording of this row said we "record NOTHING when a client ends," which was false — the exit IS logged, and that log line is what proved the 733 updater-kill. What is missing is the WHY, not the event | Distinguishing a user close from a crash from an external termination is impossible from our one line. On 2026-08-07 this produced a WRONG conclusion, not just a slow one: three exits were read as Roblox's updater killing clients when the user had closed them himself, and nothing in our logs could tell the two apart. A reason on the exit line would have made that error impossible | Enrich the existing line — exit code, last private-bytes reading, session duration. One log statement, not a new teardown path. Add a distinct line when several clients exit inside a short window, which is the updater-kill signature | clean |
| F-082 | (investigation 2026-08-08) | `MemoryDefaults.cs:27` + `MemoryWatchdog.cs:212` | qol | 5 | 3 | The watchdog has exactly two triggers and neither can fire for an oversubscribed multi-client user. (1) Per-client cap = 35% of installed RAM, floored at 4 GB — on 16 GB that is 5734 MB, and a Roblox client peaks around 3300 MB, so it never crosses at any tier at or above 16 GB. (2) Projection needs `aggregateGrowth > 0`; once clients plateau, `hasProjection` is false and it cannot fire either. There is NO absolute low-headroom check. Measured 2026-08-07: per-client median 2650 MB, peak 3280 MB. Ten accounts need ~26.5 GB; a 16 GB machine has ~12.9 GB usable — oversubscribed by 13.6 GB, with RoRoRo silent throughout | `852bc66` (v1.12.0.0) replaced a FIRING cap with one that cannot fire. The v1.11.1 logs from 2026-08-01 show the old fixed cap crossing repeatedly at 2640 MB; after the change it is 16886 MB on the same machine. Scaling a PER-CLIENT cap with installed RAM is the wrong axis for a multi-client app — the risk is aggregate, not per-client, and the more RAM you have the higher the cap gets. This does not cause drop-outs; it is why nobody is warned before one. Users on less RAM hit the wall first and get the least notice | Add an absolute headroom trigger — warn when available memory drops below the reserve, independent of growth. Make aggregate footprint the primary signal. Pairs with F-080, which puts the running total on screen so the user can see it coming | clean |
| F-083 | (follow-up 2026-08-09) | `MemoryDefaults.ExpectedClientMb` | qol | 4 | 2 | The pre-launch advisor and the fits-how-many arithmetic both consult a constant — 2650 MB — measured on ONE machine (47 GB, Pet Sim 99, Roblox 733). Hardware, game and client build are all variables. Meanwhile `MemoryWatchdog` samples every tracked client's private bytes every 30 seconds and has this machine's real answer in hand | A threshold tuned to the wrong number either cries wolf or never fires, and both end with the warning being ignored. Este's framing: it should learn. THE TRAP that makes this non-trivial — under memory pressure Windows trims working sets, so `PrivateMemorySize64` reads LOWER exactly when the user is in trouble; learning from those samples shrinks the footprint, quiets the warning, and does so BECAUSE things got worse. A feedback loop that disables the alarm in proportion to the fire | Learn p75 of SETTLED clients (reuse the existing `MinimumObservation` growth gate), discard every sample taken while `BelowReserve`, clamp to [1200, 5000] MB, seed 2650 until 20 samples, reset on a client-version change. Feeds the advisor only — NOT the anomaly cap (a heavy machine would teach itself that heavy is normal, reintroducing F-082 by another route) and not the headroom trigger (that is a measurement, not an estimate). Scoped: docs/superpowers/specs/2026-08-09-rororo-adaptive-footprint-scope.md | open |
| F-084 | (F-001 review, 2026-08-09) | cross-surface — every owned dialog | qol | 3 | 4 | Eight owner-resolution sites gate on `owner.IsLoaded` alone before assigning `dialog.Owner`: `LaunchHeadroomWindow.xaml.cs:74`, `EdgeRemediationWindow.xaml.cs:94`, `ConsentSheet.xaml.cs:46`, `App.xaml.cs:984`, `:1008`, `:1594`, `:1622`. `MainWindow.xaml.cs` cancels close and calls `Hide()` (the normal minimize-to-tray), and **`Hide()` does not unload a window, so `IsLoaded` stays `true` while `IsVisible` goes false**. F-001 fixed exactly this in `StopAllConfirmWindow.Confirm` and `WelcomeWindow.ShowTour` by adding `&& owner.IsVisible`; the other eight were out of that finding's scope. **Re-counted 2026-08-10, and the number was wrong in both directions.** The headline says eight sites; the row's own enumeration lists seven; the tree has seven, all citations resolving verbatim. Worse for scoping: **four of the seven call `SurfaceMainWindow(owner)` on the following line**, which `Show()`s and un-minimises the owner before `ShowDialog()` (`App.xaml.cs:2060-2071`, in tree since May 2026). **Real exposure is three, not eight** — scoping eight one-line fixes buys four no-ops. Both errors predate this row. And it misses four genuinely unguarded `Owner` assignments at `MainViewModel.cs:3597, :3650, :3657, :3664` that no row owns. | Any tray-invoked dialog raised while RoRoRo is minimized becomes an owned child of a window the user cannot see, so dismissal returns activation to an invisible owner instead of falling through. `StopAllConfirmWindow.xaml:13` documents the intended behaviour — “CenterScreen (no owner) — the tray can fire this with the main window hidden” — which the `IsLoaded`-only guard silently defeats. | Add `&& owner.IsVisible` at each site. Better: extract the nine-line resolve-and-parent block (now duplicated across all of them) into one helper so the rule lives once — the spec for F-001 originally called for passing the owner in as a parameter, which is the shape that makes this collapsible. Also drop the now-stale “Owner resolution matches `LaunchHeadroomWindow.ShouldProceed`” claim at `StopAllConfirmWindow.xaml.cs:33`, true before F-001 and false after. | open |
| F-085 | (v1.17 flatline, spec §7) | main window, Bloxstrap warning banner | visual | 2 | 2 | `MainWindow.xaml:1630-1644` holds **three** un-themed literals, not the two spec §7 names: `Background="#3F3000"`, `BorderBrush="#8F7000"`, and the body `Foreground="#FFE3A6"`. None binds a brush key, so the banner renders the same warm amber under all four built-in themes, including `flatline`, whose field is `#101010` and whose entire argument is that no hue carries meaning. **Corrected 2026-08-10 by re-verification, and this row was falsified by its own cycle.** The literal count is exact — three, at `MainWindow.xaml:1630`, `:1631`, `:1644`. The evidence claim is not. This row says the literals are invisible to both fences and that nothing would catch a fourth being added; item 3a of the same v1.17 cycle then shipped `ThemedStatusColourTests.NoColourLiteralIsWrittenIntoAppXamlOutsideTheAllowList` (`:383`), which walks App XAML, allow-lists this banner **by this row's own finding id** (`:290-295`), and enforces `AllowedXamlLiteralCeiling`. A fourth literal in this banner now fails that ceiling. **Ceiling re-measured 2026-08-10 after v1.18 item 10: 95, down from 97**, independently re-counted at HEAD as App.xaml 11 + MainWindow.xaml 8 + AboutWindow.xaml 10 + CookieCaptureWindow.xaml 14 + `Modals/` 52 = 95 exactly. The two that left are F-079's, not this row's — Stop all's raw fill and foreground going onto the governed path. This row's own three literals are untouched and still sit on the allow-list. **Written by item 6 and invalidated by item 3a, same day, same cycle.** Also: this row's fix direction points at F-068's shared banner recipe, and F-068 has **no banner primitive at all** — that half is 0% shipped, not partial. | A colour-only banner that opted out of the theme system and stayed out. Not a legibility failure — amber on brown clears AA — but it is the one warm block left on the main window under a theme that has no other hue, and it is invisible to both fences v1.17 shipped: `ThemedStatusColourTests` walks `*.cs` and this is XAML, `ContrastPairGateTests` reads `{DynamicResource}` attributes and this is a raw hex. A literal matches neither pattern, so nothing would catch a fourth one being added tomorrow. | Bind a themed banner recipe rather than a one-off. Belongs with **F-068**'s shared button/banner style work — the mutex-recovery banner (F-066) and the compat banner are the same shape, and fixing one by hand is how three near-copies became three. Found during v1.17's `/spec` recon and deliberately left out of that cycle. | open |
| F-086 | (v1.17 gate audit) | `ContrastPairGateTests` / every `MutedTextBrush` binding | a11y | 3 | 1 | The gate measures only elements declaring both `Background` and `Foreground` inline as `{DynamicResource}`. Scanned against `HEAD` with the gate's own regexes on 2026-08-10: **44 elements, 8 distinct pairs**, and `MutedTextBrush` is the foreground of none of them. It used to be. Commit `2c9ab16` (PR #100, F-032's own fix) rebound three `MutedTextBrush` foregrounds to `WhiteBrush`, merging `MutedTextBrush on NavyBrush` into the existing `WhiteBrush on NavyBrush` — same 44 elements, one fewer pair. The gate's three in-code claims of "9 distinct pairs" describe an app that has not existed since that merge; corrected in the same PR as this row. **Re-measured 2026-08-10: 105, grew by 1.** **Re-measured again at the end of v1.18: 113, grew by 8** — this cycle's own new hint text added them, counted as `Foreground="{DynamicResource MutedTextBrush}"` bindings in App XAML at the branch point (105) and at HEAD (113). Recorded because a fence that cannot see a token gets blinder as the token spreads, and this gap widened inside the very cycle that closed thirteen rows beside it. The same figure in `ContrastPairGateTests`' doc comment was corrected at item 12; this copy fell between item 11, which owns this file but not `src/`, and item 12, which owns `src/` but not this file. The scan was reproduced independently with the gate's own regexes — 44 elements across 18 files collapsing to 8 pairs, with `MutedTextBrush` the foreground of none of them, exact. **Re-measured again 2026-08-10 after v1.18 items 9 and 10, and the element count MOVED: 39 elements across 16 files, down from 44 across 18. Pairs are unchanged at 8.** The nine swept Close buttons stopped declaring `Background` and `Foreground` inline when they took `SecondaryStrongButtonStyle`, so they left the gate's field of view — `AboutWindow.xaml` and `SessionHistoryWindow.xaml` dropped out of the scanned set entirely. `MagentaBrush` on `WhiteBrush` is still 8 sites, so F-050's exemption measures exactly what it did. **This is the row's own thesis arriving again, from the opposite direction:** a fix that is unambiguously good shrank the gate's coverage by 5 elements and 2 files, `MinimumElements` is 30 so nothing failed, and nothing announced it. **Stale in code and not corrected here, because item 11 does not touch `src/`:** `ContrastPairGateTests.cs:65`, `:196`, `:205` still quote 44 as the current measurement. Those three are item 12's or the next PR's, and a stale number inside a fence's own message is this defect one level down. | F-032's fix removed the only declared pair whose foreground was the prose token, so a token with roughly 104 bindings is now measured by nothing on any test run. `MinimumPairs` is 6, so the drop from 9 to 8 failed nothing and announced nothing. A gate that loses coverage silently is worse than one that never claimed it — this is the same defect class as a stale register row, wearing a test's clothes. | Measure a small named-pair list unconditionally — `MutedTextBrush` vs `WhiteBrush` and `MutedTextBrush` on `RowBgBrush` at minimum — so the numbers `spec.md > §4.3` states by hand get re-derived on every run instead of once, in prose. The inherited-fill composition problem the gate correctly refuses to guess at does not apply to a fixed pair somebody names deliberately. | open |
| F-087 | (v1.17 fence, item 3) | Plugins — consent sheet | visual | 1 | 1 | `Plugins/ConsentSheet.xaml.cs:90-92` — `CapabilityRow.NamespaceBrush` returns `(Brush)(Application.Current.TryFindResource("CyanBrush") ?? new SolidColorBrush(Color.FromRgb(0x17, 0xD4, 0xFA)))` when the capability is host-enforced, and the `MagentaBrush` / `0xF2, 0x2F, 0x89` equivalent when it is not. `ConsentSheet.xaml:92` binds it as `Foreground="{Binding NamespaceBrush}"`. Surfaced by v1.17 item 3's colour-literal fence, which spec §7 never named; it sits on that fence's allow-list at `ThemedStatusColourTests.cs:84-89` with its reason inline, and this row is what makes that entry traceable to a finding instead of a silent permission. | **Fallback-only reach, stated at its real size.** `TryFindResource` returns the themed brush whenever `App.xaml`'s dictionary is loaded, which is every path a user can reach, so in a running app these literals paint nothing. They are still two brand hexes hardcoded in App code, the same defect class as F-085, and a `??` fallback is the shape that survives a palette change unnoticed precisely because nothing ever renders it. Small, real, and worth a row rather than an allow-list entry nobody can trace. | Move the branch into XAML: a `Style` with a `DataTrigger` on `IsHostEnforced` setting `Foreground` to `{DynamicResource CyanBrush}` or `{DynamicResource MagentaBrush}` — the same idiom v1.17 item 3 used to replace the two status converters, so the sheet stops resolving brushes in a model class at all. | open |
| F-088 | (v1.17 reconciliation) | main window status bar | visual | 2 | 3 | `MainWindow.xaml:1873-1888` — the live-process dot's `Fill` is a literal `<SolidColorBrush Color="#4FE08C" />` inside a `Setter.Value`, with a `DataTrigger` swapping to `#4A5C70` at zero clients. Shipped alongside F-080 in PR #96. Present on `main` at `:1776` before the v1.17 branch was cut, so it is not a regression from that cycle — it is a site that cycle did not know about. **Closed 2026-08-10 by v1.17 item 3a, added at `/build` rather than deferred.** Both literals are gone: the `Ellipse.Style` at `MainWindow.xaml:1871-1895` now sets `Fill` to `{DynamicResource WhiteBrush}` with a single `DataTrigger` on `LiveProcessCount == 0` setting `{DynamicResource MutedTextBrush}`. The arrangement is inverted from the per-row dot — live in the `Setter`, zero in the trigger — because `DataTrigger` matches on equality and the bound value is an `int`, so "zero" is expressible and "any number of clients" is not; inverting it would have cost a converter or a new view-model bool, which is the machinery §5.2 deleted. **The artifact that proves it** is `ThemedStatusColourTests.NoColourLiteralIsWrittenIntoAppXamlOutsideTheAllowList`, a third fact on item 3's fence that scans App **XAML** for literal `#RRGGBB` outside an allow-list. It was written first and watched fail on exactly `MainWindow.xaml:1877` and `:1884`, then watched pass. Its allow-list is the durable part: **a literal is permitted only when an OPEN register row already owns it**, each entry citing the id inline — F-085, F-066, F-079, F-063 and F-089 all appear there, and `App.xaml`'s brush dictionary is the one entry with no finding because it is the governed path's origin rather than an escape from it. That rule is what closes the hole this row is really about: not one dot, but that a sixth site could have hidden the same way. | The fifth status-colour site, and the one `spec.md > §5.3` never enumerated. v1.17 rebound four — row dot, idle chip, row memory chip, compact memory chip — and left this one, so under flatline the status bar still shows brand green while every row above it is achromatic. Both fences are structurally blind to it for different reasons: `ThemedStatusColourTests` walks `*.cs` and this is XAML, `ContrastPairGateTests` reads `Background=` / `Foreground=` attributes and this is a literal nested in a style setter. | Apply §5.3's mapping with the same `Style` + `DataTrigger` + `{DynamicResource}` shape the four rebound sites now use: running takes `WhiteBrush`, zero clients takes `MutedTextBrush`. One element, and it is what makes "no brand hue on the main window under flatline" true of the whole window rather than of the account rows. **Done as written.** The on-screen half is still owed: the dot reading correctly in all four themes, and specifically not green under flatline, is a checkpoint-C2 item for a human, because no test in this project loads a `Window`. | clean |
| F-089 | (v1.17 fence, item 3a) | main window, every account row — batch-selection toggle | visual | 2 | 4 | `App.xaml:53-94` — `SelectionDotStyle`'s `ControlTemplate` holds four un-themed hexes: the outer ring's `Stroke` at `:67`, the checked inner `Fill` at `:72`, the checked ring `Stroke` at `:77`, and the hover ring `Stroke` at `:81`. Three of the four duplicate a brush declared thirty lines above them **in the same file** — two are `CyanBrush`'s value and one is `MutedTextBrush`'s — and the fourth matches no slot in the dictionary at all. Surfaced by item 3a's XAML colour-literal fence, which is also the reason this is a row: that fence permits a literal only where an open finding already owns one, and nothing did. Spec §7 does not name this surface; `AX-4` and `AX-14` both touched the batch dot and were **refuted** as out of the campaign's scope, so neither left a row behind; `F-035` counts this style ("`App.xaml`'s ... 1 keyed control style") but is `clean` and is about the vocabulary not existing, not about what this one holds; `F-068` cites it the same way and its fix direction — three new button styles — would not reach a `ToggleButton` template. No ratio is quoted here on purpose: this cycle's own lesson is that a number nothing reproduces is worse than no number, and nothing measures this pair. | `ThemeService.ApplyTo` replaces brush instances in the resource dictionary; a hex written into a `ControlTemplate` is not one of them, so this toggle keeps brand cyan and a navy-grey ring under every theme including `flatline`, whose field is achromatic. It is on screen once per account, so an eight-alt user sees eight of them. Note what this is **not**: `spec.md > §6.4` checked this control and recorded it clean, because it carries its state in shape — filled versus hollow — and the label beside it reads "In batches" / "Skipped". The state survives with no colour at all. This is a theming defect, not a legibility one, which is why it is sev 2 and not higher. | Bind the four setters to `{DynamicResource}` — `CyanBrush` for the checked fill and stroke, `MutedTextBrush` for the hover stroke, and for the resting ring either `InteractiveEdgeBrush` (it is a `Cursor="Hand"` control, so WCAG 1.4.11 governs its boundary and that token is derived to clear 3:1 under any theme) or `DividerBrush` if the ring is judged decoration. Pick deliberately rather than by nearest-hex: the resting value matches no slot, so there is a real choice being made. Belongs with **F-068**'s shared-style work, which is already rewriting `App.xaml`'s control vocabulary. **Closed 2026-08-10 by v1.17 item 3b**, in the same session that opened it — the builder's call, on the grounds that this was the last always-visible brand hue on the main window and `prd.md > Story 3.1` would otherwise have shipped as a claim the cycle's own flatline captures contradicted. All four setters now bind `{DynamicResource}`: checked fill and checked ring take `CyanBrush`, hover ring takes `WhiteBrush`, resting ring takes `MutedTextBrush`. **The resting ring deviates from this row's own fix direction, and the reason is a fence, not a preference.** `InteractiveEdgeBrush` was written in first and reverted: `InteractiveEdgeBindingTests.TheDerivedEdgeIsNeverBoundToADecorativeSurface` went red on it, because that test classifies `Ellipse` as a shape and its `IsInteractive` escape reads only the element's own attributes — `Cursor="Hand"` sits on the enclosing `Style`, not on the ring. The classifier is narrow, but its own doc says it is tuned tight against precisely the *"just use the visible one everywhere, it looks cleaner"* edit, and widening an accessibility fence to admit the case being argued is that edit. So the ring took `MutedTextBrush` instead. Rest and hover stayed two distinct slots deliberately: the literals they replace were two different greys, and collapsing both to one would have deleted the hover affordance while reading as a faithful port. **Cost, named:** the resting ring is more visible than it was, on every unselected row — the value it now carries is the one the control previously used for hover. That is a visual-density change across a list of eight, and it is a C2 eyes-on item. **The artifact that proves the fix** is the same fence that opened the row: F-089's allow-list entry was retired *first* and `NoColourLiteralIsWrittenIntoAppXamlOutsideTheAllowList` watched go red on all four hexes, then green, and the ceiling dropped 101 → 97. Retiring the entry is the load-bearing half — an allow-list entry that outlives its finding is one layer up from what `NoExemptionOutlivesItsFinding` exists to catch, and leaving it would have let a future literal inherit a closed row's permission. | clean |
| F-090 | (v1.17+ rendered gate, Phase 2) | text and password inputs wherever they sit on a card surface, 8 of 14 call sites | a11y | 3 | 3 | `InteractiveEdgeBrush` is derived by `ContrastGuard.Ensure(theme.Navy, theme.Divider)` ([`ThemeService.cs:211`](../../../src/ROROROblox.App/Theming/ThemeService.cs#L211)) — against **Navy**, and only Navy. It clears WCAG 1.4.11's 3:1 there by the width of the derivation walk: 3.0699 brand, 3.0194 midnight, 3.0364 magenta-heat, 3.0258 flatline. Every built-in ships `RowBg` lighter than `Navy`, so the same edge on a card measures **2.8244 brand, 2.8075 midnight, 2.8149 magenta-heat, 2.2825 flatline** — under the floor in all four, worst in the accessibility theme. Measured as pixels by `RenderedStyleGateTests`, not computed: the numbers are sampled off a `RenderTargetBitmap` of the real control carrying the real style under the real resolved theme. Call sites counted against the tree 2026-08-10 — 14 total, 8 on `RowBgBrush`, 6 on `NavyBrush`, the two remaining matches being the style definitions themselves. | **Three separate checks each had a reason not to see this, which is why it survived.** Phase 1 (`ContrastPairGateTests`) pairs `Background` with `Foreground` and never reads a `BorderBrush`. `InteractiveEdgeBindingTests` polices which *role* the derived edge is bound to, deliberately not what value it lands at against a given surface. And **F-031 is `clean` and records 3.03:1**, which is true — of Navy. A number that is correct about one surface and silently generalised to another is the same defect class the v1.17 reconciliation spent an item removing, and this one was inside a fix rather than inside prose. Not a regression from any cycle: the derivation has been Navy-only since the edge was introduced. | Derive the edge against the surface it will actually sit on rather than against `Navy` alone — either a second derived slot for card surfaces, or a derivation that takes the intended surface as its argument. Belongs with **F-068**'s shared-style work, which already owns `ControlStyles.xaml`'s vocabulary. Until then `RenderedStyleGateTests` pins the gap per theme rather than exempting it: the card cases assert **below** 3:1 and equal to the recorded value, so the test fails if the gap widens, fails if a new theme arrives unrecorded, and fails when the gap closes — which forces the pin's deletion and the cases' promotion into the general rule. | open |
| F-091 | (v1.17 C2 walkthrough) | every plugin's own UI, and every user-authored theme | visual | 3 | 4 | Plugins are separate processes with their own WPF windows, and the plugin contract has **no theme message** — the only colour in `plugin_contract.proto` is a `color_hex` badge override at `:244`. `626labs.ur-task` 0.5.0, whose manifest advertises *"theming that follows the host"*, instead reads `activeThemeId` out of the host's `settings.json` and looks it up in **its own hardcoded palette**. Confirmed by scanning the shipped binary: it carries the ids `brand`, `midnight` and `magenta-heat`, and the hexes `17D4FA`, `0F1F31`, `F22F89`, `3FB8D9`, `1A0F1F`. It carries no `flatline`, no `101010`, no `D4D4D4`. Found by Este walking the v1.17 C2 checkpoint on a running build: three themes changed the plugin's UI and flatline did not. | **CORRECTED 2026-08-10, and the correction is mine.** This row originally claimed *"every user-authored theme is already broken in every plugin."* **That is false.** `ur-task`'s `HostThemeReader` does read user themes: it takes `activeThemeId` from `settings.json`, and when the id is not one of its mirrored built-ins it loads and parses `themes\<id>.json` off disk (`HostThemeReader.cs:158-161`), snake_case slots and all. A user theme works. The claim was asserted from a binary string-scan without reading the plugin's source, and it reached a commit message, a PR body and a dashboard decision before anyone checked. **The real defect is narrower and more interesting:** built-in themes live in host *code* and are never written to disk, so the plugin has no way to read them and must **mirror** them — `BuiltIns = { Brand, Midnight, MagentaHeat }`. Flatline shipped after `ur-task` 0.5.0, so it is absent from the mirror, falls through to a `themeslatline.json` that does not exist, and lands on Brand. The plugin's own source names the hazard: *"Mirror-drift risk: if RoRoRo's built-ins change, these copies need a matching bump."* **The coupling is worse than a palette copy.** To do this at all the plugin must know the host's settings filename, its camelCase key, the themes folder layout, the per-file snake_case naming policy, and the reader's comment/trailing-comma tolerance — five internal storage details, any of which can change without the plugin knowing. That, not the palette, is what a contract message removes. **CORRECTED AGAIN 2026-08-10, second pass, and this one is mine too.** The cell below this sentence used to repeat the very claim the correction above kills: *"any id absent from the plugin's table falls back to brand, so every user-authored theme is already broken in every plugin."* **Also false, and false for the same reason** — `ResolveActive` checks the mirror first and then falls through to `themes\<id>.json`, so an id absent from the table is exactly the case that DOES work. A corrected row that still carries the original error in a later sentence is worse than an uncorrected one, because the correction reads as verification. **This is not a flatline bug and fixing flatline would hide it.** The real shape is an asymmetry: a user theme is a **file**, so the plugin finds it; a built-in is a **record in host code**, so the plugin can only mirror it. Every built-in added after a plugin ships is therefore invisible to that plugin, silently, and flatline is simply the first one. The "+ Build a theme..." feature works *precisely because* it writes to disk, which is the one path the host does not use for its own themes. It is also a FOURTH copy of the palette, in a different repo, on top of the three hand-synced copies already recorded as an open issue — and the only copy that cannot be fixed by editing this repo. | Push the resolved palette from host to plugin over the existing gRPC channel, so a plugin never needs to know a theme id at all: a theme message in `plugin_contract.proto`, a `ROROROblox.PluginContract` version bump, a host-side push on theme change, and a matching `ur-task` release. That is a contract change to a NuGet external authors consume, and the manifest pins `contractVersion 1.0` / `minHostVersion 1.4.3`, so it wants its own design pass rather than an improvised patch. **CLOSED 2026-08-11, verified on a running build through the production path.** Five theme switches, each observed as a `host pushed a palette` line followed by an `applied 8 brushes` line in `ur-task.log`: brand `#0F1F31`, midnight `#0A1320`, magenta-heat `#1A0F1F`, **flatline `#101010`** and a user-authored theme `#151026`. The stream, not just the connect-time read. Plugin autostarted by the host after the startup gate, one instance, no sideloading. **Flatline reaching a plugin window is the thing this row was filed for and it now does.** Earlier status note, superseded: **STATUS 2026-08-11 — code landed on both legs, row deliberately NOT closed.** RoRoRo `feat/plugin-theme-feed` ships the message, both RPCs, the bus event, the adapter and the author-guide section, proven end-to-end over a real named pipe in `PluginTestHarness` (three tests written first and observed failing with `UNIMPLEMENTED`). `rororo-ur-task` `feat/theme-from-host` deletes the mirror and takes the feed. **What is not verified is the only thing this row is about:** four built-in themes plus a user theme repainting a real Ur Task window. No suite spans two processes, so that check is eyes-on and owed. Also blocking: `ROROROblox.PluginContract 0.8.0` is not on nuget.org, and neither are 0.5.0 through 0.7.0 — the plugin branch built here against a local pack. The row closes when someone has watched flatline reach the plugin, not when the code that should make it happen merged. **Adding `flatline` to the plugin's table is explicitly NOT the fix** — it is a fifth copy and leaves the user-theme case broken. | closed |
| F-092 | (v1.17 C2 walkthrough) | per-row status dot, active state, all themes | a11y | 2 | 3 | Reported by Este on a running v1.17 build: the active dot is hard to read as *on* at a glance. *"I still can't tell that it's on. It does look a little bit brighter, but it's fine."* Measured, it is doing what §5.3 designed it to: active `WhiteBrush` sits at 13.17:1 against the row under flatline and idle `MutedTextBrush` at 4.98:1, and `TriggeredStatusColourGateTests` confirms all four states resolve to distinct values in every built-in. The gap between active and idle specifically is about 2.6:1 — above the mutual-distinctness bar the gate enforces, below what reads instantly on an 8px circle. **Accepted for ship by the builder in the same breath as reporting it.** | This is the C1 trade arriving in practice, not a new defect. `CyanBrush` was rejected for active because it collides with `RowExpiredAccent` at 1.00:1 under flatline, and `WhiteBrush` was the only slot keeping all four states apart in every theme — so the dot is as distinct as the ten-slot contract allows without a new token. What makes it shippable is `SecondaryStatusText`, which states all four states in words directly beside the dot (§6.1), so the dot is a redundant echo rather than the carrier. That argument is exactly why §5.3's honest-limit paragraph declines to assert 3:1 on it. Worth recording that a human still reached for the dot first, because the redundancy argument says they should not have needed to. | Nothing this cycle. If it is revisited, the cheap levers are size and weight rather than colour — an 8px circle is small for a 2.6:1 step — and the expensive one is an eleventh slot for status, which the contract-does-not-grow invariant argues against. Do NOT reach for a second accent: that reopens the collision §5.3 rejected. | open |
| F-093 | (v1.18 fence, item 2) | `Core/IAppSettings.cs` — `DefaultPlaceUrl` | qol | 2 | 1 | `Core/IAppSettings.cs:7` says of `DefaultPlaceUrl` that *"the Preferences dialog allows editing."* It does not, and has not for more than a cycle: the App references the setting **zero** times, while `RobloxLauncher.cs:246,353` calls it "(legacy single-URL setting)" and "vestigial", and `RobloxLauncherTests.cs:362` pins that it must be ignored. Surfaced by `SettingsReachabilityTests` on its first run and exempted there with the reason inline, to keep old `settings.json` files deserializing — the exemption is a decision, not an oversight, which is the fence's whole rule. | This is F-023's defect with the sign flipped. F-023 was a setting that persisted and could not be reached; this is a setting that persists, cannot be reached, is not supposed to be reached, and carries documentation claiming it is. The doc is what makes it a finding rather than dead weight: somebody reading `IAppSettings` to find out what Settings owns is told this one is editable, and the next person to act on that adds a control for a value `RobloxLauncher` deliberately ignores. A comment that lies about reachability is exactly the failure mode `RawTheme`'s camelCase comment produced under F-053 — documentation causing the bug it describes. | **Delete the field, do not add a control.** The honest end state is removing `DefaultPlaceUrl` from the settings record and the interface, with the deserialization question answered deliberately rather than by keeping a dead property alive: `System.Text.Json` ignores unknown keys by default, so an old file with the key loads clean without it. If the field is kept for any reason, the doc comment goes first. Deliberately not opened mid-build; recorded in `spec.md > §10` and made a row here so it stops living in a spec. | open |
| F-094 | (v1.18 item 7, spec §5) | Settings — Alerts and memory, `AlertsStatusLine` | visual | 2 | 3 | `PreferencesWindow.xaml:480-481` binds `AlertsStatusLine`'s `Foreground` to `{DynamicResource CyanBrush}` — the accent, the same treatment a success gets. **And it does report failures, which is the condition `spec.md > §10` set for opening this row.** `Core/Discord/AlertStatusLine.Compose` returns *"Your webhook was deleted, so nothing is reaching your phone. Alerts are falling back to desktop only"* and the clan-channel equivalent, and its own doc comment calls that state the loudest thing this line can say because it *"has NO other discovery path"* — Discord never tells the user they deleted the webhook, alerts silently fall back to desktop, and everything looks configured. Three further arms report degraded states in the same cyan: no webhook pasted, desktop-only, and nothing routed at all. | The one message on this page that has no other way of reaching the user is painted in the colour this app uses for "worked". v1.17 established `RowExpiredAccentBrush` plus the triangle as the warning vocabulary across expired rows, idle chips, memory chips and the compat banner; v1.18 added two more speakers of it — `MemorySettingsWarning` under F-023 and `ThemeStatusLine` under F-051 — and both were written as deliberate divergences FROM this line, mirroring its mechanism and refusing its colour. So the page now carries two failure lines in the warning accent and one in the success accent, which is worse than the uniformity it replaced. Noticed while writing spec §5 and left alone on purpose: retro-fitting it was not a v1.18 row, and doing it silently is how a register row goes wrong. | Route the failure arms of `AlertStatusLine.Compose` to `RowExpiredAccentBrush` with the triangle prefix and leave the "Sending to #channel" arms in `CyanBrush`, which means the composer has to return a severity alongside its sentence rather than a bare string — the same shape `ThemeStatusSummary.Line` already ships, so there is a precedent to copy rather than a mechanism to invent. Do NOT repaint the whole line one colour: "sending to your channel" is a success and reads correctly in the accent today. | open |
| F-095 | (v1.19 C2 setup, 2026-08-11) | plugin host, every installed plugin | qol | 4 | 1 | Two folders under the plugins root declaring the same manifest id **disabled plugin support entirely**. `InstalledPluginsLookupAdapter.Refresh()` built its index with `ToDictionary(p => p.Manifest.Id)`, which threw `ArgumentException` on the duplicate key — **one statement below the `catch` whose own comment promises "a corrupt plugins root must not stop the App from launching"**, so the guard was defeated by the next line. The constructor threw, resolving `PluginHostStartupService` threw, and `App` logged *"plugins disabled this session"* at **Debug**. Every plugin gone, no message anywhere a user would look. Found by copying a plugin folder to back it up before an update — an ordinary thing to do — during the v1.19 C2 setup. | **FIXED 2026-08-11 in the v1.19 branch.** Duplicates now keep the first folder and log a **Warning** naming both paths so the offending copy is findable; the index build moved inside the guard that was supposed to cover it; the `StartPluginHostListener` swallow went Debug → Warning. Three tests in `InstalledPluginsLookupAdapterTests`, verified to fail against the old `ToDictionary` with the exact `ArgumentException` from the live log. **Still open:** the failure is a log line, not a user-visible message — a clan member who hits this still sees plugins silently missing. Surfacing it in the Plugins window is a UI change and its own row. | open |
| F-096 | (v1.19 C2 walk, 2026-08-11) | `rororo-ur-task`, any plugin | qol | 2 | 2 | A second Ur Task instance starts, shows its **full window**, and reports *"Not connected to RoRoRo"* with 0 macros and *"Startup failed: Failed to register hotkeys"* — because the first instance already holds `Ctrl+Shift+R` (win32 1409) and the window it presents is indistinguishable from a genuine connection failure. Observed during the v1.19 C2 setup when a manual launch raced the host's own autostart: two windows, one working and one that looked broken, and the reasonable read was that the app was broken. RoRoRo guards itself with a singleton mutex; the plugin has nothing. **Not caused by the theme feed** — the same race existed before it, and the theme feed is what made it visible, since the working copy was themed and the loser was not. | Single-instance guard in the plugin: a named mutex on the plugin id, and a second launch either focuses the existing window or exits quietly rather than presenting a UI that claims the host is unreachable. Worth checking whether the plugin author guide should require this of every plugin rather than leaving each author to rediscover it. | open |

## Rulings by the user (these override the register)

- **The two shipped emoji stay.** Este, 2026-08-04, on approving this register: the die on
  `Reroll identity` is thematic to streamer mode's deliberately silly naming, not decoration, and
  the same goes for the clipboard on `Copy AI prompt`. Any finding whose fix is "delete the emoji"
  is **closed as ruled**, not open. What those findings were really reacting to is **placement** —
  `Reroll identity` is a settings-shaped action sitting on the main dashboard — and that is already
  covered by the goal-1 relocation work. Invariant 5 is amended in the brief accordingly.

## Absorbed

- **VC-2** → absorbed into **VC-1** (F-025) — same root cause (fill == page bg, no shape affordance), same one-control-style-dictionary fix.
- **VC-5** → absorbed into **VC-4** (F-026) — fix direction is VC-4's fix restated; also carries a factual error (AboutWindow is also NoResize/500px, so Preferences isn't uniquely fixed or narrow).
- **VC-9** → absorbed into **VC-8** (F-064) — same missing SectionHeading treatment, same one-line fix ("deleting a local variant"); its rendered claim is also false (About's "Links" heading is visibly bolder/accent even under flatline).
- **VC-14** → absorbed into **VC-15** (F-037) — one of the four meanings VC-15 already enumerates for accent fill.
- **VC-20** → absorbed into **VC-11** (F-027) — its own fix direction defers entirely to VC-11 ("whatever replaces the two-tone header per VC-11, Welcome adopts it").
- **CO-12** → absorbed into **CO-1** (F-035) — gutter/margin drift is exact, but the entire remedy is "put the numbers in the dictionary CO-1 creates"; its independent button-padding claim was also refuted on recount.
- **CO-13** → absorbed into **CO-1** (F-035) — radius census is exact, but remedy is again "two named resources in CO-1's dictionary"; carries one bad citation (conflicts with CO-8 on what's at `MainWindow.xaml:1360`).
- **AX-9** → absorbed into **AX-1** (F-052) — same naming absence restated per-row; AX-1's fix ("no control ships without AutomationProperties.Name") already covers it; what's left sits on account rows and the Plugins list, out of scope.

## Refuted

- **VC-7** — out of scope (contested-mutex recovery, Bloxstrap strip — not settings/nav/chrome); the cited hex values render identically to their theme tokens by default, so the visible defect is nil.
- **VC-19** — out of scope (transient session-state banners, not settings/nav/chrome); cites "Rubric — similar components sharing a visual language," which is not in the measuring stick.
- **QF-14** — a feature request in a UX costume; no C1-C9 convention requires "Check for updates" and none of the four goals asks for it.
- **QF-16** — new capability request (per-row "Launch again," a filter row); no navigation or settings convention is violated.
- **CV-15** — out of scope (a launch-time interruption modal, not settings/nav/chrome); its own `convention violated` field cites CLAUDE.md rather than C1-C9 or the four goals.
- **CV-22** — merely different, not better; the proposed rewrite keeps the exact "promise-then-reassure" cadence it objects to — taste, not conformance or goal progress.
- **AX-2** — out of scope; the follow-chip strip is an account-row control, explicitly named in the brief as a carve-out example.
- **AX-3** — out of scope; the color swatch, add-tag plus, and drag grip are all account-row controls (the brief's own carve-out examples).
- **AX-4** — out of scope; the batch-selection dot is an account-row control bearing on an action, not on navigation or settings.
- **AX-10** — out of scope; container naming lives inside the account list and Plugins list contents, not in navigation, Settings, or page chrome.
- **AX-14** — the in-scope measurement is wrong. Pixel-measured off the brand capture, the nav band and per-row secondary buttons clear 24×24 at ~29px; the padding-derived "≈23px" undercounts by ignoring the text line box. What's left (the 14×14 batch dot, the swatch) is real but already refused under AX-3/AX-4.
- **AX-16** — out of scope; Diagnostics' internal label/value structure is not a navigation, Settings, or page-chrome claim, and it concedes it's the same grouping primitive AX-12 already carries in-scope.

## Corrections to the brief and the campaign state

- **Title-convention count is seven, not six.** `FriendFollowWindow.xaml` sets no `Title` in XAML, but `Friends/FriendFollowWindow.xaml.cs:133` sets `Title = $"ROROROblox -- Friends -- {ChromeName(current)}"` at runtime — a seventh convention, the only three-part title in the app, and the only one built on the repo name. The brief's XAML-only sweep missed it.
- **Invariant 5's "no emoji" rule is not already enforced — two emoji ship.** `MainWindow.xaml:302` `Header="🎲 Reroll identity"` (U+1F3B2) and `Theming/ThemeBuilderWindow.xaml:52` `Content="&#x1F4CB; Copy AI prompt"` (U+1F4CB) are both `Emoji_Presentation=Yes` codepoints, rendered in full color by Segoe UI Emoji inside monochrome navy chrome.
- **Invariant 2 ("the 626 Labs duo is never split") is breached in two shipped windows, not zero.** `JoinByLink/JoinByLinkWindow.xaml:27-33` ships cyan + white with no magenta separator, and `About/WelcomeWindow.xaml:38-43` ships white + cyan with no magenta either — the second instance the originating lens missed entirely.
- **`ROROROblox` (the repo name) leaks into user-facing copy in four places, not two or three.** `Friends/FriendFollowWindow.xaml.cs:133` (window title / taskbar / Alt-Tab), `Tray/TrayService.cs:221` ("Open ROROROblox," tray menu), `Tray/TrayService.cs:98-100` (tray tooltip, all three states — the surface a tray-resident app shows most often), and `Diagnostics/DiagnosticsWindow.xaml.cs:220` ("ROROROblox support snapshot," first line of a file users forward). All four violate the memory rule that user-facing brand is RoRoRo; ROROROblox stays for code identifiers only.

## Notes carried forward

- **The suite has pre-existing wall-clock flakiness, and it is not a register row.** Measured across
  the v1.18 cycle: **3 of 14 full runs failed on real-time-bounded tests across two classes**, every
  failure showing a roughly 30-second wall clock against a normal 10. It fired again on the
  verification run for item 11 itself — `FpsCapSettlerTests.QuietFileThenSurvivingWrite_Settles`,
  30 s, *"Pump stalled: the settler never re-read GetLastWriteTimeUtc() within 00:00:30 of real
  time"* — and the immediate re-run went green in 10 s at 1490 passed / 0 skipped. **So the number is
  4 of 15 and the direction is unchanged**; `FpsCapSettlerTests` documents the behaviour in its own
  source, and a 4-way bisect during item 7 initially implicated a new test before the identical
  config went 6/6 green. Not caused by this cycle. **No row, on the precedent directly below this
  one:** the "no test loads any XAML" note was declined a row for being test infrastructure rather
  than a UI finding, and this is the same class. F-086 is not a counter-example — that row is about a
  gate silently losing sight of a shipped UI token, a defect in the app's coverage rather than in the
  runner. The cost is real and worth naming anyway: an acceptance criterion of the form "exactly 1490
  passed / 0 skipped" is not verifiable on a single run, so any checkpoint that reads a suite result
  has to re-run before it believes a red. It belongs on the repo's own issue list, not here.

- **The reachability fence cannot express "reachable from where a user would look."** v1.18 item 2
  shipped `SettingsReachabilityTests`, which asserts that *something somewhere* in the App view layer
  can change each persisted setting. `CarefulSquadLaunch` already satisfied that from inside the
  Squad Launch modal, so **the fence would never have caught F-020** — the row whose entire complaint
  is that the only way to change squad-launch behaviour is to begin one. Item 4 added a second
  reachable edge, not a first. Its own docstring calls it a fence and not a gate; this is the
  concrete instance. **No row:** it names no defect in the app, it has no live victim now that F-020
  is closed, and it is already recorded in the test's own source and in `spec.md > §10`. Worth
  carrying here because the next cycle that reads a green fence as "every setting is findable" will
  be wrong, and the fence cannot tell them so.

- **`EdgeRemediationAnswers` is structurally invisible to any name-derived fence.** The persisted
  property is plural and its accessor is singular (`Get`/`SetEdgeRemediationAnswerAsync`), so the
  names cannot match. Exempted in `SettingsReachabilityTests` with the reason inline rather than
  papered over. No row — it is a naming asymmetry in a working feature, and the exemption is the
  decision.

- **No test loads any XAML, so CI cannot catch a window that fails to render.** Raised by wave 2's review gate. `src/ROROROblox.Tests` contains no window instantiation and no `LoadComponent` call — every test targets view-models and services. That is precisely why the `GamesWindow` `TargetType="UIElement"` bug survived from 2026-05-04 to now: a resource-resolution failure at template-apply is invisible to 1268 green tests. Wave 2's own blank-page guard is also runtime-only and uncoverable today. Fix direction is a smoke test that constructs each `Window` on an STA thread inside an `Application`, which turns "does this window even load" into a CI question. Not a UI finding, so no register row — but it is the reason two of this campaign's bugs were found by eye instead of by build.

- **Welcome-tour sentinel bug, found incidentally (QF-13 skeptic).** `MainWindow.xaml.cs:103-110` marks the `.welcome-shown` sentinel *before* checking `Accounts.Count == 0`. An upgrading user who already has accounts burns the sentinel without ever seeing the tour — worse than the original finding stated, since it means the one surface documenting six unlabelled row affordances silently never fires for that user class at all.
- **Flatline fixture definition, load-bearing for several downgrades.** Per `docs/ui-capture-checklist.md:21-23`, flatline collapses backgrounds and text colors but keeps exactly one accent — it does not collapse accent-colored elements into the text color. Several original findings (VC-8, VC-9, VC-10, and to a lesser degree others) claimed accent-colored headings or dividers "disappeared" or became indistinguishable under flatline; the captures disprove that, and those findings were downgraded or absorbed accordingly rather than refuted outright, since the non-accent half of each claim still held. **Corrected 2026-08-10:** this note cited `:8-9` until now, which is the nav-rail correction in that file's own banner, not the fixture definition. Same defect class as the rows above, so it was fixed in the same PR. **And read "flatline" here as the adversarial capture fixture**, preserved since v1.17 as `flatline-lab` in `src/ROROROblox.Tests/FlatlineLabGateTests.cs` — not the built-in theme that now ships under the id `flatline`, which is a different set of hexes and a different set of numbers.
- **QF-12 evidence error.** `Settings/SettingsWindow.xaml`'s seven `IsDefault` hits are `{Binding IsDefault}` / `DataTrigger` bindings against the game library's default-game flag, not `IsDefault="True"` button defaults. The "Enter behaves differently depending on focus scope" argument built on that citation does not hold and was struck from the surviving finding.
- **CO-12 evidence error.** The original button-padding claim ("16,8 leads with 26 uses") mislabeled all-`Padding` counts (Buttons + Borders + TextBoxes) as Button-only counts; `10,8` never appears on a Button at all. The corrected Button-only leaders are 16,8=25 / 10,6=19 / 14,8=12 / 12,6=7. Moot for the register since CO-12 absorbed into CO-1, but worth knowing if the shared dictionary's padding scale gets picked from this data.
- **AX-6's residual gap.** Even the best available theme-derived CTA foreground (navy) only reaches 4.40:1 on magenta in the shipped brand theme — still short of WCAG's 4.5:1 floor. The fix direction in F-050 needs a validation step, not just a resolver, to catch that the "pick the better of two slots" mechanism doesn't clear the bar on its own for magenta fills.
- **CO-6 is stronger than originally written.** `Navy == Bg` holds in all three built-in themes (`ThemeStore.cs:208/217, 224/233, 240/249`), not just the shipped default — the secondary-button affordance problem is structural to the app's theme model, not an artifact of one theme's specific values.

## F-031 verified and widened, 2026-08-05 — it fails in every shipped theme

Computed from the verbatim slot values in `ThemeStore.cs:202-250`, not from the
register's own evidence column. WCAG 1.4.11 requires a non-text interactive
boundary to reach **3:1**.

| theme | Navy == Bg | secondary button edge (Divider on Navy) | primary button edge (Cyan on Navy) |
|---|---|---|---|
| brand (shipped default) | yes | **1.26:1 — FAIL** | 9.39:1 pass |
| midnight | yes | **1.16:1 — FAIL** | 8.05:1 pass |
| magenta-heat | yes | **1.14:1 — FAIL** | 4.90:1 pass |

Three things this settles:

1. **It is not a flatline artifact.** The adversarial theme made it obvious, but
   the failure ships in the default theme every user has right now, at a fifth of
   the required contrast.
2. **`Navy == Bg` in all three built-ins**, so a secondary button's fill
   contributes exactly zero separation. The entire affordance rests on a
   1.2:1 hairline. Any user theme that leaves those slots equal — which every
   built-in does, so it reads as the intended pattern — inherits the same failure.
3. **The primary recipe passes comfortably.** This is not "the palette is too
   dark"; it is one recipe, used on the nav band, Remove, Reroll all identities,
   and 8+ other buttons, whose only boundary token was never contrast-checked.

This is why F-025, F-029, F-030, F-031, F-026 and C5 are one finding wearing six
numbers: they all reduce to *grouping and affordance are carried by a fill, and
the fill is the same colour as what it sits on.* Three waves have now worked
around it — wave 2's rail selection had to be a shape, wave 3's popup had to
carry a border — and each workaround was correct, local, and re-derived from
scratch because there is no shared vocabulary to fix once.

`App.xaml` holds 10 brushes, 9 converters, and **one** keyed control style
(F-035). That is the reason every fix has been a hand-copy.
