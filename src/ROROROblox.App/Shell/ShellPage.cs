namespace ROROROblox.App.Shell;

/// <summary>
/// The seven destinations the shell hosts (F-013; KnownRobloxIssues added 2026-09-23). Order here is
/// rail order: the two former toolbar peers first, then the Tools-menu destinations in their menu order.
/// </summary>
internal enum ShellPage
{
    Games,
    Settings,
    History,
    Diagnostics,
    Plugins,
    About,
    KnownRobloxIssues,
}
