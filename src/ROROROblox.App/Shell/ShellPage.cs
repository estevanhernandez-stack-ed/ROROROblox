namespace ROROROblox.App.Shell;

/// <summary>
/// The six destinations the shell hosts (F-013). Order here is rail order: the two former toolbar
/// peers first, then the Tools-menu destinations in their menu order.
/// </summary>
internal enum ShellPage
{
    Games,
    Settings,
    History,
    Diagnostics,
    Plugins,
    About,
}
